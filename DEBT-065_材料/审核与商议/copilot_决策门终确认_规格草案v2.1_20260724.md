**终判：v2.1 仍不可定稿。** 核心路线已经闭合，但还剩两组实质阻断及三处实现歧义；若直接落码，不同实现者仍可能产生不同的资产拒绝、条目失效和测试行为。

| 项 | 判定 | 独立核查 |
|---|---|---|
| 1. 三阶段异常矩阵 | **部分闭合** | 原先“指纹失配既整包硬失败又跳过条目”的直接冲突已消除：红线 7 规定失效条目不早退，矩阵及 §2.5 均把内容哈希失配归为条目失效，见 `团队文档\我的笔记\spec草案_DEBT065类型格与授权状态轴_v2.1_20260724.md:35-45,113-117`。但矩阵漏列 `authoring_revision`、`interpretation_revision`、顶层 `rulecard_bundle_id` 失配；§2.5 又明确把它们归为 `stale_card_binding`，两处覆盖范围不一致，见同文件 `:44,114`。此外矩阵的运行期缺席只列类型格和授权表，却漏掉第三项别名资产，见 `:45,90`。 |
| 2. `card_fingerprint.v1` | **哈希算法闭合，来源选择未完全闭合** | 原始单卡对象、排序键、分隔符、字符编码及“不哈希知识图谱重建对象”均已定死，可唯一计算，见同文件 `:113`；真实卡包也确为 `bundle_id + cards[]`，见 `agent_v1\regulations\rulecard_v2\mbis_cop_2023\rule_cards.json:1-5`。但未规定卡包内重复 `rule_card_id` 必须硬失败；现有加载器只是普通 `json.load`，随后按卡标识建节点，见 `agent_v1\src\evo_agent_baseline\ingest\rulecard_loader.py:114-117,130-173`。重复时“该单卡对象”不唯一。 |
| 3. W0 身份通道 | **闭合** | 通道名、载荷、来源、别名版本与快照绑定均已明确；尤其规定对原始关系先做 `count==1`，禁止规范值去重和后写覆盖，见规格 `:121-135`。这正面封住现行查询可返回多行、旧代码字典覆盖的问题，见 `agent_v1\src\evo_agent_baseline\kg\queries.py:30-36`、`agent_v1\src\evo_agent_baseline\retrieval\fact_retriever.py:490-502`。 |
| 4. 触发器授权漏洞 | **特定漏洞已闭合** | 授权条目只能有一个目标、每卡组件值单一，触发器值还必须与授权目标恒等，见规格 `:113-117,150-158`。因此 `{A,B}` 授权集根本无法通过加载，原 `A⊆{A,B}` 路径不再存在。须注意：授权目标仍依赖人工裁定，单组件值本身并不能证明它不是证据宿主；这是正向授权资产的职责，不是该判据的漏洞。 |
| 5. 二分测试 | **未完全闭合** | 断言 A 可直接执行，词表十值可核对，见规格 `:81-84`及 `controlled_vocabularies_v1.json:16-26`。断言 B 的事实账也正确：生成计划确有 11 种，见 `generator.py:303-365`；别名表覆盖这 11 种，见 `projection_runtime_mapping_v1.json:50-61`；注册表共有 19 种，见 `registry.py:1921-2149`。但规格只写“11/8 两个显式清单”，没有列出成员、清单落点，也没有明确断言“可生成清单 == 实际计划类型集合”。实现者仍可写出不同清单而仅靠并集测试通过。 |
| 6. 楼级与位置维 | **闭合** | §3.2 已明确只废止“组件维楼级结构 NA”，§5 明确保留位置维，见规格 `:152-158,168-172`，不再冲突。 |

**新引入或遗留的实现歧义：**

1. `alias_mapping_version` 写成“与 lattice 冻结版本同键”，可能被理解成类型格版本，而正确对象应是类型格字段 `alias_mapping_version`，见规格 `:66-68,129-133`。
2. 类型格仍含 `rulecard_bundle_id`，但又声明类型本体不随卡修订升版；该字段是强校验还是仅作来源注记、失配如何处置，未定义，见规格 `:60-77`。
3. 异常矩阵把“非法 evidence 格式”列为整包硬失败，但授权证据只有示例，没有必填字段、类型、枚举及引用存在性规则，见规格 `:43,101-108`。

**剩余阻断：**

1. 补全异常矩阵及授权资产结构契约：覆盖修订字段、两类 `bundle_id`、别名资产缺席、重复卡标识和证据格式。
2. 把注册表 19 项的 11/8 成员清单及相等断言写死，不能只写数量和并集。
3. 消除 `alias_mapping_version` 与类型格版本的措辞混淆，并说明类型格中 `rulecard_bundle_id` 的校验语义。

本次全程只读，未修改文件。终判：v2.1 仍不可定稿。 核心路线已经闭合，但还剩两组实质阻断及三处实现歧义；若直接落码，不同实现者仍
