# 规格版本与发布口径

状态：W0 静态资源层封口版。

跨包权威源与负向不变量见：`../_封口总则_字段权威源与负向不变量.md`。如本章与总则冲突，以总则和字段所属包权威章节为准。

## 1. 包版本口径

本包与 W1 / W2 共同构成数据生成层封口版。W0 负责 19 张 registry、实例对象字段、measurement / sidecar contract、surrogate / formula 与约束口径。

## 2. 对外汇报口径

- W0 registry 总数：19 张。
- W0 不消费 rule_card v2。
- W0 不生成 `NormativeProjection`，也不维护 W2 输出字段表。
- W0 保留的旧蓝图结构均已在 `../_封口总则_字段权威源与负向不变量.md` 中标明保留 / 偏离。

## 3. 禁止写入本章的内容

- 实现状态。
- 测试状态。
- 工程任务、实施状态或推进记录。
- 等待外部专家确认的 pending authority。
