# 实施记录：换池捆绑批三件（乙路 A+B ／ 分布参数八槽 ／ 乙12 甲案含 `.to_ba`）

- **日期**：2026-08-05
- **工单**：`实施工单_换池捆绑_20260805.md`（四步）
- **权威依据**：`决议_乙路_20260805.md` ／ `决议_分布授权_20260805.md` ／
  期限锚案三线全票甲（`审核结果_{official,qwen,glm}_期限锚实施_20260805.md` 乙12 节）
- **性质**：本单**不跑生产池、不连 Neo4j**；池 v2 生成与灌库是下一单。

---

## 〇、术前备份（铁律第一条，三行）

| # | 内容 | 落点 |
|---|---|---|
| 1 | 已跟踪改动 patch | `scratchpad/backup_20260805/tracked_20260805.patch`（631 KB） |
| 2 | 未跟踪文件清单 | `scratchpad/backup_20260805/untracked_list_20260805.txt`（135 条） |
| 3 | 未跟踪文件打包 | `scratchpad/backup_20260805/untracked_20260805.tar.gz`（764 KB） |

术前 HEAD：`8c386907bb3247a371604f7541bfcc1bb818ca39`。

---

## 一、步 1：乙路 A+B（两侧同批，无只落一侧的中间态）

### 1.1 改动面

| 侧 | 文件 | 改了什么 | 进指纹？ |
|---|---|---|---|
| **A（真值侧）** | `projection_runtime_mapping_v1.json` | `card_overrides` 里 s2_1_3_n 意向卡的 `extra_trigger_predicates[0].slot_id`：`procedure.investigation.intention_notified` → `procedure.investigation.detailed.intended`（**一个字符串**；`partition` / `owning_interface_ids` 不变——实查两槽同属 `sidecar_bool_slot_registry`、同 `carrier_domain=procedure`、同楼级粒度） | ✖ |
| **B①** | `semantic_slot_registry_v1.json` | 登记 `.intended`（`semantic_domain=procedure`、`allowed_roles=["trigger"]`）；语义说明里逐字写明「**不得**与 `intention_notified`（该义务的履行）混同」 | ✔ |
| **B②** | `rule_cards.json` | 意向卡 `slot_role_map` 新增 **sr02**（slot=`.intended`、roles=`["trigger"]`、required=true）＋ `trigger_conditions.items` 新增 **trg01**（`==true`，引用 sr02） | ✔ |

**未动**：sr01（证据通道）逐字节不变；`obligation_graph.nodes[*].trigger_condition_ids` 不动
（求值器按 (卡, 作用域) 聚合**卡级** trigger，与节点是否自报依赖无关；工单 B② 也只授权两处）。

### 1.2 九步清单逐步结果

| 步 | 动作 | 结果 |
|---|---|---|
| 0 | 术前备份三行 | ✔ §〇 |
| 1 | **先写重核准记录再刷指纹** | ✔ `重核准记录_换池捆绑_20260805.md`；写记录时两表 sha 仍是旧值 |
| 2 | A 侧字符串改 | ✔ 实查 `card_overrides` 全仓**唯一消费者**是 `workflow_engine/regulation_projection_contract.py:67`，`evo_agent_baseline/` 零命中 ⇒ 闭包侧结构上看不见它（决议 §一.2 亲验复现） |
| 3 | B① 语义槽登记 | ✔ 语义槽 66 → **67** |
| 4 | B② 卡本体 | ✔ 卡数 **469 不变**；卡指纹 `3305088e…` → `f6de9536…`（落盘后实算，与内存预算逐字相同） |
| 5 | `rulecard_v2 rebuild` | ✔ 族 57 / 卡 469 / **槽 190→191** / regime 41 / 定义 1 / 例外 0 |
| 6 | `check_rulecard_derived_release.py --rebuild` ＋ 无参复核 | ✔ 三份重建，**3/3 与生成器一致** |
| 7 | `build_card_applicability_manifest.py --check` | ✔ 卡总数 469、**已授权 82 不变**、强校验全过 |
| 8 | 双池 bundle `--force --auth-asset` 重建 | ✔ 重建**前** `--verify-only` 两池均报 `card_applicability_manifest digest 不符`（证实必须重建）；重建后两池自校验通过 |
| 9 | 发布门禁 `check_applicability_release.py` | ✔ 两池各 **12/12 PASS，失败 0** |
| 10 | **A 表 row 116 ＋ 桶表 row 208 两行 sha 同批刷** | ✔ 同卡同 sha 全仓**恰三处**（两表 ＋ manifest；manifest 走脚本重建，不手改）。其余命中全在 `experiments/_applicability_assets/*/archive/` 历史归档（派生物，不动） |
| 11 | 两表回读 | ✔ A 表 `ACTIVE 125 / STALE 0 / DISABLED None`；桶表 `ACTIVE 207 / STALE 0 / DISABLED None` |
| 12 | 收尾双审计器 | ✔ §四 |
| 13 | 全量 pytest | ✔ §四 |
| 14 | KG 重灌 | ✖ **押后**（本单边界明写不连库；且新语义槽要进运行时切片必须重灌 —— 见 §六 尾巴 1） |

### 1.3 锚沿革

| 锚 | 术前 | 术后 |
|---|---|---|
| 卡指纹（意向卡） | `3305088ef10defca2c699b44ef134ea0df36d9d86bf1c80f6d84fda244db9dad` | `f6de95365997c74626851cf2f493f7a1a112126ab5ee1fc1af0d2c9872c45148` |
| A 表 `registry_digest()` | `516a9052a592818bdb6e4cb4161d0bf2eb5398c9f0e3b3648eab566a6a24ff37` | `b48feb11efad8a6f70fc23a5aaf59e669cbd75bace5ffc390646ae5ef68cc494` |
| 桶表 `registry_digest()` | `0d83ffa8393a3cc30316d499d899b6390d135d26294c72fa44b329973228a1dd` | `061fb61b95a1250964b6ab172079d973a8f8ca9a67f3545fe3a9c0350ccebd7d` |
| `rulecard_pack_sha256` | （随卡改动重算） | `114c04a7a047968a03dde20ae7efdc402b13417586de58fbb56351ac4e3bdd06` |
| 双池 bundle sha256 | — | 301 `f23b13a95108405b22f3aafc58c2c9122f0f3519f2fbcb4fe78432ebf59b4188` ／ 401 `3f9544fa03b3cbbec380f11c2c551b47df0b5ba9baa05085cfce3df176446bf4` |
| `LICENSE_DEPENDENCY_SHA256` | `a5f88a43ec91f67d…` | **未动** |

> 术前两表 digest 由「把新 sha 换回旧值后重算」得到（本次改动只碰这一个字段），
> 不是从别处誊抄。

### 1.4 金丝雀（**先验后写盘**，`canary_yi_route_intention_trigger.py`）

六格 ＋ 变异对照 ＋ 循环前件反例格 G 的完整结果见
`重核准记录_换池捆绑_20260805.md` §四。三条结论：

1. **A 格（触发 false）动作面 0 条** ⇒ 整卡本作用域不适用；变异对照下现状是 5 条 ⇒ 非空真；
2. **G 反例格成立**：换回 `intention_notified` 后，「义务未履行」的世界里整卡塌成不适用
   （动作面 0 条），换真前件后同一世界义务照常生成（5 条）
   ⇒ 「拿履行当前提 ⇒ 违反面结构上不可达」**变成可执行事实**；
3. 批面重放（冻结批 30 栋）：**适用 30 → 10 栋**，与决议 §一.5 预注册期望逐条命中。

### 1.5 🔴 一处预注册期望必须订正（不是失败，是基数已变）

决议 §一.5 写「evidence satisfied 27→8/10（sr01 未动）」。**实测两臂都是 0 条**：

- **现状臂（不改卡）就已经是 0**（30 栋全 unknown）；
- 原因**与乙路无关**：当前工作树里同批未提交的「判定诚实」改动（产物态许可闸）
  已把本卡证据通道整体改判成 `open / artifact_state_not_valid_evidence`。
  那个「27」是**冻结批产物（HEAD 代码）**的数字，工作树上已不存在。

⇒ 换池批验收的预注册值订正为 **`evidence satisfied 0 → 0`**；
「乙+丙则 2/10」同样失去基数，一并作废。**可引的仍是「适用 30→10」。**

### 1.6 B4 交互（决议 D6 合一裁定，照办）

- **性质断言不动**：丙类三锚零确定判定，仍是性质断言、不锚条数（本单实测仍为 0，§三.1 ③）。
- **条数期望（159→139 等）不改冻结测试常数**，作为换池批预注册值记在本记录（§三.3）。

---

## 二、步 2：分布参数落 `registry.py`（8 槽 ＋ 连带）

按 `决议_分布授权_20260805.md` §一表逐槽落，**占位标 `PLACEHOLDER_pending_distribution_authorization_20260805` 已全部撤除**。

| # | 槽 | 分布 | mean / sigma | typical | `distribution_source` 尾段 |
|---|---|---|---|---|---|
| 甲1-4 | `duration.notification.{appointment_ri, nomination_temp_ri, nomination_temp_ri_terminated, role_ri_terminated}.to_ba` | `rounded_truncated_normal` | 4.8 / 2.6 | [0,20] | `…_notification_family_pre_S2_5` |
| 甲7 | `duration.submission.repair_revision.to_ba` | 同上 | 5.5 / 1.8 | [0,20] | `…_submission_family_revised_proposal_component` |
| 甲9 / 甲10 | `duration.notification.{appointment_supervising_ri, supervision_team_changed}.to_ba` | 同上 | 4.8 / 2.6 | [0,20] | 通知族同上（**边界例注记已写进 `semantic_note`**） |
| 乙11 | `duration.delivery.repair_revision_proposal` | 同上（**裸正态，不写零膨胀标签**） | 0.45 / 1.15 | [0,3] | `…_same_day_shape_aligned_to_duration_delivery_deadline_to_person` |

连带三项，全部照决议落：

1. **甲9/10 边界例注记**：守则一个期限盖住「通知＋呈交」两个动作，归呈交族也说得通；
   两档实测合规率 0.8510 vs 0.8671（n=30 期望违规 4.47 vs 3.99）⇒ **该分歧不承重**。
   glm 线的 3.5/1.6「监督流程更紧」系工程直觉发明的区分，**不采**（不发明规范）——逐字写进条目。
2. **乙11 不写 `zero_inflated_discrete`**：`semantic_note` 写明「0 值占比 ≈0.5161 是
   **派生结果**、不是被授权的参数」，并说明两种零膨胀写法各自会怎么让「授权的 π0」
   与「实际 0 值占比」对不上。
3. **Q3 两收件人共用一条量**：`semantic_note` 逐字写明「本槽以一条量承载 §2.1.3(p)/(q)
   两个收件人，两支永远同判，真实偏差不可建模」＋ 指向债台账（§五）。

另在八槽组头注释里落了**决议要求的合规率来源声明**（≈85% 是工程估计、
不反映香港真实合规率、不是标定目标、S2.5 标定档 4.1/1.9 对本组不适用、
该 85% 唯一功能是让合规与违规两侧在 n=30 都有代表）。

### 2.1 🔴 一处权威文本不一致，按决议自己的委托条款裁定（须复核）

`决议_分布授权_20260805.md` §一表的「typical / physical」列把甲类 physical 写成 **[0,30]**，
而同决议 §一末尾委托的逐槽替换文本（官方线商议 §六）写的是
「**`physical_bounds` 全部不动**（甲类 [0,60] ／ 乙11 [0,30] ／ `.to_person` [0,14]）」。

- **按委托条款取后者**（决议明写「逐槽替换文本见官方线商议 §逐槽节」）；
- **差异对采样零影响**（已核机制，不是推断）：`_sample_typical_distribution` 先 clip 到
  `typical_bounds` 再 clip 到 physical，typical 上界 20 < 30 ⇒ physical 取 30 还是 60
  逐字节无差别；
- 该说明已同批写进 `registry.py` 组头注释，**不留在只有本记录才看得到的地方**。

⚠️ 这一条**不属于「三份决议互相冲突」**（那要停下回报），是**同一份决议内表格与
其委托条款的转写差异**；但仍在此显式标出，供复核。

---

## 三、步 3：乙12 甲案 ＋ `.to_ba` 同批楼级化

### 3.1 改动面

两条目各加 `"granularity": "building"`（`duration.delivery.deadline.to_person` ／ `.to_ba`），
**分布参数一个不改**（描述单次事件，与抽几次无关）。另按决议连带：

- `.to_person` 的 `recommended_distribution` 由 `zero_inflated_discrete` 改
  **`rounded_truncated_normal`**（**零采样影响**：本条没有 `calib_zero_prob`，
  零膨胀分支根本不进，实际一直按 normal 采；唯一副作用是发射行 notes 的
  `distribution=<名字>` 文本变化）；
- `.to_person` 的口径陈述改「**每栋合规率**」（0.5161 ⇒ n=30 期望 15.5 栋合规），
  并写明「⛔ 绝不能把每碎片 52% 乘起来当楼级合规率（0.52⁷≈1%）」。

`.to_ba` 同批楼级化的裁定依据逐字写进条目：qwen 审核建议 ＋ glm 同意在案 ＋
**官方线证伪了 glm 的旧反对理由**（`sidecar.py` 对 `granularity=="building"` 的数值槽
**直接跳过逐片段路径** ⇒ 改楼级后根本不存在碎片行，不存在「楼级行＋碎片行双候选」）。

### 3.2 测试面

`worldgen/tests/test_deadline_anchor_emission.py`：

- 原「碎片粒度 2 条**记录性断言**」`test_b6_fragment_granularity_anchored_slots_are_multi_row`
  按其自注（「乙12 甲案落地时该改断言而非回滚裁定」）**翻成楼级硬断言**
  `test_b6_no_fragment_granularity_anchored_slots_remain`：
  ①带锚条目里**不许再有碎片粒度的**；②乙12 射程两槽**必须在楼级带锚集合里**
  （防 B6 主断言静默少验两条）；③碎片发射路径对这两槽**一行都不许产出**；
  ④楼级路径对两槽**各恰 1 行**。
- 逐（楼,锚）恰 1 行由 `test_b6_one_building_row_per_anchor` **自动接管**（射程现算）。
- 模块 docstring 同批改写（不打补丁）。
- 楼级带锚条目 8 → **10**；`test_c4_anchor_declarations_are_not_empty` 的
  `expected >= 8` 仍成立。

### 3.3 重放预注册（scratchpad 冻结批重放，判据打印）

工具：`scratchpad/replay_yi12_building_granularity.py`（**只读、不连库、不生成生产池**）。
口径与 `实施记录_期限锚_20260805.md` §三 supply 臂同源，唯一差别是多一条对照臂：

- **base 臂**＝8 条新期限锚槽注入楼级行 ＋ 两槽保持**碎片行**并回填锚（重现乙12 翻车形状）；
- **甲案臂**＝两槽碎片行整体撤掉，换发射器现算的**楼级 1 行**。

| # | 判据 | 实测 | 判定 |
|---|---|---|---|
| ① | 乙12 锚 `repair.completion_report.submitted_to_ba` 的 60 条从 `ambiguous_fact_binding` → 绑定成功 | 60 条：`ambiguous 60 → 0`；甲案臂**确定判定 60**（satisfied 30 ／ violated 30，**上界口径**） | ✔ |
| ② | #8 锚 `repair.prescribed.completed` 的 93 条改绑楼级单行后**每栋内判定一致** | 义务数两臂同为 93；**（栋,卡）内判定不一致 base 15 → 甲案 0** | ✔ |
| ③ | 丙类三锚 159 恒等 | base 159 ／ 甲案 159，甲案确定判定 **0** | ✔ |
| ④ | `s2_1_3_r` 触发器零扰动 | 两臂逐条相同 `True` | ✔ |

**② 值得单独记一笔**：qwen 在期限锚审核里只是**推断**「同一栋楼同一个『送交 BA』事件，
机制上允许跨碎片判出不一致」——本次实测坐实了它：**base 臂 15 栋真的出现
同卡（`s2_1_3_r`）内 satisfied ＋ violated 并存**，甲案后归零。
这条从「担心」变成了「量到的缺陷 ＋ 修好的证据」。

⚠️ **上界口径**：satisfied/violated 的拆分由**已授权但未标定**的分布决定；
可引的只有「多少条脱离 unknown / 多少条产出确定判定」这两个结构性数字。
⚠️ 甲案臂 #8 的 satisfied/violated 由 70/23 变 85/8 —— 这是「一栋一次采样取代逐碎片采样」
的**预期效应**，不是判定变准的证据。

---

## 四、步 4：总验收

| 项 | 结果 |
|---|---|
| 全量 pytest（`workflow_engine` ＋ `evo_agent_baseline` ＋ `agent_v1/tests`） | ✔ **3,424 通过 ／ 1 skipped ／ 11 xfailed ／ 0 失败**（817 秒）；改红六处见 §四.1 |
| 卡包加载与指纹链断言 | ✔ A 表 `ACTIVE 125 / STALE 0`；桶表 `ACTIVE 207 / STALE 0`；两表 `DISABLED_REASON=None` ⇒ **`stale_rows == 0`** |
| 发布门禁两池 | ✔ 301 / 401 各 **12/12 PASS，失败 0** |
| 金丝雀全绿后才写盘的顺序证据 | ✔ 金丝雀（含批面重放）跑完并全绿**之后**才动 `rule_cards.json`；重核准记录写在刷两行 sha **之前** |
| 收尾双审计器 | ✔ 见下 |
| 与 #29 ／ 期限锚已落资产零冲突 | ✔ 两案的测试全绿（`test_deadline_anchor_emission` / `test_deadline_anchor_supply` / `test_actor_role_crosswalk` 等） |

**收尾双审计器（术前 → 术后）**：

| 审计器 | 术前 | 术后 |
|---|---|---|
| `audit_rulecard_modality_drift.py` | 卡 469 ／ 条款引用 480 ／ 已覆盖 206 ／ **漂移 37（18.0%）**，降级 34 ／ 升级 3 | **逐项相同** |
| `audit_action_slot_wiring.py` | 卡 469 ／ 义务节点 487 ／ 世界侧槽名池 **66** ／ 无 evidence 绑定 **101/487 = 20.7%** | 槽名池 66 → **67**（新登记的 `.intended`），其余**逐项相同** |

⇒ 乙路**没有**改变任何一条情态判定，也**没有**改变满足绑定面 —— 与「本件只补前件、
不碰证据通道」这个主张一致。

### 4.1 全量 pytest ＋ 改红的六处冻结常数

首轮 **11 红**，全部是**冻结计数**，逐项归因后重锚（先量后冻）：

| 文件 | 常数 | 旧 → 新 | 归因 |
|---|---|---|---|
| `test_rulecard_v2.py` | `semantic_slot_registry_count` | 66 → **67** | B① 登记 `.intended` |
| 同上 | `slot_count` | 190 → **191** | sr02 新开一个 (slot_id, 空限定符, roles=["trigger"]) 桶 |
| `test_source_coverage_matrix.py` | `trigger_items` | 428 → **429** | 卡级 trg01 |
| 同上 | `slot_roles` | 877 → **878** | sr02 |
| `test_blueprint_deriver.py` | `by_channel["trigger"]` ／ `["slot_role"]` ／ 合计 | 428→429 ／ 876→877 ／ 3048→**3050** | 恰两条通道各 +1，**其余七通道逐位不变** |
| `test_blueprint_state_eval.py` | 配对数 | 2564 → **2566** | 同上两条各产一条配对 |
| 同上 | 入口合一净 key | 2138 → **2139**（**只 +1**） | 两条在净 key 口径下折成同一个 key（trigger 项经 `slot_ref_id` 引用的正是 sr02）——这正是该门槛要测的 dedupe 形状 |

**「未变的项即证据」**：卡数 469 / 族 57 / regime 41 / 量表 28 / 证物 25 / 时间锚 19 /
`workflow_artifact` 335 / `workflow_deadline` 25 / `evidence` 370 / `obligation_graph` 503 /
`recipients` 75 / `nodes` 487 / `edges` 4 全部逐位不变。

**第 11 红另有性质**——`test_slot_alias_reconciliation.py::test_known_gap_inventory_is_exact`
报 `.intended` 是「新增缺项」。查实**不是缺口，是对账锚批陈旧**：
该测试的世界侧锚是 `baseline_batch_final_seed301`，实测该批 30 栋里 **0 栋**有该槽；
而当前批 `wave1_closing_seed401_20260804` 30 栋里 **30 栋**都有（真 10 ／ 假 20）。
按甲/乙/丙任一分类都会写下一句错话，故新开**丁组 `GAP_ANCHOR_BATCH_STALE`**，
注释里写清两批并排实测与「换锚属独立小案（会同时移动甲乙丙三组的判定基础）」。

---

## 五、随定稿落库的债（决议要求，不留草案）

1. **乙11 建模保真度缺口**：一条量承载 §2.1.3(p)/(q) 两个收件人，两支永远同判，
   「交了承建商没交业主」不可建模。解法＝拆两锚两槽（规则扩展另案）。
2. **`duration.notification.deadline` 本体仍挂 S2.5 标定档（4.1/1.9，96.3%）**，
   与新六条同族不同参（85.1%）将长期并存 —— 下次换池批一并复审「标定档要不要退役」。
3. **载体闸缺口**：乙11 的 `procedure.repair.revision_proposal.submitted_to_ba` 与
   甲9 的 `procedure.repair_supervising_ri.appointment.completed` 在 ownership_map 有登记、
   **不在 `sidecar_bool_slot_registry` 实采清单** ⇒ 从未产出。属「140 声明 vs 46 实采」族。
4. **`_sample_typical_distribution` 注释与行为不符**：注释写「保证尾部不取 0」，
   实际只 clamp 负值、挡不住下游 round 把 0.4 变 0 —— 将来若要用零膨胀写法，这是必须先修的坑。
5. **W2 override 与世界注册表对账闸**（决议 §一.4 ①，优先级高于乙路本体）：
   首版形态＝强制 override 引用槽必须已登记（机械可判）＋「许作前件」白名单人工冻结；
   **别在首版承诺「履行槽机器识别」**。本单落地后 `.intended` 已登记，但**闸本身仍未建**。
6. **C2 report 轴供给独立性缺陷**（决议 §一.3 另立新案）：射程 51 条契约行 ／ 424 条 satisfied ／
   16 张卡，意向卡 27 条只是实例。**本单不碰**。

---

## 六、尾巴（明示，不做隐形欠账）

1. 🔴 **KG 重灌押后**。新语义槽 `.intended` 要进**运行时切片**的 `semantic_slots`
   必须重灌 KG（`retrieval/pack_builder.py` 的 `semantic_slot_dto_from_node` 从 KG 节点读）。
   ⚠️ **不重灌不影响触发器求值**（触发器走 `slot_ref_id → slot_role_map → slot_id → 事实`，
   不查 `semantic_slots`；`semantic_slots` 只喂 `_slot_domain` 的碎片作用域判据，
   而 `procedure` 域不在 `_FRAGMENT_DOMAINS` 里 ⇒ 本卡作用域判定不变）。
   但**下一单（池 v2 生成 ＋ 灌库）必须带上它**，否则切片里永远缺这条登记。
2. **`verify_rng_isolation_pairing.py` 的层一字节锚对本单不适用**：`.to_person`/`.to_ba`
   行数从每栋 7–8 行变 1 行，「既有单元逐字节不变」必红 —— 这是甲案的**预期影响**，
   官方审核已明确「那是本步的纯净性判据，不是架构不变量，在换池批里本就不适用」。
   本单**未跑该脚本**；换池批要跑的话判据须先改成「两槽行数按预期变、其余单元不变」。
3. **换池批预注册期望表**（换池批真实数字与这些对账）：
   - 乙路：**适用 30 → 10 栋**；`evidence satisfied 0 → 0`（§1.5 订正后的值）；
   - 乙12：锚 `repair.completion_report.submitted_to_ba` **60 条 ambiguous → 0**、确定判定 **60**（上界）；
   - #8：锚 `repair.prescribed.completed` 93 条，**（栋,卡）内判定不一致 15 → 0**；
   - 丙类三锚 **159 恒等、确定判定 0**（性质断言，不锚条数）；
   - 分布：八槽 `distribution_source` 全表**零 `PLACEHOLDER`**；
     反标签装饰闸——任何 `zero_inflated_discrete` 条目必须带 `calib_zero_prob`。
   ⚠️ 这些是**冻结批注入口径**；换池批是重生成，两张表口径不同，
   旧表须原地标「沿革」不许原位改（决议 §二.5）。
4. `check_batch_acceptance` 门限随分母漂移未核（#25 遗留，**换池批验收前必核**）。
5. 本单新增两个脚本：`agent_v1/scripts/canary_yi_route_intention_trigger.py`（入库，
   是本项目改卡验收协议的第三个夹具）与 `scratchpad/replay_yi12_building_granularity.py`
   （scratchpad，一次性重放）。
