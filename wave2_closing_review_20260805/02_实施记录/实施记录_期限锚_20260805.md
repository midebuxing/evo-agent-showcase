# 实施记录：期限锚供给案（2026-08-05）

**权威依据**：`决议_期限锚_20260805.md`（六决策点全票＋形态 C 两线复核定案）
／工单 `实施工单_期限锚_20260805.md`。
**备份**：`杂物箱/备份_期限锚_20260805.patch`（动手前的完整工作树 diff，3888 行）。
**未连 Neo4j**；验证池与复算脚本全部落 scratchpad（仓库外）。

---

## 一、改动清单（文件:函数级）

### 判定侧

| 文件 | 位置 | 改了什么 |
|---|---|---|
| `closure/obligation_deriver.py` | `evaluate_deadline` `same_day_as` 分支 | **R1**：`_canon_truthy` → `== 0` 数值比较（浮点/整数同形）。非数值输入落 `open/missing_time_anchor`。判据来源写进 obligation `notes`（`deadline relation structural constant`），不静默硬编码。**`_canon_truthy` 本体未动**（另有 5 个跨 kind 消费点）。 |
| 同上 | 模块级新常量 `FORBIDDEN_DEADLINE_ANCHOR_SUPPLY` | 丙类三锚禁供名单（frozenset），逐条附中文守则原文与禁供理由。 |
| 同上 | `_bind_deadline_fact` | 返回类型 `Optional[FactAtom]` → **`(fact, status)` 二元组**；新增**级 0 provenance 通道**（优先于槽/量表两条老通道）；碰撞策略：0 条→落老通道／恰 1 条→用／**>1 条→`"ambiguous"`，禁 `facts[0]`**；丙类锚点不经本通道取值。 |
| 同上 | `evaluate_deadline` 绑定调用点 | `status == "ambiguous"` → `blocked / ambiguous_fact_binding`（**复用既有原因码**，消费者文案「候选事实不唯一，系统拒绝任取其一下结论」逐字适配，零新码、零消费者文档改动）。 |
| `closure/fact_binding.py` | `FactIndex.__init__` / `_build` | 新增 `time_anchor_index`（按 `provenance.time_anchor_key` 建，**不过别名归一**；解释性事实照 slot/measure 同款分流；**不做载体过滤**——过滤会在载体形态变化时静默丢行）。 |

### 世界侧

| 文件 | 位置 | 改了什么 |
|---|---|---|
| `worldgen/registry.py` | `.to_person` 的 `rule_card_threshold` | **R5**：锚名 `repair.completion_report_and_mbi4.submitted_to_ba`（锚点册外的孤儿字符串）→ `repair.completion_report.submitted_to_ba`；`unit` `calendar_day` → `day`。 |
| 同上 | `sidecar_measurement_registry.records` | **新增 8 条** duration 条目（甲 7 ＋ 乙 #11），全部 `granularity: "building"`、带 `rule_card_threshold.time_anchor_key`、逐条附中文守则原文与条款号。 |
| 同上 | `_build_sidecar_contract` ownership_map | 8 个新槽登记（`carrier_slot` FK 完整性所需，`checks.py:1105`）。 |
| `worldgen/sidecar.py` | 新 `_registry_time_anchor_key` | 从注册表条目取锚点，回写进发射行。**「回填两例外」在此生效**。 |
| 同上 | `_sample_sidecar_facts_for_fragment` | ①`granularity=="building"` 的数值槽跳过逐片段路径；②`time_anchor_key=None` → 从注册表取。 |
| 同上 | 新 `_sample_building_deadline_anchor_facts` ＋ `_building_deadline_anchor_records` | 楼级独立追加发射步骤（形态 C）：逐（楼,槽）恰 1 行、`granularity:"building"`、**不打 `aggregation` 标记**、`source_refs=[world_id]`、槽级子 rng。 |
| 同上 | `_sample_sidecar_bool_slots_for_building._emit` | `time_anchor_key` 同样从注册表取（bool 槽今天无 `rule_card_threshold` ⇒ 恒 None ⇒ 输出逐字节不变；接线是为了不变量对两条路径同时成立）。 |
| 同上 | `_build_sidecar_runtime_bundle_for_buildings` | 调用点插在 `_emit_scope_declaration_rows` **之后**——`seq_no` 是桶内下标，插中间会把既有楼级行 `fact_id` 整体位移。 |
| 同上 | `DEADLINE_ANCHOR_DURATION_SLOTS` | 惰性求值代理，**从注册表现算**（不写第二份名字清单，避免第二权威漂移）。 |
| `worldgen/rng_domains.py` | 新域串 `SIDECAR_DEADLINE_ANCHOR` | 与 `SIDECAR_NUMERIC` 分开（本步无 fragment 维，共用会靠"恰好不撞"）。 |

### 验证工具与测试

| 文件 | 改了什么 |
|---|---|
| `scripts/verify_rng_isolation_pairing.py` | 新 `--step deadline` ＋ 专用细尺 `_verify_deadline_sidecar_delta`（整表级 allowed/禁止二分对本步太粗）。 |
| `closure/tests/test_deadline_anchor_supply.py`（新） | B1/B2/B3/B4/B5，共 21 例。 |
| `worldgen/tests/test_deadline_anchor_emission.py`（新） | C4 静态闸 ＋ B6 ＋ 形态 C 形状断言，**16 例**（M1 后：原 14 例 ＋ 发射覆盖闸 ＋ 碎片粒度记录性断言；反向闸下限改现算）。 |
| `closure/tests/test_deadline_anchor_binding.py` | 适配二元组返回（新 `_bind` 辅助，顺带把「意外触发碰撞策略」变成当场炸而不是被压成"绑不上"）。 |
| `closure/tests/test_derivation.py` | `test_deadline_same_day_as_truthy` → `test_deadline_same_day_as_zero_day`。**原用例把病灶当规格写着**（喂布尔 True 断言 satisfied）。 |
| `worldgen/tests/test_registry.py` | 条数 11→19、槽名集合、`.to_person` 的 `unit`/锚名（**原断言锁的正是错的一方**）。 |

---

## 二、验收三层

### 层一（边界）：字节锚 **PASS**

基线池 = HEAD `8c386907` ＋ 动手前工作树 patch（独立 worktree 复原），候选池 = 现工作树。
两侧同为 `count=50 / seed=301 / ctcov=on / fragment_count_per_building=4`，落 scratchpad。

⚠️ **锚定批参数补全（official 审核 M4②，2026-08-05）**：原文只写了前三项，
**漏了 `fragment_count_per_building`** ⇒ 层一池按记录不可精确重放。
补的这个 4 有两条来源、互相对得上：①`generate_world_batch` 的默认值就是 4
（`generator.py:3298 / 3417`，经 `target_count` 传给片段模板选取）；
②由本表自己的读数反推——回填行数 400 ÷ 50 栋 ÷ 2 个白名单槽（`.to_ba` / `.to_person`，
均为碎片粒度 ⇒ 行数即碎片数）＝ **每栋 4 个碎片**。
📌 但**别把它当"两池已对齐"的证据**：official 线独立生成的复现池同样声明用 4，
却报「每栋 ~7 行 `.to_person`」——两池的碎片规模事实上不同，成因未查。
⇒ 结论只到「这一项必须随锚定批显式打印，不能靠默认值推」，
碎片规模是否真的一致**未证**（换池批重建层一池时须实取核对）。

```
[PASS] world.* 全部 14 个单元                     逐字节相同
[PASS] proj.projections / threshold_evaluations /
       basis_items / matched_families /
       coverage_control_metadata                  逐字节相同
[PASS] sidecar.records                            逐字节相同
[允许] world.meta（registry_bundle_hash /
       deterministic_key）                        —— 见下
[允许] sidecar.entries      12650 → 13050
[允许] proj.cohort_manifest 1 行

[deadline] 既有行 12650 行全部保留，非锚点列零变化
[deadline] 锚点回填行数 = 400（白名单槽 to_ba / to_person）
[deadline] 新增行数 = 400，落在 8 个新槽
[参考] J1 保留键率 1.0000；expected_verdict 翻判 0（0.0%）
[参考] threshold pass_bool 逐行差异 0/6707
```

⚠️ **`world.meta` 变是本步的正当结果、不是锚失败**：本步真的改了权威注册表（+8 条），
`registry_bundle_hash` / `deterministic_key` 随之换 ⇒ **本步是换池级改动**
（决议 §一.6 把批级数字验收挂换池批，正是这个理由）。
允许它变不等于放宽锚——本步真正的锚是 `world.*` 实体表与 `proj.*` 判定侧全部逐字节不变，
这三行都过了。qwen ① 决策准则（红＝与共享流耦合，立即停供给剥离另案）**未触发**。

### 层二（语义）：**PASS**，红先行已验

- **B1/B2 红先行**：新测试文件在落 R1 之前跑，`test_b1_same_day_zero_is_satisfied[0]` /
  `[0.0]` 与 `test_b1_same_day_nonzero_is_violated[1/1.0/2/3.0]` **实测全红**，
  落 R1 后转绿。整数 0 与浮点 0.0 同形。

  🔴 **口径订正（official 审核 M4①，2026-08-05）——先前这句把「红的原因」写成了另一件事**：
  原文写「0 判 violated、1 判 satisfied、2/3 落 open/missing_time_anchor ——语义反转逐条复现」。
  **那组读数不是在纯 HEAD 上取到的**，必须点名配置：

  | 配置 | 实测 | 说明 |
  |---|---|---|
  | **纯 HEAD**（`8c386907`，绑定通道还不存在） | **6 failed / 1 passed**，6 条**全部**失败在 `assert obl.closure_status == 'open' != 'closed'` | **全部堵在绑定失败**——用例喂的带锚事实在老通道下必 miss，**一条都没走到比较分支，语义反转一次都没被复现** |
  | **保留绑定通道、单独回滚 R1**（`same_day_as` 分支还原成 `_canon_truthy`） | **7 failed**，逐条复现「0 判 violated / 1 判 satisfied / 2·3 落 open」 | 上面原文描述的那组读数，出自**这个**配置 |

  ⇒ 红先行作为**门**是过的（纯 HEAD 确实红），但「红在哪一环」两个配置不同，不能混着写。
  📌 附带一条值得点名的形状：`test_b1_same_day_boolean_is_not_a_verdict` 在**纯 HEAD 上是绿的**
  （同样因为绑定先失败），只有在「保留通道、单回滚 R1」下才红
  ——典型的「**测试跑在缺陷不可能显现的输入上**」（记忆
  `feedback_test_on_input_where_the_bug_cannot_manifest`）。
- **B3**：provenance 通道优先于别名桥布尔闸（`repair.prescribed.completed` 用例）；
  0 条→诚实 miss；>1 条→`ambiguous_fact_binding`；绑定成功时锚点回写断言。
- **B4**：禁供名单为模块级常量；三锚都在锚点册里（防打错字）；世界侧零声明（双向断言：
  判定侧一条 ＋ 世界侧静态闸一条）；误供带锚事实也不出确定判定；**反向对照**——
  同形状事实换成非丙类锚点必须能判（防「provenance 通道整个不工作」廉价通过）。
- **B5**：`inspection.prescribed.completed` within-7-day 双向（5 天 satisfied / 30 天 violated）。
- **B6（形态 C 承重前置）**：逐（楼,锚）楼级行恰 1 行 —— **绿**，未触发回退形态 A。
  同批断言：零 `aggregation` 标记、`granularity=building`、无 `fragment_id`、
  `source_refs=[world_id]`、同栋确定性、异栋有差异、值落物理边界内。
  🔴 **射程已扩（official 审核 M1，本轮落）**：原 B6 只调楼级发射器 ⇒ 只覆盖
  `granularity=="building"` 的 8 条，而带锚条目实际 **10** 条
  ——碎片粒度的 `.to_ba` / `.to_person` **结构上不在射程内，而乙12 翻车的正是这 2 条**。
  现按粒度二分：楼级 8 条仍是硬断言；碎片级 2 条落**记录性断言**
  （行数 = 该栋碎片数、逐行同锚），并新增**发射覆盖闸**
  `test_b6_scope_covers_every_anchored_registry_entry`——每个带锚条目都必须真被某条
  发射路径产出。变异实测：把碎片路径的锚点回写摘掉，两条新断言当场红，
  旧的楼级硬断言仍绿（正好复现「M1 之前看不见」那个形状）。
- **C4 静态闸**：注册表全部 `rule_card_threshold.time_anchor_key` ∈ 锚点册 19 条；
  含反向闸与 `_and_mbi4` 孤儿名专项。
  🔴 反向闸下限由硬编码 `≥8` 改为**从注册表现算**（M1）：原来的 8 是**楼级**条目数，
  两条碎片级带锚槽整个消失也不会红；现在同时断言「全部带锚条目都住在
  `sidecar_measurement_registry` 里」（挡「有人把带锚条目加进别的表 ⇒ 无发射器覆盖」），
  加条目自动纳入射程，不会下次又盲。

### 层三（病原回归）：**PASS**

在冻结批 `wave1_closing_seed401_20260804`（30 栋，池 gen_seed_401，确定性地板档）上复算。

🔴 **supply 臂的口径（glm 审核 §九.1-1 要求写明，别再让读者自己猜）**：
**supply ＝ 本单代码 ＋ 冻结批事实包原样——不注入、不回填、不增新行**。
即两臂喂的是**同一份**冻结事实包，只有闭包代码不同。
这正是本层能证明「零扰动」的原因：该批 1,393 条事实**零条**带
`provenance.time_anchor_key` ⇒ 新开的 provenance 索引全空 ⇒ 级 0 通道惰性，
603 条 `kind=deadline` 全部堵在绑定之前，**R1 改的比较分支一次都到不了**。
⚠️ 别与 §三 位移量化混：**§三那一臂是注入的**（楼级 240 条 ＋ 回填 412 条），
口径完全相反，两处的 supply 不是同一个东西。

```
[层三①] kind=deadline 与冻结产物不一致的键 = 0
        （603 条逐条对状态码 + evidence_fact_ids）
[层三②] trigger 义务状态 base≠supply 的楼数 = 0 / 30
        （s2_1_3_r 回归：无任何数值进 procedure.* 布尔闸槽）
[层三③] 义务总数 base=272122  supply=272122  （0 增 0 减）
```

---

## 三、位移量化（**上界口径 / 占位分布，数字不得当结论引用**）

口径：冻结批 30 栋事实包上，注入**新发射器现算**的楼级行（240 条）＋
给既有 `.to_ba`/`.to_person` 行回填锚点（412 条），用新闭包代码复算。
开关态与批清单一致（`exclude_fallback_reasons_facts` / `authorized_scope_selection` /
`mask_lookup_targets` 全 True），applicability bundle = `gen_seed_401` digest 校验通过。

| 锚点 | 类 | 义务数 | base 脱离 | supply 脱离 | satisfied | violated | 落点原因码 |
|---|---|---|---|---|---|---|---|
| `appointment.ri.made` | 甲1 | 23 | 0 | 23 | 12 | 11 | — |
| `nomination.temporary_ri.made` | 甲2 | 2 | 0 | 2 | 2 | 0 | — |
| `nomination.temporary_ri.terminated` | 甲3 | 30 | 0 | 30 | 17 | 13 | — |
| `role.ri.terminated` | 甲4 | 30 | 0 | 30 | 19 | 11 | — |
| `repair.revision_need.exposed` | 甲7 | 14 | 0 | 14 | 0 | 14 | — |
| `repair.prescribed.completed` | 甲8 | 93 | 0 | 93 | 70 | 23 | — |
| `appointment.repair_supervising_ri.made` | 甲9 | 30 | 0 | 30 | 16 | 14 | — |
| `role.supervision_team.changed` | 甲10 | 30 | 0 | 30 | 17 | 13 | — |
| `repair.revision_proposal.submitted_to_ba` | 乙11 | 60 | 0 | 60 | 14 | 46 | — |
| **`repair.completion_report.submitted_to_ba`** | **乙12** | **60** | **0** | **60** | **0** | **0** | **`ambiguous_fact_binding`** |
| `inspection.prescribed.completed` | 挂起#5 | 12 | 0 | 0 | 0 | 0 | `missing_time_anchor` |
| `inspection.report.submitted_to_ba` | 挂起#6 | 60 | 0 | 0 | 0 | 0 | `missing_time_anchor` |
| `appointment.representative.supervision.made` | 丙13 | 30 | 0 | 0 | 0 | 0 | `missing_time_anchor` |
| `investigation.detailed.commencement` | 丙14 | 66 | 0 | 0 | 0 | 0 | `missing_time_anchor` |
| `repair.prescribed.started` | 丙15 | 63 | 0 | 0 | 0 | 0 | `missing_time_anchor` |
| **合计** | | **603** | **0** | **372** | **167** | **145** | |

- **甲类 8 锚 252 条全部脱离且全部确定判定** —— 与底稿预期上界 252 逐条吻合。
- **丙类 159 条恒等** —— 脱离 0、确定判定 0。
- **挂起的 #5(12)/#6(60) 恒等** —— 未供给，不受影响。
- **确定判定合计 312 条**（＝252 甲 ＋ 60 乙11）。372 − 312 = **60 条落 `ambiguous_fact_binding`**（乙12，见 §四）。

⚠️ **与工单的一处口径分工**：工单写「50 栋验证池 seed 301 跑位移量化」，实际把两个池
分给了各自有意义的那一层——**层一用 50 栋 seed301 配对池**（字节锚要的是「同一份世界、
两份代码」，配对池是唯一形状），**层三与位移量化用 30 栋冻结批 seed401**
（层三的判据是「既有 **603 条** `missing_time_anchor` 逐条不变」，那 603 条只存在于这批
冻结产物里；换一个池就没有可逐条对的基线，且须另建 applicability bundle）。
两个池的开关态与锚定批都随输出打印。

⚠️ **satisfied / violated 的拆分完全由占位均匀分布决定，没有任何依据**，
不得据此说「合规率如何」。可引的只有「有多少条脱离 unknown / 有多少条产出确定判定」
这两个**结构性**数字，且它们是上界（分布授权若允许事件缺席，实际会更低）。
⚠️ supply 臂是在**冻结批事实包上模拟**新世界侧输出，不是重生成的池
（重生成即换 `registry_bundle_hash` ⇒ 换池，批级数字按决议 §一.6 挂换池批）。

### 全量测试

`workflow_engine` ＋ `evo_agent_baseline` ＋ `agent_v1/tests` 一次跑齐：
**3,380 通过 / 1 skipped / 11 xfailed / 0 失败**（646 秒）。

改红后修好的三处旧断言（都是**原断言锁着错的一方**，不是本单改坏）：
- `test_derivation.py::test_deadline_same_day_as_truthy` —— 喂布尔 True 断言 satisfied，
  即把 R1 那个语义反转当规格写着；
- `test_registry.py::test_to_person_slot_has_cop_confirmed_threshold` —— 锁着
  `unit="calendar_day"` 与锚点册外的孤儿锚名；
- `test_registry.py::test_sidecar_measurement_registry_present` —— 条数与槽名集合（11→19）。

---

## 四、🔴 停下回报：乙12 的「回填」与「碰撞策略」在楼级作用域下结构冲突

**现象（实测，非推理）**：`repair.completion_report.submitted_to_ba` 的 **60/60** 条义务
落 `blocked / ambiguous_fact_binding`，一条确定判定都没有。

**机制**：
1. 决议 §一.3 定 #12 走 `.to_person` ＋锚点回填；`.to_person` 在注册表里**没有 `granularity` 键**
   ⇒ 默认 fragment 粒度 ⇒ 每栋楼 **7–8 行**（逐碎片各一行，值各不相同）。
2. 回填把**同一个锚点**写进这 7–8 行的 provenance。
3. #12 的 60 条义务**全部是楼级作用域**（实测：碎片作用域只有 #7 的 14 条与 #8 的 93 条），
   而楼级索引**看得见碎片行**（既有实据：`repair.prescribed.started` 的楼级义务今天就绑着
   一条碎片 sidecar 行）。
4. ⇒ 同（作用域,锚）候选 7–8 条 ⇒ 决议 §三.2 的碰撞策略触发 ⇒ 拒绝任取。

**这不是 bug，是两条决议条款相交出来的结果**：碰撞策略是对的（任取就是 2026-07-27
捏造 135 条期限判定的病灶形状），回填也是决议明写的。缺的是「回填的槽必须是楼级粒度」
这一条前提——决议做出 #12 决定时没有核 `.to_person` 的粒度。

**没有本单内的合法解**：
- 把 `.to_person` 改成 `granularity: "building"` ⇒ 改动既有槽的采样行数与取值
  ⇒ 层一字节锚必红（那是"既有单元变了"，不是"新槽纯追加"）；
- 改 validator 的作用域过滤放行 ⇒ 决议 §二已裁定为「有意设计边界，动它是新语义」（形态 B 被否）；
- 给 #12 另起一个楼级新槽（像 #11 那样）⇒ 与决议 §一.3 明写的「走 `.to_person`」相悖，
  且会出现两个槽承载同一个真实量，还要另走分布授权。

**⇒ 交决策门二选一**（两条都超出本单授权，未自决）：
| 候选 | 代价 |
|---|---|
| 甲：`.to_person` 改楼级粒度 | 换池级世界侧改动（既有槽的行数与取值都变），须重立池锚；语义上更正确（「同日送交」本就是楼级一次性行政事件，逐碎片抽是 spec §3.1 点名的建模走样） |
| 乙：给 #12 新起楼级槽 | 纯追加、层一保持绿；但两个槽承载同一真实量，`.to_person` 沦为死数据，且须补分布授权 |

**在裁定之前，现状是安全的**：60 条落 `ambiguous_fact_binding`（消费者文案「候选事实不唯一，
系统拒绝任取其一下结论」），**没有一条被捏造成确定判定**。相比 base 的
`missing_time_anchor`，这个码其实更准确——锚点确实供上了，不准的是粒度。

🔴 **但「现状安全」只对「不注入」这个口径成立（glm 审核 §九.1-2）**：
今天之所以什么都没坏，是因为冻结批**没有注入**任何带锚事实（见 §二/层三的 supply 口径），
那 60 条 `blocked` 只存在于 §三的**模拟注入**臂里。
**换池批必然注入**——世界侧注册表已经带着这 8 条新条目 ＋ 两条回填，重生成即产出带锚行
⇒ 60 条真的落 `blocked` ⇒ 按判定权红线 `allow_stop = open==0 且 blocked==0 且 schema/forbidden 过`，
**`allow_stop` 直接为 `False`**。
⇒ **换池批之前必须先裁甲/乙**（official 线已表态「甲，挪到换池批与分布授权一并落」，
见 `审核结果_official_期限锚实施_20260805.md` §四）。把「现状安全」读成「可以就这样开换池批」是错的。

🔴 **过渡期的硬前置：M1 必须先补**（official 审核 §四 ③ 明列）。
乙12 是靠位移量化**偶然撞出来的**，不是靠 B6 那道闸——B6 当时结构上看不见碎片粒度的两条带锚槽。
若不把射程扩到全部带锚条目，同一形状会在换池批里静默复发，
而那时它已经混在一堆真数字里，比现在难查一个量级。
（M1 已于本轮落地：`worldgen/tests/test_deadline_anchor_emission.py`
新增 `test_b6_scope_covers_every_anchored_registry_entry` 发射覆盖闸
＋ `test_b6_fragment_granularity_anchored_slots_are_multi_row` 记录性断言，
反向闸下限改为从注册表现算。）

---

## 五、与决议/工单的形状差异（三处，均已按「以决议为准」处理并在此注明）

1. **新增注册表条目是 8 条，不是「甲 8 ＋ #11 第 9 条」的 9 条**。
   底稿写的「甲类 8 个」把 #6 算在内，而决议 §一.5 把 #5/#6 一并挂起；
   #8 又复用既有 `.to_ba` ⇒ 甲类真正需要新槽的是 7 条，加 #11 共 **8 条**。
   决议的实质（哪些锚该供）没变，只是计数标签对不上。
   ⚠️ **两处口径已统一（official 审核 M4③，本轮落）**：先前只改了本节，
   **权威数据文件里那句话没跟着改**——`registry.py` 乙 #11 条目的注释仍写着
   「故本条是甲类八锚之外新增的第 9 条」，两处并存会再骗一次人。
   现已把该注释改成 8 条口径（本条是 8 条里的第 8 条），并写明「甲 8」这个基数
   本身就不成立（把挂起的 #6 算进了甲类、#8 复用既有槽不需新条目）。
2. **「>1 条→报错」落成 `blocked / ambiguous_fact_binding`，不是抛异常**。
   抛异常会让 30 栋批当场崩，与「诚实产出 unknown」的整体设计相悖；
   `ambiguous_fact_binding` 是**既有**原因码，四处消费者文案（`report_writer` /
   `report_contract_v4` / `unknown_attribution` / `identity_v2`）逐字适配，零新码。
3. **丙类禁供闸只堵新开的 provenance 通道，不堵既有 slot/measure 老通道**。
   堵老通道会改动现存 159 条丙类义务的 `evidence_fact_ids` 与 notes
   （#15 今天经别名桥绑着一条布尔门状态行），与层三「零扰动」判据直接冲突。
   世界侧零声明由两条独立断言钉死，禁供的实际效力不受影响。

---

## 六、尾巴（明确列出）

1. **批级数字验收挂换池批**（决议 §一.6）。本单只验机制，未跑换池批、未跑大模型批。
2. **分布授权待决策门**：`分布授权草案_期限锚_20260805.md`（8 槽当前全是占位均匀分布，
   `distribution_source = PLACEHOLDER_pending_distribution_authorization_20260805`）。
   草案里另有三个待裁问题，其中 Q3 是**乙11 一条量承载两个收件人**
   （§2.1.3(p) 業主代表 ／ (q) 註冊承建商）——两支永远同判，真实偏差建不出来。
3. **乙12 二选一待裁**（§四）。
4. **归因码拆分挂归因批**：`missing_time_anchor` 一码压两因。本单实测出新的交互效应——
   供给后该码从 603 降到 231（＝丙 159 ＋ 挂起 72），拆码的分母变了。
5. **台账四条新债本单未修**，形状复核结果：①`offset_unit` 不读 —— 复核属实，本单未碰；
   ②`registry.py` `.to_ba` 条目 `measure_key` 指 `duration.submission.deadline` —— 复核属实，
   **本单未改**（它不在期限绑定路径上，改它属 threshold 消费路径的事）；
   ③`rule_slice.time_anchors` 只带 6/19 —— 未复核（本单绑定不读它）；
   ④拆码 —— 见上条。
6. **未做**：没有连 Neo4j、没有改法规卡包任何文件（指纹链未触）、没有动 `analyze`/eval 侧、
   没有动 `_canon_truthy` 本体、没有动 validator 作用域过滤、没有修 R2（`before` 比较符方向）。
   R2 在本批实测**未被踩到**（丙类三锚全部落 `missing_time_anchor`，`before` 分支无输入）。
7. **验证产物全部在 scratchpad**，随时可删：`pool_dl_baseline` / `pool_dl_candidate` /
   `deadline_baseline_tree`（git worktree，已清理）/ `deadline_displacement.py` /
   `deadline_displacement_result.json`。
