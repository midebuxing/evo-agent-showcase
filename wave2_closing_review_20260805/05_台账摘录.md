# 台账摘录：`团队文档/我的笔记/技术与研究债.md` 的 2026-08-05 当日新增节（原样摘出）

- **来源**：本仓库主台账 `技术与研究债.md`，第 9586 行至文件末（第 10076 行）。
- **摘录口径**：**2026-08-05 当日全部新增节**，逐字原样，未删未改。
  委托方指定的起点「1b 三线审核关案」实际位于本摘录的第 8 节之后（原文件第 9779 行）——它**不是**当日第一节，其前还有 rng 隔离 1a 的开工门/翻案/实施/审核共 8 节，同属 2026-08-05，一并摘出。
- **注意**：台账是**内部工作台账**，写法为「边做边写」，含大量作废/推翻的沿革记录（保留是有意的防错设计）。
  凡带删除线、「已作废」「别再引」字样的数字，一律不得当作现状引用。

---
### 波次二开工门三线收敛（2026-08-05 凌晨，rng 隔离案设计定案）

**Q1**：(d) 全票成立、**必须最先落**；稳定键＝`(阶段名, world_id, fragment_id)` 字符串
种子（⛔ 裸 fragment_id 跨池重名／fragment_index／hash()——PYTHONHASHSEED 重放不稳）；
**改动面三通道分治**（官方线钉出）：①模板选择全表 shuffle（`_select_fragment_templates:
662/679`，长度变→既有片段选型变）②四后置阶段＋**sidecar**（qwen+官方同点名 sidecar
批级跨栋流）③🔴 `fsp_estimate=f(max over 全楼 conditions)`（generator.py:2851-2852）
——**确定性非追加、rng 治不了、对 expected_verdict 隐身**（实测甲-a：80/200 保留片段
fsp 值变、87/200 至少一 flag 翻、8 条直入规则卡槽）。
**Q2**：(i) 追认甲-b 在池现状全票；「门过」表述改记「force 越门」；甲-b 收益当前
不可引（自身即污染源）。**Q3**：配对判据升级＝保留键 100%＋**量测值逐字节**（比判定
翻转灵敏 3.5×）＋派生 flag 逐键＋sidecar 逐值/跨栋断面＋列表长度单测；SE 上界不入。
**Q4**：换池清单修正＝步 1 拆 **1a**（shuffle 稳定化＋四阶段与 sidecar rng 隔离）／
**1b**（fsp 取 max 解耦——世界语义改动，单独裁定后实施）；冻结全池 0.2412 与队列
0.1893 双数；期限锚与 sidecar join **分开**（join=首次接入整层 threshold，先出只读
离线预估）；补回改卡 C 档。
### 🔴 第三波翻案：sidecar 是判定级正主（2026-08-05 凌晨，核清_rng隔离1a）

实测（12 栋 HEAD 重现：世界 8 表与生产池逐字节同、expected_verdict 翻 24/79，差异
槽恰 9 个 sidecar 槽）：`sidecar_join_status` 恒 unavailable **不蕴含**不进判定——
sidecar 数值经 `regulation_projection_executor.py:1462-1477` 与世界量测**同路**进
threshold（占该批 threshold 行 66.3%）。⇒ ①「S2.5 上界零」不成立（三线同错——
都信了探查的 join 推断，教训：**join 状态是一条路的状态，不是槽的全部去向**）；
②「期限锚只加锚零影响」不成立（`sidecar_rng` 种子含 `registry_bundle_hash`，
**加任何条目即整批 sidecar 重掷**）——期限锚供给升级为高扰动件；③1a 不倒：方向/
键/顺序照旧。**实施拆两批**：1a-i（四阶段+sidecar rng，**有字节锚**——世界生成与
批规模无关，12 栋 0.9 秒即复现池前缀）／1a-ii（shuffle 稳定化，结构上无字节锚，
零消耗单测+属性测+12 栋配对）——**分落分验，合并抹掉唯一字节锚**。
改动点清单：通道① 1 处（:659/662/679）；通道② 10 表达式/5 改动位＋sidecar 4 处
（:462 轴积**须加 combo 维**，否则 #29 改轴全移位）。待裁三小件：rng 形参删留/
12 栋选栋约束改法（COASTAL 全池仅 1 栋）/i-ii 分落追认。
### rng 隔离案完整门闭合＋实施卡定稿（2026-08-05 凌晨，三线差量全收敛）

官方线独立复现更强：24/24 翻判归 sidecar 阈值行、183/183 pass_bool 差全在 sidecar
槽、**任何注册表字段级变动**（含与 sidecar 无关表）都换 `sidecar_rng` 种子、
HEAD 50 栋 **2.3 秒**逐字节复现生产池全部 12 表。五问三线收敛＋官方升级四条：
**实施序（定稿）**＝**1a-0 解绑**（sidecar_rng 种子从 registry_bundle_hash 改稳定键
——不解绑则 30.4% 背景翻判淹没后续一切改动的验收；包哈希仍锚池身份不锚流）→
**1a-i** 四阶段子 rng（独立提交，持 sidecar 侧全池字节锚）→ **1a-i′** sidecar
槽级+combo 维子 rng（独立提交，持四阶段侧字节锚；`:462` 轴积加 combo 维=
`"|".join(sorted(k=v))`）→ **1a-ii** shuffle 稳定化（无字节锚：零消耗单测+属性测
+配对）→ **1b**=#31 fsp（藏匿轴不降级）→ 期限锚等注册表改动（解绑后按 glm 语义
边界：只加 time_anchor_key 不加 conditional_inputs=纯追加）。
验证＝**count=50 全池字节配对**（allowed-change 集互斥、成本 2.3 秒）；rng 形参删、
T1/T2 断言改输出层不变式（含通道①同治）；选栋约束只留「加片段栋非末位」。
风险双轴：淹没轴（sidecar↔deterministic_key 耦合，1a-0 治）＞藏匿轴（fsp，1b 治）。
### rng 隔离 1a 全序落码完毕（2026-08-05，七道字节锚全过）

四步照实施卡逐步落、逐步验，**每步各自持一个字节锚**（新脚本
`agent_v1/scripts/verify_rng_isolation_pairing.py`，50 栋全池 23 个比较单元逐字节配对，
按口径登记表 §一 把**开关态/判据/锚定批**三项打印进输出；生成一个池 1.2-1.5 秒）。
新建 `worldgen/rng_domains.py`：**9 条**域串登记表 ＋ 唯一构造入口
（`sub_rng` 字符串种子、`stable_sort_key` SHA-256），**导入即自检域串唯一性、拒非 str 键段**
——「两个消费点共用域串 ⇒ 退化成一条流」这个最阴的失败形态被结构化排除。

**七道验证全 PASS**：identity（生产池 vs HEAD 重跑）23/23 ／ 1a-0 16/16 ／
registry-mutation 21/21 ／ 1a-i **12/12** ／ 1a-i′ **16/16** ／ 1a-ii 4/4 ／ 自检两跑 23/23。
`deterministic_key` 全程 `923f9e48…afffd` 逐字节不变 ——**只解流不解锚**兑现。

**1a-0 的效应单独量到了**（这是分步落的全部意义）：拿一个对世界生成**完全惰性**的
注册表变异（`RegistryBundle.version` 加后缀，generator/sidecar 都不读它）做对照——
HEAD 上它改 `sidecar_entries` **8,119/19,090**、翻 `expected_verdict` **113/340 ＝ 33.2%**、
`pass_bool` 差 763/11,318；解绑后**三项全 0**。⇒ 台账「任何注册表字段级变动都重掷整批
sidecar」独立复现（官方线 12 栋报 30.4%，全池口径更高）并**已关闭**。波次二后续
#23 词表／#29 BA→BD／改卡 C 档／期限锚供给不再自带 33% 量级的背景翻判。

**两处对核清的订正（都会让「照核清改」出错）**：
① **S4 主 rng 消费是 7 处不是 2 处** —— 核清 §1.1 只列 `:2431/:2441`，实读还有
`_compute_inspected_area_ratio_per_fragment` / `_sample_rebar_type` / `_sample_rebar_location` /
`_sample_corrosion_loss_type` / `_compute_rebar_section_loss_ratio_per_class` 五处（chain derive
与钢筋锈蚀分支）。**照核清只改 2 处的话隔离是假的、而字节锚照样会绿**——因为主 rng
在四阶段之后再无消费者，位移体现不出来。
② **`buildings` 表不是整表不变** —— 核清 §4.2 说它在模板选择之前生成故 1a-ii 下逐字节相同；
只对 16 列成立，第 17 列 `derived_outcomes_json` 是 `populate_derived_flags` 事后回写的，
1a-ii 下差 24/50 栋。验证器已把它拆成 `.core` / `.derived_outcomes` 两个单元
（不拆会把一条正当的 allowed-change 误报成锚失败）。

**1a-i′ 顺带把 1a-0 结构化**：四点全槽级化后批级 sidecar 流零消费，`rng` 形参从 sidecar
五个函数与 `validation.py` 一并删除（连带消掉 `rng=None → random.Random()` 这个
「仅供测试便利」的非确定性缺省）。两者治的不是同一半：1a-0 治「改注册表重掷」，
**治不了**「第 i 栋片段数一变、第 i+1..n 栋全部移位」；1a-i′ 两者都治。

**一条查实但未发作的潜伏耦合（官方线尾巴 1，关闭到「查实」）**：四阶段 ⊥ sidecar
**不是正交**，通道存在且只有一跳 —— `measurements` → `populate_derived_flags` 回写 RAS →
sidecar 读 `repair.repair_required` 造 `H.repair_need` → 三个 sidecar bool 槽的
`conditional_formula` 消费它（`procedure.supervision_representative.planned` /
`procedure.repair.prescribed.started` / `artifact.proposal.repair`）。本次没发作的原因查实：
1a-i 只动了 RAS 四个回写字段里的 **`verification_failed`（30/340）**，而 sidecar 从 RAS
只读 `repair_required`。⇒ **字节锚为真 ≠ 两侧正交**；将来任何让 `repair_required` 翻的改动
（1b 的 fsp 直接动 `assessment_flags`）都可能让这条锚红，**红了是通道发作、不是锚坏了**。

**残留（不在 1a 范围）**：`coverage_id = f"CVR-{片段后缀}-{fragment_index:02d}"` 含列表位置
⇒ 抽样**值**已隔离、**id 层未隔离**。真实生成路径不发作（ctcov 只在尾部追加），
但「前插一个片段」时 id 会移位。改 id 构造会动既有池主键，属换锚级改动。

**测试**：新增 `worldgen/tests/test_rng_isolation.py` 15 条（T1 结构保证 / T2 四阶段前插不变式 /
T3 模板插入稳定性属性测试 / T4 防坍缩 / T5 `hash()` 跨进程不稳实证 / T6 sidecar 加槽与加轴组合纯追加）。
**全部做过坍缩变异对照**（把子 rng 退化成「按 world 共用一条推进流」）→ T2 四条 ＋ T6 三条**全红**，
非空护栏。改 11 处测试直调点；4 处「不会红但变空护栏」的旧判据同步重写
（`test_worldgen_component_type_coverage` 两条 rng 状态断言 → 输出层不变式；
`test_precondition_supplement_slots` / `test_reporting_axes_slots` 的 `seed` 参数化 → 片段数参数化，
并把两个模块 docstring 里「sampling_order 排最后 ＝ 缺省等价机器判据」**降级为遗留纪律**
——它现在只管上游可见性，不再管随机流）。
全量 pytest 零红：`src/workflow_engine`＋`agent_v1/tests` **1,454 通过 / 2 xfailed**、`src/evo_agent_baseline` **1,858 通过 / 1 skipped / 9 xfailed**（后者与本改动**零 import 耦合**——分层单向红线逐行核过，该包里 10 处 `workflow_engine` 全是「禁 import」自述注释、真实 import 0 条；跑它只为排除意外）。
详 `实施记录_rng隔离1a_20260805.md`。
### 1a 全序落地（2026-08-05 凌晨，四步各自独立字节锚验证）

1a-0 解绑（惰性字段对照：HEAD 改 RegistryBundle.version ⇒ sidecar 8,119/19,090 变、
判定翻约三成——⚠️ 具体百分比随变异后缀漂 27.6-33.2%（官方线审核 F3 实测），
**只可引定性「解绑后归零」不可引具体比值**）→1a-i 四阶段子 rng（**实施自纠核清：S4 主 rng 消费
7 处非 2 处**——漏 5 处则假隔离且字节锚照样绿；sidecar 19,090 行锚 12/12）→1a-i′
sidecar 槽级+combo 维（批级流退役；四阶段侧锚 16/16）→1a-ii shuffle 稳定键排序
（主 rng 消费归零；无字节锚靠 T1 结构+T3 属性测+配对）。50 栋全池配对脚本
`verify_rng_isolation_pairing.py` 七道全 PASS（23 比较单元、三项打印齐——口径通则
首个新脚本样板）；deterministic_key 逐字节不变。全量 1,454+1,858 零红；15 条性质
测试坍缩变异全验。**尾巴三条已记**：coverage_id 含 fragment_index（id 层未隔离，
换锚级）；四阶段↔sidecar 潜伏耦合（1b 动 assessment_flags 时那道锚可能红——
**红了是通道发作不是锚坏**，预先记死）；coverage_control_metadata 未变根因未追。
下一步：三线审核 1a → #31 fsp 裁定 → 期限锚（纯追加形）→ 换池捆绑件。

### 1a 三线审核关案（2026-08-05 凌晨）

glm 准予提交／official 通过（9 发现、2 须修均测试护栏空洞）／qwen 通过（七道门
亲自重放数字逐项吻合）。三修已落：T4 补 sidecar 键完整性测试（漏最后一维时原
15 条全绿的洞——combo 维/slot_id 维首抽互异断言）；T6 numeric 对照改**前插**
（追加对照在坍缩变异下绿——顺序敏感坏实现对尾部加免疫）；台账 33.2% 措辞降定性。
rng 隔离测试 16 条全绿。**#22 关案**；官方线交叉锚（实施终池与其亲手跑池 23/23
逐字节同）作为第八道独立锚存证。
### 1b fsp 门三线全票闭合（2026-08-05 凌晨）

**(b) 逐片段＝恢复 spec 被违反的既有接线**（三线独立同框：守则 §4.3.3/§4.3.4 把 FSP
定义在結構構件、spec 08 明写输入=片段级 `ratio.fsp.structural_performance` 且该值
已算好落盘；全楼 max=实现偏离，三池实测**只多报不漏报** 29.7-35%——给结构达标构件
签发「緊急補救+立即上報」；甲-a 片段级 fsp 变 0/192 ⇒ (b) 顺带杀非追加通道）。
**Q2 订正**：派生 flag 进不了 expected_verdict（`_evaluate_world_predicate:917-928`
对其恒 True）⇒ 1b=**指标换锚非真值换锚**，与换池捆绑无损；旧批召回/精确率不可跨批比
须记。**Q3 验收三层**：边界层（allowed-change 扩 fsp 派生族+显式移锚集）＋语义层
（①有量测片段 estimate 逐值等量测 ②只许 True→False 单向翻转 ③无量测落
not_applicable+no_assessment——该分支三池 0/862 是死分支须激活）＋病原回归层
（加片段零扰动）。**同批必改**：`risk.building_safety.emergency` 同吃 fsp（:2727）。
**1b 工单前置**：其余 13 个 derived flag 逐行清点跨片段聚合（glm：「只有 fsp 一处」
是推断非实测）。**另立条目**：spec 08 §5 声称 projection executor 读派生旗标、
实测 W2 零消费（spec-code 缺口，qwen 发现）。
### 1b fsp 接线恢复落码完毕（2026-08-05，三层验收全过）

**前置清点已从推断变实测**（glm 那条未验项关闭）：AST 扫 `_compute_derived_flags_for_condition`
函数体全部标识符——10 个形参**只有 7 个被读到**，`building`/`fragment`/`mechanism`
**一次都没读**；其余六个对象全走 `_build_per_fragment_state_lookup` 按 `fragment_id` 取、
`measurements` 按 `target_ref` 收。⇒ **除 fsp 外没有第二处跨片段/楼级聚合输入**。
⚠️ row 4/5 不是第二个源、是 row 1 的下游（row 1 吃 fsp），改一处三行同时归位；
楼龄是楼级属性但**不是跨片段聚合**（加片段不改楼龄，不制造非追加性）。

**接线**：`generator.py` 删掉楼级三行（原文留注释备查），改查本片段
`ratio.fsp.structural_performance`（`_lookup_fragment_fsp_measurement`，:2671）；
`index.fsp.estimate` 写该片段量测值、**无量测时不写该键**（Dict[str,float] 写不下
not_applicable，写默认值＝在数值通道里重犯"把没评估过伪装成评估过"）；
row 1 `risk.building_safety.emergency` 同批改；row 8 的 `not_applicable`+`no_assessment`
**死分支首次激活**（1b 前三池 0/862）。`_compute_max_condition_severity` docstring 补作用域
（同一函数被两处按两种作用域调用而多年未被发现的入口）。

**位移量化（50 栋验证池，seed301／ctcov on／内存不落盘）**：flag 翻转 **124/347**
＝ True→False **119** ＋ bool→NA **5** ＋ **反向 0**；涉及片段 **124**、涉及楼 **42/50**；
**`risk.building_safety.emergency` 分支翻 0**（本池无 fsp<0.75 片段——**池相关不是结构性质**，
换含该类片段的池会翻并顺 row4/5→RAS 回写→sidecar 传导）。三个百分比随
`_FSP_FLOOR_PROXY=0.95`（DEBT-021 待校准）移动，**但单向性与阈值无关**（恒等式性质）。

**验收三层全过**：①边界层——配对脚本加 `--step 1b`，fsp 派生族三单元从零变动集**显式移入**
「按语义核」集（集合＋注释同改）；**20 个须相同单元逐字节一致**，含 `sidecar.entries` 绿
（1a 潜伏耦合**未发作**，机制：row 1 翻 0 ⇒ `repair_required` 不动 ⇒ 通道没通电）与
**全部 proj.\* 绿**（`expected_verdict` 翻 0/347、`pass_bool` 差 0/11,646）；唯一变动单元
`condition.derived_outcomes` **297/347**（347−297=50 恰好一栋一条：持全楼 max 的那个片段
两值相等）。②语义层三硬断言＋③病原回归层（加片段零扰动，**前插**非追加）落
`worldgen/tests/test_fsp_fragment_scope.py` 31 条；**变异对照四层全红**（楼级 max 复辟：
索引不符 44 条／5 个 seed 全无正向翻转／5 条不落 NA／5 个 seed 既有片段被扰动）＝非空护栏。

🔴 **两句必须随行**：①**1b 是「指标换锚」不是「真值换锚」**——派生 flag 结构上进不了
`expected_verdict`（`_evaluate_world_predicate:917-928` 对 non_null/value_equals/value_in
一律 return True），本次配对的 proj 侧逐字节全绿已把这句**从推断变成实测门**；
别把系统侧位移错算到真值头上。②**旧批的召回/精确率一律不可跨批比**——进 agent 的事实面
已变（§4.3.4 两张卡的触发面直接受 124 条 flag 翻转影响），跨批引数字必须标不可比。

**实施中被测试逼出来的一件事**：`index.fsp.estimate` 改成有条件写之后，在已填过
`derived_outcomes` 的世界上**重跑**会留下陈值（其余字段走 `.update()` 键恒被盖，只有它有条件写），
已加显式 `pop`；生产主链只调一次故零行为差异，守的是重跑语义。

**测试**：`src/workflow_engine`＋`agent_v1/tests` **1,486 通过 / 2 xfailed**（1a 收官 1,454，+31 新增，
另改 1 条按量测在否分账）；`src/evo_agent_baseline` **1,858 通过 / 1 skipped / 9 xfailed**。
中途 `test_slot_alias_normalization_scan` 报新函数裸比 `.slot_id`，按护栏本意补白名单甲
（两侧同为世界侧量测槽名），**没用 `in` 之类写法绕开扫描器**。
**尾巴**：下游判定位移未跑批（换池捆绑件）；~~含 fsp<0.75 片段的池上传导链形态未验~~
（**已被审核门 M1 订正，该场景不存在**，见下节）；`_FSP_FLOOR_PROXY` 仍无 spec 锚定。
详 `实施记录_1b_fsp_20260805.md`。

### 1b 三线审核关案（2026-08-05）——通过；M1/M2 已落；死析取项立新债

**三线判决**：glm **过**（零必须修；亲验三形参零读、pop 生效、白名单甲正当）／qwen **过**
（零必须修；**全部数字亲跑复现**——自反演备份 patch 重建 1b 前基线、自生成两池跑
`--step 1b` 配对 20 单元全绿、变异四层全红、位移 124/347＝119+5+0 逐项吻合）／官方
**有条件通过**（代码本体七点全过——347/347 独立重算、自建变异 26 红；**唯一不过的是
第 6 点归因**，即 M1/M2）。⇒ **代码零改动通过，两条文档级必须修已落**：

- **M1**（已修）：实施记录三处「emergency 翻 0 是池相关、换池会翻」**是反的**——
  fsp_true = clip(1.20−0.30·max_sev−0.10·age_norm, 0, 2)，`max_severity`/`age_norm`
  各截断 [0,1]（`generator_sampling.py:112-119`／`generator_base.py:373-375`）⇒
  **值域下界 0.80 > 阈值 0.75，换任何池都不会翻**；row 4/5 里唯一吃 fsp 的析取项就是
  row 1（`generator.py:2787/:2799` 我亲核）⇒ row 1→4/5→RAS→sidecar 整条传导链对 1b
  改动面**结构不可达**，两个"允许但未变"单元实为结构不可变。
- **M2**（已修）：同一错误预期已随单落进 `verify_rng_isolation_pairing.py` 注释
  （「红了是通道发作不是锚坏」）——对 1b 是反的（通道不可能发作，红了就是锚坏），
  留着会让下一个人去找一个不存在的池。注释已整块改写。
- **三线矛盾的处置（「视角不是裁决」实例）**：qwen 也测得值域 0.8308–1.1084、低于
  0.75 者 0，却把归因标「池相关」——括号里自己写的「0.75 低于值域下界、新旧口径都
  不可能触发」恰恰支持结构论；glm 收下实施记录原话没算值域。**按论证质量取官方线**，
  两处关键行号（公式截断、row 4/5 析取）我逐一亲核后才落修。
- **🔴 新债（官方线发现，1b 之前就有）**：row 1 `risk.building_safety.emergency` 的
  `fsp < 0.75` 析取项**自上线即死代码**——spec 06 §10 公式值域 [0.80, 2] 与 spec 06 §11
  row 1 阈值 0.75 **内部不自洽**（阈值设在值域之外）。修向未定（提阈值 vs 改公式下界 vs
  删析取项），属 spec 修订案，与 DEBT-021（`_FSP_FLOOR_PROXY` 无锚）同族同批议。
- **qwen 存疑（不阻塞，记档）**：row 1 无量测时 `fsp = 1.20`（按完好取值）**无 spec 明文**
  ——§10 只给公式常数项，未规定无量测用法；方向与「不臆造紧急」一致，
  但若 W2/闭包将来纳入 row 1 消费须先补一次裁定。
- **建议队列（不阻塞）**：变异对照四层固化为可选 CI 测试（glm）；白名单甲新条目补
  失效警示句（qwen）；实施记录补 1.20 与 spec 锚定关系出处（qwen）。

**#31 至此关案**（实施＋三线审核＋必须修落地）。生效随换池。

### 期限锚案决策门关案（2026-08-05）——三线全票＋E1 实验＋形态 C 二轮复核，实施卡定稿

**材料五份**：底稿＋三线商议（官方/glm/qwen 六决策点**全票同意方向**）＋E1 最小实验
（全批 30 栋实测）＋形态 C 二轮定向复核（官方/qwen 两线全票不违背）。
决议全文 `决议_期限锚_20260805.md`（含实施卡骨架§四、底稿订正清单§五）；此处只记要点：

- **路线定案**：R1（`same_day_as`→`==0`）与丙类禁供钉死为独立安全前置先落；R5 锚名对齐
  ＋静态闸；绑定通道（**provenance 优先**＋碰撞策略同（楼,锚）唯一/0→miss/>1→报错禁
  `facts[0]`）与世界侧接线（注册表新条目＋#8/#12 回填例外＋sidecar 数值路径取锚点）同批；
  发射＝**形态 C：不打 `aggregation` 标记的楼级原生事实**（spec §3.1 既定形状，
  打标记才是错用；承重前置＝逐（楼,锚）唯一断言 B6，红则回退形态 A）。
- **E1 实验三修正**（`实验_期限锚E1最小实验_20260805.md`）：#8 的 93 条是**绑错**（别名桥
  →布尔闸、"observed True unusable"）不是绑不上 ⇒ provenance 必须**优先**于槽/量表通道，
  否则 #8+#15 共 156 条（25.9%）任何纯供给动作救不回；绝不往 `procedure.*` 布尔闸槽塞
  数值（`s2_1_3_r` 触发器塌陷实测，净恶化）；「脱离 missing_time_anchor ≠ 产出确定判定」
  （`offset_value=None` 者落 schema_contract_violation）。
- **量与口径**：可供＝甲 8 锚（#5/#6 挂起）＋乙 2 锚；丙 3 锚 159 条不供（#13 措辞＝
  「本单禁供＋结构化钉死＋供路待提前量语义裁定」，#14/#15 数值供给意义上永久）；
  #11 世界侧无承载槽须新增注册表第 9 条＋分布授权、卡侧无 regime（`==0` 来自 relation
  结构常量、provenance 注明）；**弃用槽 `duration.delivery.deadline` 的 88.8% 一律不再引**，
  真实承载 `.to_person` 0 值占 52%（107/206）；批级预期数字一律**上界口径**（≤），
  分布授权定稿后回填。
- **台账新立四债（本单不修）**：①`evaluate_deadline` 不读 `offset_unit`——期限路径零单位
  比较，月单位 deadline 供数即静默错判（#6 挂起的加重理由）；②`registry.py:2889` `to_ba`
  条目 `measure_key` 指 `duration.submission.deadline` 疑登记残错（threshold 消费路径启用
  会踩）；③`rule_slice.time_anchors` 只带 6/19 锚、从 threshold_regimes 投影不从 deadlines
  投影——「登记了没接线」第四例；④`missing_time_anchor` 一码压两因（447/156，供给前
  口径）拆码挂归因批（交互效应：供后 B 因 156→63）。
- **🔴 主动否掉的假路（防复发，官方 E10）**：「改读本卡 `threshold_regimes.measure_key`
  查 `measure_index`」零世界侧改动、今天就覆盖 10/25，**但** sidecar 行 `measure_key`＝
  `slot_id` ⇒ `[0]` 任取 ⇒ 2026-07-27 捏造病灶复发——不许走。
- **「查不到≈查错」新例**：底稿引乙类分布用了弃用槽；qwen 亲验才发现真实承载是
  `.to_person`。引用任何槽分布前先核 `deprecated_at`。

**#32 决策门至此关案，转实施**（工单 `实施工单_期限锚_20260805.md`）。

### 期限锚案实施落地（2026-08-05）——三层验收全过；一处结构冲突交决策门

实施记录全文 `实施记录_期限锚_20260805.md`（含改动清单、三层验收输出、位移量化表）。
台账只记要点与**推翻/更正**：

- **三层验收全过**。层一（字节锚，50 栋 seed301 配对）：`world.*` 14 单元与 `proj.*`
  判定侧全部逐字节相同，`expected_verdict` 翻判 0、`threshold pass_bool` 差异 0/6707；
  既有 12,650 行 sidecar **非锚点列零变化**，新增 400 行落在 8 个新槽、回填 400 行落在
  白名单两槽。层三（冻结批 30 栋复算）：kind=deadline 与冻结产物**不一致 0 键**、
  trigger 状态 0/30 栋有差、义务总数 272,122 → 272,122（0 增 0 减）。
- **位移量化（上界口径，占位分布，不得当结论引）**：603 条期限义务，供给后脱离
  `missing_time_anchor` **372 条**，其中**确定判定 312 条**（甲 8 锚 252 全中，与底稿预期
  上界逐条吻合；乙11 60 条）。**丙类 159 条恒等**（脱离 0、确定判定 0），挂起的
  #5(12)/#6(60) 恒等。⚠️ satisfied/violated 的拆分**完全由占位均匀分布决定**，
  可引的只有「脱离条数 / 确定判定条数」两个结构性数字。
- **🔴 新发现（交决策门）：乙12 的「回填」与「碰撞策略」在楼级作用域下结构冲突**——
  `repair.completion_report.submitted_to_ba` 60/60 条落 `blocked/ambiguous_fact_binding`，
  确定判定 0。机制：`.to_person` 注册表里**没有 `granularity` 键** ⇒ 默认碎片粒度 ⇒
  每栋 7–8 行；回填把同一锚点写进这 7–8 行；而 #12 的 60 条义务**全是楼级作用域**、
  楼级索引看得见碎片行 ⇒ 候选 7–8 条 ⇒ 碰撞策略拒绝任取。
  **两条决议条款都没错，缺的是「回填的槽必须是楼级粒度」这条前提**（决议定 #12 走
  `.to_person` 时没核它的粒度）。本单内无合法解（改 `.to_person` 粒度＝层一必红；
  改 validator 过滤＝形态 B 已被否；另起新槽＝与决议明写相悖）。
  ⇒ 候选甲（`.to_person` 改楼级，换池级、语义更正确）／乙（给 #12 新起楼级槽，纯追加但
  两槽承载同一真实量）。**裁定前现状安全**：60 条诚实落歧义码，没有一条被捏造成确定判定。
- **更正：新增注册表条目是 8 条不是 9 条**。底稿「甲类 8 个」把 #6 算在内，而决议 §一.5
  把 #5/#6 一并挂起；#8 复用既有 `.to_ba` ⇒ 甲类真需新槽 7 条 ＋ #11 ＝ **8 条**。
  「#11 第 9 条」这个标签作废（实质未变）。
- **「>1 条→报错」落成 `blocked/ambiguous_fact_binding`**（既有原因码，四处消费者文案逐字
  适配，零新码零文档改动），不是抛异常——抛异常会让 30 栋批当场崩，与「诚实产出 unknown」
  的整体设计相悖。
- **丙类禁供闸只堵新开的 provenance 通道**，不堵既有 slot/measure 老通道：堵老通道会改动
  现存 159 条丙类义务的 `evidence_fact_ids` 与 notes（#15 今天经别名桥绑着一条布尔门状态行），
  与层三「零扰动」判据直接冲突。世界侧零声明由判定侧与世界侧两条独立断言钉死。
- **R2（`before` 比较符方向）本批实测未被踩到**：丙类三锚 159 条全部落 `missing_time_anchor`，
  `before` 分支无输入。禁供钉死生效。
- **`missing_time_anchor` 拆码案的分母变了**：供给后该码从 603 降到 **231**
  （＝丙 159 ＋ 挂起 72），拆码工单里的「447/156」口径须按新分母重推。
- **新债（本单发现，未修）**：乙11 `duration.delivery.repair_revision_proposal` **一条量承载
  两个收件人**（§2.1.3(p) 業主代表 ／ (q) 註冊承建商），两支永远同判，
  「交了承建商没交业主」这类偏差建不出来。已进分布授权草案 Q3。
- **测试**：三套一次跑齐 **3,380 通过/1 skipped/11 xfailed/0 失败**。改红的三处旧断言**都是原断言锁着错的一方**（`test_deadline_same_day_as_truthy` 喂布尔 True 断言 satisfied＝把 R1 那个语义反转当规格写着；`test_to_person_slot_has_cop_confirmed_threshold` 锁着 `unit="calendar_day"` 与锚点册外的孤儿锚名）——**「测试红了」这次的正确读法是「测试一直在保护缺陷」**。
- **方法留痕**：`world.meta` 在本步**必然变**（真改了权威注册表 ⇒ `registry_bundle_hash` /
  `deterministic_key` 换）。这不是锚失败，是「本步是换池级改动」的机器证据——
  与决议 §一.6「批级数字挂换池批」互为印证。整表级 allowed/禁止二分对本步太粗，
  故给 `sidecar.entries` 另配了行级细尺（既有行按 key 定位逐列比、只许 `time_anchor_key`
  从 null 变成已登记锚点、新增行只许落新槽）。

### 乙路 #30 决策门关案（2026-08-05）——做、两侧同批挂换池；真病灶改立 report 轴案

三线商议（官方/qwen/glm）＋底稿 506 行。**三线独立推翻底稿同一处归因**（满足通道
＝卡 sr01 → 契约 row 116 楼级直判，非碎片 any_true；底稿 `1-(0.69)^8≈0.93` 是数值
巧合——glm 逐栋核 `evidence_fact_ids`、官方三条反证含「0028 栋 0/6 碎片为真却
satisfied」、qwen 数 30/30 绑定事实 granularity=building）。定案五条与九步清单、
两债（override 对账闸优先级高于乙路本体／`.intended` 补登记归 B 批第一步）、
D6 合一裁定（性质断言＋预注册条数）详 `决议_乙路_20260805.md`。

**🔴 新案（#33，比乙路本身重）**：C2 report 轴独立采样（0.87、零条件依赖）与程序闸
零耦合 ⇒ **证据力为零的事实在解除义务**——51 契约行／本批 424 条 satisfied／16 张卡。
「通知≟呈交」法律分叉被消解（不论等价与否，不耦合即假阳）。0005 反向假阳并案。
**官方 E3 连带钉死**：evidence false 硬编码落 `open/observed_false_without_violation_basis`
无 violated 分支 ⇒「丙路改绑即可出 violated」不成立；抓逃逸的设计位＝**丁路**终局判据
双向行（`binding_contract_registry.py:1857` 已挂号未实现，世界输入 `.started` 已就位）。

**方法论一笔（入册）**：本案是「群发三线＋逐条亲验」的效益上限样本——底稿一处
机制错被三线用三套不同证据独立抓出并收敛到同一真相；单线审核大概率照单全收
（glm 在 1b 案就照单收过实施记录的归因）。承重事实「本批真值面该族零行」系 qwen
单线提出，我用 eval_report 探针独立复核后才入决议——**单线事实必须二路复核**。

### 期限锚实施三线审核关案（2026-08-05）——通过；M1-M4 已落；乙12 三线全票甲挂换池批

**三线判决**：glm **通过**（41 例亲跑；抓到「现状安全只对不注入口径成立」——换池批必然注入
⇒ 60 条 blocked ⇒ `allow_stop=False`）／qwen **七点全过**（全部亲跑复现：自建基线树重放
50 栋配对 PASS、30 栋层三纯重放 30/30 全等 272,122 恒等、红先行独立探针）／官方
**有条件通过 M1-M4**（代码本体独立复现全过）。**M1-M4＋glm 两条已落**（修复代理，
变异两组验证）：B6 射程扩到注册表全部带锚条目（楼级 8 硬断言＋碎片级 2 记录性断言，
反向闸从注册表现算；变异实测「摘碎片锚点回写」恰被新断言抓住＝原盲区）；L849→L858；
两处假陈述改准（不存在的布尔门／同面断言自称双向）；红先行配置订正——**纯 HEAD 上
六条全堵在绑定失败（open），语义反转只在「保留绑定通道、单独回滚 R1」配置下复现**
（「测试跑在缺陷不可能显现的输入上」又一例，这次抓在记录陈述层）。
修后 `worldgen/tests` 861 通过 0 失败。

**乙12 裁定（三线全票甲，实施随换池批）**：`.to_person` 注册表改楼级粒度，与分布授权
定稿同批落。三线互补的裁定依据：官方**实验证乙无效**（楼级新行与碎片带锚行共存仍
ambiguous——乙不是纯追加，须对既有槽做减法＋废掉刚落的 R5）；glm 补不对称警告
（若只加楼级行而留碎片行会给 #8 制造双候选）；qwen 补 **`.to_ba` 同形错位**——同一楼级
「送交 BA」事件按碎片独立采样，机制上允许跨碎片判出不一致（93 条里 70 sat/23 vio 混合
与此相容；本单前全落 missing_time_anchor 无判定故一直没显形），**建议换池批把 `.to_ba`
一并裁**（整体改楼级则碎片作用域只见 1 行，glm 的双候选警告即不触发——两线立场可调和）。
🔴 **换池批启动前必须裁定乙12（现默认甲）**，否则 60 条 blocked 阻止 allow_stop。

**建议队列（不阻塞，来自官方线）**：老 slot/measure 通道 `facts[0]` 单候选兜底；
四条真空可绿的循环断言；#9 引文丢触发前件；#9/#11 两个 `carrier_slot` 声明而不产出
（ownership 声明 vs bool registry 不产，与「两表无对账」旧债同族）。
另记：批验收工件幂等测试存在顺序敏感 flake（qwen 撞到，单跑绿）；层一绝对行数两次
口径不一（12,650/400 vs 19,412/694，池均已删，**引绝对数须带池留存**；碎片规模
`fragment_count_per_building` 两处声明同为 4 却推出不同行数，**未证一致，须显式打印**）。

**流程教训（已入记忆）**：`git diff` 备份不含未跟踪文件——本单 `rng_domains.py` 与
配对脚本动手前态没备到。**#32 代码层至此关案**；批级数字验收＋乙12 甲案＋分布授权
定稿＝换池批三件套。

### 分布授权定稿（2026-08-05，两线决策门）——换池批三件套之二关案

八槽参数定稿见 `决议_分布授权_20260805.md`（甲1-4/9/10＝4.8/2.6 通知族**标定前**估计；
甲7＝5.5/1.8 呈交族既有成分；乙11＝裸正态 0.45/1.15 **不写零膨胀标签**）。三条论证质量
裁决入册防复发：①**承继已标定参数会把标定目标一起承继**——4.1/1.9 的目标是「5-bin
不塌」，新槽不进 5-bin，照抄则 n=30 下六通知槽「至少一槽整批零违规」概率 0.90（分布须
覆盖两侧的约束被违反）；②**照抄分布标签前先读归一化机制**——`.to_person` 的
`zero_inflated_discrete` 因缺 `calib_zero_prob` 实为裸正态，照抄标签≠照抄分布；
③glm 在期限锚审核里反对 `.to_ba` 楼级化的「双候选」机制理由被官方线证伪
（楼级槽不走碎片路径），换池批裁 `.to_ba` 时**不得再引双行论**。
qwen 线当日降级（复核进程空转 3.1 CPU 时杀除，探活单在途）；本门两家族（官方＋glm）
满足关键分叉要求。落地并入换池批捆绑实施，不单独开单。
换池批三件套进度：分布授权 ✅／乙12 甲案（裁定✅落地挂捆绑）／批级验收表（待捆绑实施时预注册）。

### #25 真值落改关案（2026-08-05）——两线审核通过＋必须修落地；🔴 双池发现另立防呆

**实施**：29 行逐位命中（Q2 20/Q3 9/L2508 零动）；schema `oneOf:[boolean,const]`（保
2,741 行零字节漂移，三值字符串枚举与 A3 互斥）；`_truth_applicable_state()` 唯一判据
入口（`is True/is False`、未知取值 fail-loud）；`unknown_pending` 不进召回不进精确率
分母、单独计数进过门输出；红先行探针坐实旧代码静默进分母（4 failed）；决议清单四处
＋清单外两处同形缺陷同批改；端到端并排 443→423／149→134 两条预注册量精确命中。
精裁材料 7 份 18 条目块撤回/坐实分标（6 条原判就是真值错故「坐实」；行 67 →pending
不宣称系统对）。**两线审核全过零阻断**（glm 抽验 6 行逐位；官方全部独立复算含红先行
变异二式）。全量 3475→修后子集全绿。

**🔴 官方线承重发现（已入记忆 `project_adding_a_world_slot_reseeds_the_pool` 升级节）**：
真值证据基础＝`_reanchor` 池、端到端批＝`_fragcov2` 池——**同 seed301 标签两个不同池**，
Q3 对账前者 10/10 精确、后者 5/10 反号，全仓零文档说明。**已裁定（门内，证据无歧义）：
真值文件锚定 `_reanchor`（`baseline_batch_final_seed301`）**，锚池声明落三处（schema
description／Q3 九行 reason 纯追加池名＋断言锁定／实施记录 §5.4.1），跨池评分该族
不可引。**通则**：凡引用世界事实的真值/裁定/断言产物必须随行声明池身份
（`worldgen_run_dir`＋hash，seed 标签不是身份）。

**连带**：`443/149 自本次作废`（本台账毛病四节旧数已加删除线；引用须点名真值文件
版本与锚池）；顺手修 `link_truth_expected_cards.py` 既存 SyntaxError（2026-07-30 引入，
零测试 import 故无人发现——「没有执行实例的规则不会浮出来」再添一例）。
**尾巴（不阻塞）**：L2508 reason 重写另案（用的是已删推理，值合规、测试已豁免）；
24 行矛盾集冻结待另案；官方三条建议修（A9 oneOf 校验入主链/取值域闸 `type is bool`/
决议 §一 L1952 归类订正）；`conditionality` 三态扩枚举未决；`check_batch_acceptance`
门限随分母漂移未核（**换池批验收前必核**）；精裁材料头部统计旧口径/跨系列矛盾/192
条目块重复覆盖三件未处置。**#25 关案，新真值队列生成仍受乙路红线约束。**

### #29 BA→BD 关案（2026-08-05）——两线审核过＋必须修落地；qwen 线当日降级实录

**#29 全案闭环**：五卡射程（两线复核全票扩）→ 实施（14 步指纹链、金丝雀先验后写盘）
→ 硬判据失败暴露底稿前提假（10 卡消费单组合）→ 甲案并存（两线复核全票，轴积 1→2、
ba 格逐字还原 combo_key 字节不变）→ 两线审核**过**（官方：14 文件零未申报、332 行 sha
复算失配 0、重放对照臂未测哑；glm：ba/bd 两桶 6+4=10 逐字、发布门禁两池 12/12、
全量 3458 复跑一致）。官方三条必须修（「1→1 替换」三份未加注副本）＋row 124 承重
追记（甲案下补键是**必要条件**非「安全收窄」——A′ 恰一行要求）＋glm 观察 1（E 格
时间点注记）全部已落。`registry_bundle_hash` 三段沿革 `d6e0eb01→ced4d247→77f7ce74`。
**#33 记账**：只进分母不进分子（三计数 +0，结构＋实测双证；(卡,slot_ref) 级零行断言
＋变异对照落测试——官方原说卡级、实施纠为 (卡,slot_ref) 级并被 glm 独立坐实，
§4.4.3 的 row 103 挂别的槽是关键反例）。尾巴：全池 50 栋配对（换池动作）／
§3.6.3(c)＝#34 小案／registry notes 普遍陈述待清点。

**🔴 qwen preview 线当日三单三败（记档改派策略）**：①期限锚实施审核首单退出码 0
零产出（重试后写出文件但 stdout 恒空）；②#25 复核空转 3.1 CPU 小时杀除；
③#29 审核 5 小时墙钟仅 398 CPU 秒（约 1% 利用率＝卡网络等待非计算，用户发现，
我首判「CPU 在涨＝正常」**错**——**判进程健康须同时看墙钟与 CPU 比值**，
只看 CPU 增量会把慢速泄漏当成活跃）。⇒ 当日剩余审核/商议以官方＋glm 两家族满足
关键分叉；qwen 恢复后再入轮换。

**〔qwen 三败根因补记，2026-08-05 用户告知〕**：qwen 已撞**七天限**——三单三败
（零产出/空转/1% 利用率挂等）全是限额下请求挂起，不是端点故障。qwen 摘出审核轮换，
恢复前审核/商议＝官方＋glm 两家族。

### 换池捆绑批落地（2026-08-05）——乙路 A+B ／ 分布授权八槽 ／ 乙12 甲案含 `.to_ba`

**三件同批落码**，详 `实施记录_换池捆绑_20260805.md` ＋ `重核准记录_换池捆绑_20260805.md`。
全量 pytest **3,424 通过 / 0 失败**；两表 `stale_rows==0`；发布门禁两池各 12/12。

**乙路（#30）两侧同批**：真值侧 `card_overrides` 换真前件槽 `.intended`（一个字符串，
不进指纹）＋ 判定侧卡本体补 sr02（trigger）与卡级 trg01（进指纹，全链重建，
A 表 row 116 ＋ 桶表 row 208 两行 sha 同批刷）。金丝雀**先验后写盘**：
A 格触发 false ⇒ 动作面 0 条；**G 反例格坐实循环前件**——换回 `intention_notified` 后
「义务未履行」的世界整卡塌成不适用，换真前件后同一世界义务照常生成；
批面重放**适用 30→10** 与预注册逐条命中。

🔴 **一条预注册期望须订正（基数已变，不是失败）**：决议 §一.5 的
「evidence satisfied 27→8/10」**两臂实测都是 0**——**现状臂就已经是 0**，
原因是同批未提交的「判定诚实」产物态许可闸把本卡证据通道整体改判成
`open/artifact_state_not_valid_evidence`；那个 27 是冻结批产物（HEAD 代码）的数字。
⇒ 换池批预注册值改 `0 → 0`，「乙+丙则 2/10」一并作废。**可引的仍是「适用 30→10」。**

🔴 **乙12 ＋ `.to_ba` 楼级化把 qwen 的「担心」变成了「量到的缺陷 ＋ 修好的证据」**：
冻结批 30 栋两臂重放实测——#8 锚 `repair.prescribed.completed` 的 93 条里，
**base 臂有 15 栋出现同卡（`s2_1_3_r`）内 satisfied ＋ violated 并存**
（＝同一栋楼同一个「送交 BA」事件被逐碎片独立采样判出不一致），甲案后**归零**。
乙12 锚 60 条 `ambiguous_fact_binding → 0`、确定判定 60（上界口径）；
丙类三锚 159 恒等、确定判定 0；`s2_1_3_r` 触发器两臂逐条相同。

**新登记四条债**（决议要求随定稿落库，不留草案）：
①**乙11 建模保真度缺口**——一条量承载 §2.1.3(p)/(q) 两个收件人，两支永远同判，
「交了承建商没交业主」不可建模；解法＝拆两锚两槽（规则扩展另案）。
②`duration.notification.deadline` 本体仍挂 S2.5 标定档（4.1/1.9，96.3%），与新六条
同族不同参（85.1%）长期并存，下次换池批一并复审「标定档要不要退役」。
③**载体闸缺口**：乙11 的 `procedure.repair.revision_proposal.submitted_to_ba` 与甲9 的
`procedure.repair_supervising_ri.appointment.completed` 在 ownership_map 有登记、
不在 `sidecar_bool_slot_registry` 实采清单 ⇒ 从未产出（「140 声明 vs 46 实采」族）。
④`_sample_typical_distribution` 注释写「保证尾部不取 0」而实际只 clamp 负值、
挡不住下游 round 把 0.4 变 0 —— 将来要用零膨胀写法这是必须先修的坑。
（另：决议 §一.4① 的 **W2 override 与世界注册表对账闸**仍未建，优先级高于乙路本体；
决议 §一.3 的 **C2 report 轴供给独立性缺陷**另立新案，射程 51 契约行 / 424 satisfied / 16 卡。）

**教训一条（新）：对账测试的锚批会悄悄过期，而过期的表现形式是「报出一个假缺口」。**
`test_slot_alias_reconciliation.py` 报 `.intended` 是「新增缺项」，查实**不是缺口**——
它的世界侧锚 `baseline_batch_final_seed301` 30 栋里 **0 栋**有该槽，
而当前批 `wave1_closing_seed401_20260804` 30 栋里 **30 栋**都有（真 10/假 20）。
按甲（补别名）/乙（禁简化）/丙（世界侧真无对应）任一分类都会写下一句错话。
⇒ 新开**丁组 `GAP_ANCHOR_BATCH_STALE`**，并把「换锚会同时移动甲乙丙三组的判定基础、
属独立小案」写进注释。**判据必须在被筛人群上有意义——这次「人群」是那个批。**

**一处权威文本不一致（已按决议自己的委托条款裁定，供复核）**：
`决议_分布授权_20260805.md` §一表把甲类 `physical_bounds` 写成 [0,30]，
而同决议委托的逐槽替换文本（官方线商议 §六）写「physical_bounds 全部不动（甲类 [0,60]）」。
**按委托条款取后者**；差异对采样**零影响**（已核机制：先 clip 到 typical [0,20] 再 clip 到
physical，20 < 30 ⇒ 取 30 还是 60 逐字节无差别）。说明已同批写进 `registry.py` 组头注释。
⚠️ 这**不属于**「三份决议互相冲突」（那要停下回报），是同一份决议内表格与其委托条款的转写差异。

**尾巴**：KG 重灌押后（新语义槽要进运行时切片必须重灌；不重灌**不影响触发器求值**，
因触发器走 `slot_ref_id→slot_role_map→slot_id→事实`、不查 `semantic_slots`，
且 `procedure` 域不在 `_FRAGMENT_DOMAINS` 里 ⇒ 本卡作用域判定不变）；
`verify_rng_isolation_pairing.py` 层一字节锚对本单不适用（两槽行数 7–8→1 必红，
属甲案预期影响，判据须改成「两槽行数按预期变、其余单元不变」）；
`check_batch_acceptance` 门限随分母漂移未核（换池批验收前必核）。
