# 决策门简报：1b fsp_estimate 全楼取 max 解耦（#31，多方群发）

> 病灶（官方线商议实测，`商议结果_official_rng隔离_20260805.md` 最大风险节）：
> `generator.py:2851-2852` `fsp_estimate = f(max over 全楼 conditions)`——加片段顶高
> max ⇒ 既有片段 fsp/flag **确定性**改变（甲-a 实测 80/200 保留片段 fsp 值变、
> 87/200 至少一 flag 翻、8 条直入规则卡槽 `assessment.fsp.below_required_safety`）；
> rng 隔离治不了（1a 已落且不覆盖此通道）、对 expected_verdict 隐身（藏匿轴）。
> 另一预记：1b 动 assessment_flags 时 1a 的四阶段↔sidecar 潜伏耦合锚可能红
> （红了是通道发作不是锚坏——台账「1a 全序落地」节）。

## 待裁

**Q1 语义**：fsp（结构性能估计）该按什么作用域聚合？候选：
(a) 维持全楼 max（现状——语义上"整栋楼的结构性能由最差处决定"未必错！但使加片段
非追加）；(b) 逐片段 fsp（各片段独立估计，楼级另出聚合视图供消费）；
(c) 全楼 max 但**只对"结构承重类"条件取 max**（收窄输入集，使新增非结构片段不扰动）。
⚠️ 这是**世界建模语义**问题不是工程问题——"哪种聚合忠实于真实楼宇结构评估"优先于
"哪种好实现"。
**Q2 真值联动**：fsp 变 ⇒ `assessment.fsp.below_required_safety` 槽值变 ⇒ 依赖它的
expected_verdict 与卡判定都动——1b 属换锚级改动，与换池捆绑（同锚）是否成立？
**Q3 验收**：1b 落地的配对判据（1a 的字节锚会按预记红——正确的验收形是什么：
allowed-change 集扩到 assessment/derived，还是逐楼语义对照）？

## 输出要求
Q1 给明确选项与理由（世界忠实性优先）；Q2/Q3 各一句。写你被指定的输出文件；只读。
