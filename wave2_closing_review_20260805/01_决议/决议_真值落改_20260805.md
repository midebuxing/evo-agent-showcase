# 决议：#25 真值落改案（2026-08-05，底稿＋两线复核合成）

**材料**：底稿 `底稿_真值落改_20260805.md`＋两线复核 `复核_25落改集_{official,glm}_20260805.md`
（两线独立重扫均得**恰好 29 行、逐位同集、无第 30 行**）＋我对 `artifact.notice.
investigation_intention` 的注册表事实核（声明同步未执行，已并 #33 第二形状）。
qwen 线降级期，两家族满足关键分叉。

## 一、落改集定稿（29 行）

- **Q2 族 20 行**（最小自洽单位——跨栋一致性断言②要求全族情形号统一到 1）：
  15 行 `false→true`＋5 行仅改情形/reason（值不变）。行号见 glm 复核 §四表。
- **Q3 族 9 行**：L599/600/601 **`true→unknown_pending`**（reason 自述判不适用而值为 true
  的矛盾行，且实测双缺）；L1118/1396/1674/2230 `false→`按 Q3 判据（双缺者 `unknown_pending`、
  proposal 在者 `true`——逐行值见底稿 A5）；L602/1952 仅改 reason。L2508 不动（唯一现文合规行）。
- **28 行矛盾集读 4 行**（两线同裁）：登记表授权射程＝599-602 四行；其余 24 行属
  §2.1.3(a/g/k/l/m/p) 六项、无任何口径裁定过，**冻进 A8 断言（期望 24）待另案请示**，
  不得自决扩范围。

## 二、判据修正一条（两线分歧的裁决）

**删除底稿 Q3 机械规则的 `ELIF NOTIFIED is true → applicable=true` 分支。**
官方线曾以登记表「双缺」一词证其必需（notified 在则非双缺）；glm 主张登记表只授权
proposal 一个正向代理。裁决取删——**决定性证据是世界自己的交叉表**：乙路案实测
seed401 的 0005 栋 `intended=false ∧ notified=true`（`procedure.investigation.
intention_notified` 是独立采样布尔，**本世界里「已通知」不蕴含「有意」**），该分支会对
这类栋造假 `true`。当前池它是死分支（唯一 notified=true 的 0009 栋 proposal 已命中
第一分支），删除对 29 行零影响；notified=true ∧ proposal=false 的将来形态落
`unknown_pending`（诚实：通知在场但意图证据缺席，且世界不保证蕴含）。

## 三、连带定案

1. **`artifact.notice.investigation_intention` 不得作「通知已作出」证据**（两线＋我三方
   一致；glm 四条理由入档）。落改实施须在 Q3 规则注释里**显式排除**该槽（官方线抓的
   文档层缺口）。
2. **`unknown_pending` 扩枚举与判据改造同一提交**（底稿 R2 坐实）：
   `score_clause_coverage.py:851/:960` 真值判断改 `is True`；精确率侧 `:874-875`
   else 分支的分母语义同步核；连带暴露面 `probe_coverage_both_sides.py:140-141`／
   `audit_atrisk_truth_items.py:84`／`link_truth_expected_cards.py:130`／
   `test_applicable_truth_circumstance_consistency.py:90`（`is False` 逃逸）一并改。
   schema `applicable` 扩 `["true","false","unknown_pending"]` 三态（布尔＋字符串的
   具体编码由实施按 schema 现形定，分母处理＝unknown_pending **不进**召回分母也
   **不进**精确率分母、单独计数出报表）。
3. **底稿两处订正**（官方线）：§5.2「只改 15 行必红」改为「只在情形号同批翻时红；
   只翻值测试全绿但矛盾行 28→43」（假验收信号警告）；§七.4 的 56/41 复算不出，
   以三谓词判据实得 51/36 为准（或按其判据重述）。
4. **时序**：本案代码＋schema＋29 行现在落（真值文件只服务 seed301 旧池，冻结批不重评）；
   **新真值队列生成仍受乙路红线约束**（判定侧未落前不得生成覆盖该细族的队列）。

## 四、验收

改前改后 diff 断言＝恰好 29 行、逐行等于定稿清单；三条一致性测试全绿＋A8 冻结断言
（矛盾行 24）；评分器三态处理红先行（先造一行 unknown_pending 喂旧代码证明静默进分母，
再落修转绿）；W2 blind 红线核（真值路径零进 agent 运行时 import 面）；
全量 pytest 零红。
