# 审核结果：#29 BA->BD 五卡实施＋甲案并存（glm 线，2026-08-05）

- **审核线**：glm（OpenCode，`coding-plan/glm-5.2`）
- **性质**：审核门（代码对不对）。只读审核，独立完成、未读其他线结果。
- **结论**：**过**。逐条实测坐实，七审核点全过；列两条非阻断的小观察。
- **执行方式**：实跑脚本／读源码／重建术前状态逐字节比对，不转述实施记录当证据。

---

## 一、七审核点逐条裁定

### 1. 五卡卡本体 ✔ 过

**实测**（`rule_cards.json` 当前工作树）：

| 卡 | `recipient_key` | `sr02.qualifiers.actor_role_key` | 形态 |
|---|---|---|---|
| 123 §3.6.2(A)(d) | `bd` | `bd`（与 `artifact_key` 同桶） | 改值 |
| 124 §3.3.2(A)(c) | `bd` | `bd`（**补键**，原 sr02 无角色轴） | 补键 |
| 125 §3.5.2(A)(c) | `bd` | `bd` | 改值 |
| 126 §3.7.1(d) | `bd` | `bd` | 改值 |
| 第五卡 §3.4.2(A)(b) | `bd` | sr02.qualifiers ＝ `{}`（未触碰限定符轴） | 只改 recipients |

**计数核对**：全库 `recipient_key="ba"` **36**／`"bd"` **5**；`actor_role_key="ba"` **18**／`"bd"` **4**。与实施记录 §2.2、official 复核 §5.2 预测逐字相符。

**`source_quote`/`source_section` 零差异**（亲验）：用 `HEAD ＋ 备份 patch` 重建术前 `rule_cards.json`，与术后逐卡比对--**恰 5 张卡有差异，且 5 张的 `source_quote` 与 `source_section` 全部相同**。故 §3.4 的「漂移 37 与本案无因果」成立（模态审计器只读这两字段）。

**`normalized_rule_text` 三张顺手改**（123/125/126「to BA」->「to the Buildings Department」、124 补回收件人）：

- 实施记录 §2.2 自陈「这是自己拿的尺度」并请复核留神。
- 我的判：**在射程内、不超射程**。依据：①底稿 §2.2 把这三张逐字标为「引文 BD／规范化 BA」同一系统性错译三实例；②该字段真有消费者（`llm_orchestrator.py` 查给大模型看、`report_writer.py` 当报告规则文本兜底），留着「to BA」等于继续把错收件人交给消费者，与本件目的直接冲突；③判定面零影响（闭包验证器不读该字段）。回退成本＝改回三个字符串再刷一遍指纹链，可控。

**第五卡 `interpretation_notes` 处理**：原 `[3]` 第 2 条「family 不在册」过期陈述**已删**（notes_count 由 3 降到 2）；第 1 条重写为「recipient_key 已于 2026-08-05 按中文正文纠正为 bd」加沿革。`family_index.json:973/974/981/982` 实查四命中，证伪「family 不在册」的自陈--并入第五卡确不连带 family 重建。

### 2. 重核准记录 ✔ 过

**实质重核定性成立**：`NEGATIVE_AUTHORIZATION_CLAUSE`（`binding_contract_registry.py:47-54`）八项失效触发里，本件真触发了三项--**卡指纹 ＋ 限定符 ＋ row 124 角色轴的存在与否**。重核准记录 §一/§二 显式声明「不得套件四批 1 的机械证明模板」，方向正确。

**逐行 sha 台账**（重核准记录 §3.1）：A 表 5 行旧->新 sha 全部由我对**已改完的落盘卡**实算 `card_fingerprint_v1` 复核--5 行全部 `OK`：

| row | 实算 fp（前 16） | 表中 sha（前 16） | 比对 |
|---|---|---|---|
| 39 | `0bc176723b1c32cd` | `0bc176723b1c32cd` | OK |
| 123 | `a038f4e71cbbe1a6` | `a038f4e71cbbe1a6` | OK |
| 124 | `5bf1c8af90e6cd40` | `5bf1c8af90e6cd40` | OK |
| 125 | `f5cf7d30d9270d40` | `f5cf7d30d9270d40` | OK |
| 126 | `d304e05d51d2f42b` | `d304e05d51d2f42b` | OK |

**甲案后「卡包 sha 未变故不动」的判断对**：甲案只改 `registry.py`（感知层）一处，未动 `rule_cards.json`。实测 `rulecard_pack_sha256 = fe7dac88f22131f3d4c49e44403e7d708b5732861824c61d09f71088fd0602a6`（与重核准记录 §3.1 写的术后值逐字相同），A 表 `registry_digest = 516a9052…`、桶表 `cac139f9…` 也均未变。⇒ 甲案确实不需要回炉重核准记录，判断正确。

### 3. 世界侧甲案 ✔ 过

**`ba` 格逐字还原**（亲验 `combo_key`）：`registry.py:3912-3922` 的 `qualifier_axis_product` 现为两格--`{artifact_key: record.inspection_log, actor_role_key: ba}` ＋ `{…, actor_role_key: bd}`。源码注释（`:3913-3915`）明写「ba 格逐字保持 #29 之前的原样：sub_rng 是纯键派生、键里带 combo_key，键一字不差才谈得上既有行不位移」。

**`bd` 格形状**：与 `ba` 格同 `artifact_key`、仅 `actor_role_key` 不同，结构对称。

**notes 改写**：原 notes「檢驗記錄已呈交建築事務監督（§3.3.2(A)(c) 等）」（自证其错--§3.3.2(A)(c) 原文恰是屋宇署）已替换为「本槽同时服务屋宇署(bd)与建築事務監督(ba)两组收件人」+ 10 张卡的实测分账（4 bd ＋ 6 ba）。注释里那句被证伪的断言（「全库没有任何卡要求『檢驗日誌呈交建築事務監督』」）留在原位加作废注记，不删。

**R2 保护名单 13 格仍全 ba**：`reporting.artifact.submitted` 轴积 13 格实测全 `actor_role_key=ba`，且不含 `record.inspection_log`（由 `test_axis_product_shape_is_locked` 锁死）。

### 4. 两表与派生链 ✔ 过

**A 表 5 行 sha 现值与卡包一致**：见 §一·2 复核结果。

**桶表 3 行（194/196/198，不止 196）--订正成立**：实查桶表按 `rule_card_id` 命中**三行**：
- row 194（＝A 表 row 124 同卡 §3.3.2(A)(c)）
- row 196（＝A 表 row 39 第五卡 §3.4.2(A)(b)）
- row 198（＝A 表 row 125 同卡 §3.5.2(A)(c)）

三行 sha 与对应卡 `card_fingerprint_v1` 全部 `OK`。卡 123/126 在桶表确实无行（按 `rule_card_id` 全表扫描确认零命中）。底稿 §6.1 #6 / §6.2 步 11 与两份射程复核都只点了 row 196，实施记录 §二·订正① 把 194/198 补上是**对的**--`bucket_binding_registry.py` 的 `_validate_against_pack()` 逐行比对 sha，漏刷会让这两行进 STALE。

**slot_index 三桶分裂形状**：`slot_index.json` 实测--
- `{actor_role_key=ba, artifact_key=record.inspection_log}` 桶：6 张卡（射程外 6 张 ba 消费卡）
- `{actor_role_key=bd, artifact_key=record.inspection_log}` 桶：4 张卡（123/124/125/126）
- 原 `{artifact_key=record.inspection_log}` 裸桶（唯一成员卡 124）：整条消失（因 124 补了 actor_role_key=bd）

合计 10 张卡，与底稿 §6.1 #16「3＋6＋裸桶里的 124 ＝ 10」逐字吻合。第五卡 sr02.qualifiers 为 `{}`、不进 slot_index（按限定符分桶），符合底稿 §2.4 形态三「只改 recipients」描述。

**回读断言**：A 表 `ACTIVE=125 / STALE=0 / DISABLED_REASON=None`；桶表 `ACTIVE=207 / STALE=0 / DISABLED_REASON=None`。两表 `registry_digest` 与实施记录 §1.1 锚沿革逐字相同。

**发布门禁两池 12/12**：`check_applicability_release.py` 实跑 `gen_seed_301` 与 `gen_seed_401` 两池，各 `通过 12 / 失败 0`。

**派生重建**：`build_card_applicability_manifest.py --check` 报 `卡总数 469 / 已授权 82 / 强校验全过`；`collect_rulecard_bundle_violations` 恰 3 条全部术前既存（三张空 `slot_role_map` 卡，与基线逐条相同）；`eval/family_crosswalk_v1.json` `card_count` 合计 469、全文件无 `"ba"` 字面量。

### 5. 重放与断言 ✔ 过

**--both 臂设计真测到「ba 格不动」**：`replay_ba_bd_out_of_scope_drift.py` 的 `--both` 走 `mutate_world` 的 `both` 模式--「保留 ba 事实逐字不动、追加 bd 事实」（甲案核心主张）。我实跑 20 栋：

| 臂 | 射程内差异 | 射程外差异（判据必须 0） |
|---|---|---|
| 替换路（缺省） | 0 | **4 张卡漂移**（s3_6_2_b_a/s3_6_2_b_b/s3_6_3_c/s4_4_3） |
| `--both`（甲案） | 0 | **0** |

⇒ `--both` 没把重放测哑、对照臂仍报 4 张证明有敏感度，甲案 3->0 实测成立。8 栋样本下替换路只报 3 张（s3_6_2_b_a 需更大样本才显现），§3.3 与 §6.5 数字差异是样本量差、不是矛盾。

**(卡,slot_ref_id) 级零行断言＋变异对照真实非空**：实查 6 张 ba 消费卡的 `(rule_card_id, slot_ref_id)` 集合得 6 条精确元组。`BINDING_CONTRACTS` 对这 6 条元组**零行命中**；四个派生视图（`SCOPE_PRECISE_BINDINGS`/`VALUE_CONSUMPTION_BINDINGS`/`DIAGNOSTIC_ONLY_BINDINGS`/`REJECTED_BINDINGS`）零泄漏。

**§4.4.3 row 103 是关键例**（核粒度订正）：§4.4.3 那张卡在 `BINDING_CONTRACTS` 里**确实有 row 103**，但它挂在 `sr01 / scope.component.inspection_included`（**另一个槽**）、`policy=diagnostic_only / verdict_permission=none`，与 ba 格消费的 `sr05 / reporting.record.submitted` **无关**。⇒ official 复核 §2.2/§四 称「在 BINDING_CONTRACTS 里零行」**在卡级不成立**，实施纠为 (卡,slot_ref_id) 级是**对的**（既有教训：事实身份＝槽名＋限定符，只按一半判必错）。变异对照 `test_mutation_ba_grid_authorization_turns_red` 也键在真正读 ba 格的 `slot_ref_id` 上、用与真闸同一个谓词查，不是手写复制品。

### 6. 金丝雀与判定影响 ✔ 过

**五格＋变异对照**：`canary_ba_bd_recipient_alignment.py --batch-root wave1_closing_seed401_20260804` 实跑，判据四条全部成立：
- ① D（卡 bd／世界 bd）与 A（卡 ba／世界 ba）逐格同解
- ② B（卡 bd／世界 ba）/ C（卡 ba／世界 bd）与 A 不同，均出 `qualifier_conflict: 2` 绑定断裂
- ③ 第五卡 A/B/C/D 四格恒等（判定影响恒等 0）
- ④ 变异对照：D 的世界侧改回 ba ⇒ 塌回 B 形状

**12 栋只 1 栋构成判据**的诚实边界：金丝雀前置闸把 11 栋全 `not_applicable` 的栋标为「不构成判据」而非「未通过」，避免空跑出假绿。这条边界写进金丝雀脚本里，不是注释。

**10 栋 55 条 ba 格义务全 unknown、satisfied 0**（亲验）：取 `wave1_closing_seed401_20260804` 前 10 栋，对每栋 `closure_validation_result.json` 的 `obligations` 找 `evidence_fact_ids` 与该栋 ba 格 `reporting.record.submitted` 事实 id 有交集的义务--共 **55 条**，`satisfaction_status` 55/55 全 `unknown`、`open_reason_code` 55/55 全 `artifact_state_not_valid_evidence`、`satisfied`/`violated` 恰 0。⇒ #33「只进分母不进分子」的实测面成立。

**第五卡四格恒等**：金丝雀输出实得 `[第五卡]` 行在 A/B/C/D 四格的 closure 与 sat 计数完全相同（n=5，blocked=3，closed=2，sat={satisfied:2,unknown:3}），运行时实证「判定影响恒等 0」。

⚠️ **小观察 1**（非阻断）：实施记录 §3.1 把 E 格描述为「`ActorRoleCrosswalkError` 当场抛」。E 格的语义是「卡写 bd ／词表未扩 ⇒ 闸应抛」。但 #29 落地后 `bd` 已进词表，**重跑 E 格会得到「未抛（闸失效）」**--这是预期的（canary 脚本头 `:22-26` 明写「落地后 E 格会报未抛，那是预期的，不是闸坏了」）。§3.1 没显式标注「该结果是改词表**之前**跑出来的」，会让后来重跑的人误以为闸坏了。属记录措辞小漏，不是闸真坏。

### 7. 记录与作废注记完备性 ✔ 过

**底稿四处作废注记**（亲验）：
- §4.2（`:299/301`）：`1->1（替换）` -> `1->2（并存）`，标题加 🔴 已作废改判
- §4.4（`:394`）：`1->1` 作废、改判 `1->2`、每栋 +1 行
- §7.3 论据一（`:659`）：`数字作废，结论不变`（仍 #29 先落、不必等 #33，但理由换）
- §9.2 表（`:783`）：`该槽行数 1 行/栋前后相同` 整行加删除线 + 🔴 本行作废、换行级细尺三条

**§4.4「+14 行」高危错引点**（`:402-413`）：单独红框标注「本段是全底稿最高危的错引点，读到这里必须停一下」+ 三条 bullet 逐字钉死「+14 行／+7.53% 是两个槽都加 bd 那一档、与本件（单槽 +1 行 ≈ +0.54%）差一个数量级、不得用它反对甲案」。防错引设计到位。

**实施记录 §六 判据更新表述**：§6.2 用「行数 +1」新口径取代底稿 §9.2「行数不变」；行级细尺三条（① 除该槽外逐字节相同 / ② 该槽 1->2 行/栋 / ③ 新增行只许落 bd 格、ba 行与旧池同键）形状照抄期限锚案先例；§6.3 把「+0」的成立条件写成机器断言（`test_ba_grid_has_no_value_consumption_authorization` ＋ 变异对照）。

**防漂移文字在**：实施记录 §四（#29 本体）与 §6.4（甲案）两条防漂移条款逐字随行，明写「任何引用本件『已完成』来主张 report 轴问题已解决的说法均不成立」。甲案补一条「4 卡是从替换路的断供还原到 #29 之前的供而证据力为零、不是修复是还原」--把「甲案恢复了接线」与「#33 仍要动这 4 卡」两件事分清，防认证效应。

---

## 二、附加核点：与在途资产零冲突 ✔ 过

**期限锚案资产零触碰**（亲验）：

- 用 `HEAD ＋ 备份 patch` 重建术前 `registry.py`，与术后逐行 diff--**全部差异集中在一个 hunk（`:3882`，`reporting.record.submitted` 块）**，37 增 1 删。`duration.*` / `time_anchor_key` / `deadline` 区域（行 2700-3400）**零命中**。
- 期限锚测试三件套（`test_deadline_anchor_binding.py` / `test_deadline_anchor_supply.py` / `test_deadline_anchor_emission.py`）实跑 **43 passed / 0 failed**。
- `duration.delivery.deadline.to_ba` 的 `recipient_qualifier.actor_role_key = "ba"` 实测仍是 `ba`（`registry.py:2904-2906` 区域未动），与 §2.1.3 呈交建築事務監督一致。

**1a/1b 资产零触碰**：`test_rng_isolation.py` / `test_registry.py` 中 `duration.delivery.deadline.to_ba` 的 `ba` 硬断言保持不动即绿（实施记录 §3.5 已坐实，本次复核未再跑全量）。

**`registry_bundle_hash` 变化是换池级的正当结果**：术后实测 `77f7ce747d9424211b9c078ee1d2e284e85d92e44d4b67395a3acd434be95815`（与实施记录 §6.1 逐字相同）。变化来源是 `registry.py` 的 `reporting.record.submitted` 轴积 1->2，属换池级正当变更；甲案与 #29 本体同批落、共用同一次换池，不额外增加锚变动成本。

---

## 三、全量 pytest 复跑 ✔ 过

```
3458 passed, 1 skipped, 11 xfailed, 4 warnings in 820.54s
```

与实施记录 §6.5 `3458 passed / 1 skipped / 11 xfailed / 0 failed` 一致（耗时 820s vs 记录 773s，正常波动）。#29 本体 3454 -> 甲案 3458，净增 4 条断言（`test_axis_product_shape_is_locked` ＋ `test_ba_bd_recipient_alignment.py` 新增的 (卡,slot_ref_id) 级零行断言＋变异对照等）。

---

## 四、收尾双审计器复核 ✔ 过

`audit_action_slot_wiring.py`：无 evidence 绑定的义务节点 **101/487 ＝ 20.7%**，与实施记录 §3.4 一致。产物写入 `experiments/action_slot_wiring_audit/`。

`audit_rulecard_modality_drift.py`：卡 **469** / 条款引用 **480** / 已覆盖 **206** / 漂移 **37**（降级 34 ＋ 升级 3），与实施记录 §3.4 一致。因 `source_quote`/`source_section` 全 469 张零差异（§一·1 已证），此 37 是当前卡包的既有基线、非本件引入。

---

## 五、小观察（非阻断）

⚠️ **观察 1**：见 §一·6 末段--实施记录 §3.1 对 E 格的描述是改词表前的预期形态，落地后重跑会得「未抛」。canary 脚本头已自标注，但实施记录未显式说明该结果的时间点。建议在 §3.1 加一句「E 格结果是改词表前跑出来的；落地后重跑会得『未抛』，那是预期的（见 canary `:22-26`）」。

⚠️ **观察 2**：实施记录 §3.3 的替换路重放是 8 栋报 3 张漂移卡，§6.5 的甲案重放是 20 栋报 0 张漂移、对照臂报 4 张。两者样本量不同（8 vs 20），第 4 张（s3_6_2_b_a）只在更大样本下显现。引用时须点名样本量，避免「3 vs 4」被误读为矛盾。

---

## 六、结论

**过**。七审核点全过、附加核点全过、全量 pytest 零红、收尾双审计器数字与实施记录逐字相符。两条小观察属记录措辞层级，不影响代码正确性与验收有效性。

实施记录 §五 的七条尾巴（射程外漂移待决策门 / KG 重灌 / `registry.py` notes 普遍陈述为假 / §3.6.3(c) 两卡 / T7 daily / 全池 50 栋配对 / `test_source_coverage_matrix.py:1068` 夹具复核）已逐条显式登记、无隐形欠账。
