# 审核结果（official 线）：#29 BA→BD 五卡实施 ＋ 甲案并存

- **日期**：2026-08-05
- **性质**：审核门（代码对不对）。方向与射程不重裁。
- **边界**：只读审核；未连 Neo4j；未改仓库任何文件（本文件除外）。
- **工单**：`审核工单_29实施_20260805.md`

---

## 总裁定：**过**（含 3 条必须修、6 条应修，全部在记录/注释层，代码零阻断）

七个审核点＋附加核点全部通过。**代码与权威数据层没有发现任何缺陷**：
改动面封闭（14 个文件，零未申报）、指纹链自洽（两表 332 行全表复算失配 0）、
判据可复现（我亲跑的每一个数字都命中）、护栏配变异对照且非空过。

**所有关键数字我都独立复算过，无一转述**（详各节「亲验」与文末复现清单）。

**必须修的三条是同一件事**：已被甲案推翻的那句「组合数 1→1（替换，不是新增）」
在仓库里还剩 **3 份未加注副本**，其中一份是 official 甲案复核逐字列出、
实施漏做的改动项。底稿与实施记录里的同类表述都已正确加注，只差这三处。

---

## 审核方法：先把「#29 到底改了什么」变成机器事实

工作树里同时躺着期限锚案 / 1a-1b / c55 等多件在途改动，
**不能拿 `git diff HEAD` 当 #29 的改动面**。故先重建术前工作树：

```
HEAD(8c38690) 的文件 + 杂物箱/备份_29_BA_BD_20260805_tracked.patch
  → scratchpad/full29/   （48 个文件，git apply 全部干净）
未跟踪侧：直接用 杂物箱/备份_29_untracked/ 逐文件比
```

**#29 的真实改动面 ＝ 14 个文件**（行尾 CRLF 归一后逐文件比对得出）：

| 类 | 文件 | 差异行 |
|---|---|---|
| 权威卡包 | `regulations/.../rule_cards.json` | 32 |
| 受控词表 | `regulations/.../controlled_vocabularies_v1.json` | 1 |
| 派生物 | `regulations/.../slot_index.json` | 43 |
| 世界注册表 | `worldgen/registry.py` | 38（**唯一一处 hunk**）|
| 世界对照表 | `worldgen/actor_role_crosswalk.py` | 9 |
| A 表 | `closure/binding_contract_registry.py` | 18 |
| 桶表 | `closure/bucket_binding_registry.py` | 6 |
| 脚本注释 | `scripts/build_rulecard_zh_sidecar.py` | 8 |
| 测试 | `worldgen/tests/test_reporting_axes_slots.py` | 49 |
| 测试 | `agent_v1/tests/test_actor_role_crosswalk.py` | 9 |
| 测试（未跟踪） | `closure/tests/test_c55_consumption_rows.py` | 25 |
| 新建 | `agent_v1/tests/test_ba_bd_recipient_alignment.py` | 新 |
| 新建 | `scripts/canary_ba_bd_recipient_alignment.py` | 新 |
| 新建 | `scripts/replay_ba_bd_out_of_scope_drift.py` | 新 |

⇒ **与实施记录 §2.1／§6.1 声明的改动面逐项吻合，零未申报改动。**
`family_index.json` 与术前**逐字节相同**（步 2「不改」坐实）。

---

## 审核点①　五卡卡本体 —— **过**

**亲验（结构比对，不是文本 grep）**：术前 vs 现在逐卡 canonical 比较，
**恰 5 张卡有差异**，卡 id 集合不变（469/469）。逐字段拆开：

| 卡 | 改动字段（全部，无遗漏） |
|---|---|
| 123 §3.6.2(A)(d) | `recipients[0].recipient_key` ba→bd；`slot_role_map[1].qualifiers.actor_role_key` ba→bd；`normalized_rule_text` "to BA"→"to the Buildings Department" |
| 124 §3.3.2(A)(c) | `recipient_key` ba→bd；`actor_role_key` **`<ABSENT>`→bd**（补键）；`normalized_rule_text` 补回收件人；`interpretation_notes[0]` 重写 |
| 125 §3.5.2(A)(c) | 同 123 |
| 126 §3.7.1(d) | 同 123 |
| 第五卡 §3.4.2(A)(b) | **仅** `recipient_key` ba→bd ＋ `interpretation_notes[1]` 重写、`[2]` 删除 |

- **只动该字段＋`interpretation_notes`**：成立。五张卡合计只出现上述字段，
  无任何第三类字段被动过。
- **全库 `source_quote` / `source_section` 零差异**：**亲验 0 / 0**（469 张逐卡比）。
- **计数**：`recipient_key` ba **41→36** / bd **0→5**；
  `actor_role_key` ba **21→18** / bd **0→4** —— 与实施记录逐字相符。
- **第五卡删掉的 `interpretation_notes[2]`**（自称 family 不在册）确属过期陈述：
  `family_index.json` 与术前逐字节相同，且实施未触碰该文件 —— 删注记而不改索引是对的。

### `normalized_rule_text` 三张顺手改：**判不超射程**

三条独立理由，逐条亲验：

1. **是卡内自洽修复，不是新主张**。这三张卡自己的 `source_quote` 写的就是
   **"BD"**（123/125/126）；第五卡与 124 的 quote 直接写 "Buildings Department"。
   改之前 `normalized_rule_text` 写 "to BA" ＝ 与同卡引文自相矛盾。
2. **中文权威正文亲取**：`regulations/markdown/MBIS_CoP_2023.md:969/1110/1187/1311/1372`
   五行**措辞逐字同文**，均为「在檢驗工作完成後，註冊檢驗人員須將檢驗日誌**呈交屋宇署**」。
3. **判定面零影响、指纹面零风险**：闭包不读该字段；而两表 sha 是在**改完的落盘卡**
   上实算的（我复算 A 表 125 行 / 桶表 207 行**全部**与卡包一致，见审核点④），
   故顺手改没有留下任何指纹不一致。

⚠️ 一处记录层不足（不影响裁定）：`重核准记录` §2.3 逐要素表只说「其余七要素未动」，
**没有披露这三张卡的 `normalized_rule_text` 也进了新指纹**。实施记录 §2.2 已显式自陈
（🔴 那段），故披露责任已尽，但重核准记录本身不完整。

### 「modality 漂移 37 与本案无因果」：**抽验成立**

不是转述——我去看审计器读什么：
`audit_rulecard_modality_drift.py:506` 只取 `source_quote`、`:508` 只取 `source_section`，
`normalized_rule_text` 在该文件里**只出现在 docstring**（`:10`），无代码读取。
而这两个字段全库零差异（上文亲验）⇒ **因果链结构上不存在**。

---

## 审核点②　重核准记录 —— **过，含必须修 1**

### 「实质重核」的论证：成立

`binding_contract_registry.py:47-54` 的 `NEGATIVE_AUTHORIZATION_CLAUSE` 逐字读过，
八项触发器里本件确实动了**卡指纹 ＋ 限定符**，且 row 124 是**角色轴从无到有**：

```
row 124 术前 qualifier_axis: artifact_key=record.inspection_log
row 124 现在 qualifier_axis: actor_role_key=bd,artifact_key=record.inspection_log
```
（术前值取自重建的术前 A 表；现值 `BINDING_CONTRACTS` 实取。）

⇒ 拒用件四批 1 的「实质字段逐字未动」机械模板，是对的。

### 🔴 一处比记录说的**更重要**：row 124 补键在甲案下不是「安全的收窄」，是**必要条件**

记录 §2.2 把补键定性为「收窄，方向安全（授权面只会变小）」。这在替换路下成立，
但**甲案之后该判断已经不够**：

- A′ 合同解释器要求「**恰一行**带 aggregation=building 标记且值为布尔」
  （`obligation_deriver.py:2898` 起，`_is_marked_agg = len(bound) == 1`，
  多行 → `blocked/schema_contract_violation`）；
- 甲案后该槽每栋供**两行**（ba/bd 各一）。若 row 124 保持无角色轴，
  `_filter_by_qualifiers` 会同时命中两行 ⇒ **该卡必然 blocked**。

⇒ 补键从「可选的收窄」升级为「甲案能成立的前提」。**这不减损记录的结论，
反而加强它**；但记录里没有这一层，读者会低估这一行的重要性。建议补一句。

### 甲案后「卡包 sha 未变故不动」：**判断对**

**亲验**（全部现算）：

| 锚 | 实算值 | 记录声明 |
|---|---|---|
| `rulecard_pack_sha256` | `fe7dac88f22131f3d4c49e44403e7d708b5732861824c61d09f71088fd0602a6` | 一致 |
| A 表 `registry_digest()` | `516a9052a592818bdb6e4cb4161d0bf2eb5398c9f0e3b3648eab566a6a24ff37` | 一致 |
| 桶表 `registry_digest()` | `cac139f9c808d24a13e15c772020f8dcf5c546cade90b2fa55f817c1392347c0` | 一致 |
| `STALE_ROWS` / `DISABLED_REASON` | `[]` / `None`（两表） | 一致 |

甲案的改动面（registry.py ＋ 测试）确实不进 `rulecard_pack_sha256` 哈希载荷
（该 sha ＝ `canonical_hash(rule_cards.json 文档)`，`applicability_v3.rulecard_content_digests`）。
⇒ 不刷指纹、不动重核准记录，**结论正确**。

### 🔴 必须修 1：重核准记录 §2.1（`:41`）的前提已假、未加注

```
重核准记录_29五卡_20260805.md:41
  换成 `{… actor_role_key: bd}`，**组合数 1→1（替换，不是新增）**。
```

甲案裁定后世界侧是 **1→2**。该句现在是**假的**。
official 甲案复核 `:201` 已经点名「重核准记录 §2.1（`:39-45`）论证『授权的宽度不变』
时**第三次**以它为前提」，实施仍未加注。

**结论本身不受影响**（授权仍是精确一格，因为四行都带了角色轴——见上一节），
但按本项目自己的纪律「推翻旧结论时明确标注被推翻的是哪一条，别让两个矛盾结论并存」，
必须补一条作废注记。**成本 ＝ 加三行注记，不改 sha、不动指纹链。**

---

## 审核点③　世界侧甲案 —— **过**

### ba 格逐字还原：**字节层亲验通过**

`registry.py` 的 **#29 全量 delta 只有一处 hunk**（术前 vs 现在完整 diff）。
在这个 diff 里，

```
{"artifact_key": "record.inspection_log", "actor_role_key": "ba"},
```

是**上下文行**（前缀空格，非 `+`/`-`）—— 即这一行**自始至终没有被改过**，
不是「改走又改回来」。⇒ 「ba 格逐字保持 #29 之前的原样」在最强意义上成立。

`combo_key = "|".join(f"{k}={v}" for k, v in sorted(combo.items()))`
（`sidecar.py:634`）由 **sorted** 生成，与字典书写顺序无关 ⇒ combo_key 逐字相同。

### 追加 bd 格在 RNG 层是**纯追加**（结构证明，比记录自陈的更强）

- `sidecar.py:632-640`：每个 combo 走
  `rng_domains.sub_rng(SIDECAR_AXIS_COMBO, building_world_id, slot_id, combo_key)`
  —— **每格一条独立键控子流**；
- `sidecar.py:891-905`：`_marginal_sample(value_type, prevalence, slot_record, rng)`
  **只消费传入的 rng**，不碰任何共享流。

⇒ ①ba 格键不变 ⇒ 种子不变 ⇒ **值不变**；②新增格不消费共享流 ⇒ **不位移任何其它抽样**。

📌 实施记录 §6.2③ 把「`ba` 行同键 ⇒ 值不变」列为「须全池配对实测坐实，**未做，记尾巴**」。
**该结论在当前代码上结构性成立**，全池配对是补一道独立实证，不是唯一依据。
（唯一残余前提：无人依赖 `sidecar.entries` 的长度/顺序，且 `deterministic_key`
已与 sidecar 采样解绑——`validation.py:360-380` 的 1a-i′ 注释确认。）

🔴 **而且这条性质早就有一条机器断言在守，实施记录把它低估了**：
`worldgen/tests/test_rng_isolation.py:343` 的
`test_t6_axis_combo_addition_does_not_shift_existing_combos`
docstring 逐字写着「轴积槽：给某个槽的轴积**加一个组合** ⇒ 既有组合的值不动……
这条直接对着波次二在册的 #29」，实现是把新 combo **插到最前**（`:359` 注释
「最大化顺序耦合的暴露面」）再比既有组合值。
⇒ **甲案的核心主张已被一条既存变异式测试覆盖**，不是只有结构推理。
实施记录 §3.5 只把它记成「射程外 ⇒ 确实不必改」，**说小了**。

### bd 格形状 / notes / R2 保护名单

- bd 格：`{"artifact_key": "record.inspection_log", "actor_role_key": "bd"}`，追加在 ba 之后。
- notes 已换成实测分账（10 张卡 ＝ bd 4 ＋ ba 6），被证伪的旧句以
  「⚠️ 此处**曾**写过…**该前提已实测证伪并作废**」形式保留在注释里——
  符合「防旧结论复活」纪律。
- **R2 保护名单 13 格仍全 ba**：`test_axis_product_shape_is_locked`
  与 `test_layer3_world_artifact_submitted_keeps_thirteen_ba` 双锁，
  且都断言 `record.inspection_log ∉ artifact_submitted` 的 artifact_key 集合。两条实跑绿。
- A 表 7 行保护名单（rows 109-112 / 119-121）由
  `test_layer3_out_of_scope_binding_rows_stay_ba` 钉成**集合相等**（不是包含），
  并额外断言这 7 行 `slot_id == reporting.artifact.submitted` ⇒ 射程内外分处两个槽。

---

## 审核点④　两表与派生链 —— **过**

### A 表 5 行 / 桶表 3 行 sha 与卡包一致：**全表复算，不是抽查**

用 `applicability_v3.rulecard_content_digests(repo_root)` 独立算出 469 张卡指纹，
再与两表逐行比：

```
A 表  BINDING_CONTRACTS        125 行，带 sha 的 125 行全部核过，失配 0
桶表  BUCKET_BINDING_CONTRACTS 207 行，带 sha 的 207 行全部核过，失配 0
```

目标行（按 `row` 字段实取）：

| 表 | row | 卡 | qualifier_axis |
|---|---|---|---|
| A | 39 | §3.4.2(A)(b) | `None` |
| A | 123 | §3.6.2(A)(d) | `actor_role_key=bd,artifact_key=record.inspection_log` |
| A | 124 | §3.3.2(A)(c) | 同上 |
| A | 125 | §3.5.2(A)(c) | 同上 |
| A | 126 | §3.7.1(d) | 同上 |
| 桶 | 194 | §3.3.2(A)(c) | slot `artifact.record.inspection_log`，`diagnostic_only` |
| 桶 | 196 | §3.4.2(A)(b) | 同上 |
| 桶 | 198 | §3.5.2(A)(c) | 同上 |

卡 123/126 在桶表**确实无行**（全表按 rule_card_id 扫过）。

### 「桶表 3 行不是 1 行」这个订正：**坐实，且是实施的真发现**

逐文件核过，底稿与两份复核**都只点了 row 196**：

- `底稿:547`「（仅当并入第五卡）row 196 sha 重算」、`底稿:599` 步 11 同；
- `复核_29五卡射程_official:150 / :170 / :180 / :337 / :345`
  逐字写「桶表恰 1 行（row 196），**无第三行**」；
- `复核_29五卡射程_glm:73 / :91 / :139 / :151 / :156` 同样只有 row 196。

⇒ 三份文档一致漏掉 194/198，实施订正正确。**这一处如果照抄复核，两行会进 STALE。**

### slot_index 三桶分裂

与术前 `slot_index.json` 逐字比对，**唯一差异**就是这三桶的重排：

- `{actor_role_key: ba, artifact_key: record.inspection_log}` 桶 —— 去掉 3 张卡
  （123/125/126），保留 §3.6.2(B)/§3.6.3(b)/§3.6.3(c)×2/§4.4.3；
- `{artifact_key: record.inspection_log}` **裸桶整条消失**（唯一成员是卡 124）；
- **新增** `{actor_role_key: bd, artifact_key: record.inspection_log}` 桶，含**全部 4 张**。

无任何其它槽被误改（工作树里 `s3_2_6_prepare_inspection_method_statement`
的两处删除属**件四批 1 的既存改动**，在术前基线里就有，不是 #29 引入）。

### 其余门禁：**全部亲跑**

| 检查 | 结果 |
|---|---|
| 发布门禁 `check_applicability_release.py` gen_seed_301 | **通过 12 / 失败 0** |
| 发布门禁 gen_seed_401 | **通过 12 / 失败 0** |
| `build_card_applicability_manifest.py --check` | 卡 **469** / 已授权 **82** / 强校验全部通过 |
| `collect_rulecard_bundle_violations` | **恰 3 条**，全部是既存空 `slot_role_map` 卡 |
| `family_crosswalk_v1.json` | `card_count` 合计 **469**、`"ba"` 字面量 **0** 处 |
| 两表 `STALE_ROWS` / `DISABLED_REASON` | `[]` / `None` |

---

## 审核点⑤　重放与断言 —— **过**

### `--both` 臂设计：**真测到「ba 格不动」**

`canary_ba_bd_recipient_alignment.py:157-168` 的 `both` 分支：
`deepcopy` 出 twin、只改 twin 的 `actor_role_key`、并给
`fact_id`/`carrier_id`/`source_node_id` 加 `::bd` 后缀后 **append**；
**原 ba 事实对象一个字节不改**。⇒ 若甲案的「ba 格不动」主张为假，这个臂会红。

### 亲跑结果

| 臂 | 命令 | 结果 |
|---|---|---|
| 甲案 | `replay_… --batch-root wave1_closing_seed401_20260804 --max-buildings 20 --both` | 射程内 **0** / 射程外 **0** ✅ |
| 对照 | 同上去掉 `--both`（＝替换路） | 射程内 0 / 射程外 **4**（`s3_6_2_b_a` 1 栋・`s3_6_2_b_b` 20 栋・`s3_6_3_c_public_danger` 7 栋・`s4_4_3` 6 栋）🔴 |

⇒ **「3 → 0」坐实，且对照臂在同一批同一栋数下仍报 4 ⇒ `--both` 没把重放测哑。**
（§3.3 报的 3 是 8 栋口径，本次 20 栋口径下替换路暴露出第 4 张 `s3_6_2_b_a`
——即栋数一放大漂移面还会变大，这与「断供会放大」的判断同向。）

⚠️ **一处必须跟着读的口径限制**（记录已标「冻结批口径」，此处点明为什么重要）：
甲案臂的「射程内 0」是**冻结批产物的结构性结果**——`mutate_world("both")` 追加的
bd 孪生行**复制了 ba 行的布尔值**，故射程四卡读到的值与术前相同。
真实换池时 bd 格是**独立伯努利**（prevalence 0.84），期望约 27% 的栋会翻值。
⇒ 「射程内 0」**不可**读作「真实换池后射程内也不变」。

### (卡, slot_ref) 粒度订正：**正确，且必要**

**亲验**：读 ba 格的引用恰 **6 个 (卡, slot_ref_id)**，其中 §4.4.3 那张卡的是
`…s4_4_3_arrange_urgent_action_or_report_ba_if_unable.c01.**sr05**`。
而该卡在 A 表**确实有** row 103 —— 挂在
`…c01.**sr01**` / `slot_id=scope.component.inspection_included` /
`policy=diagnostic_only` / `verdict_permission=none`。

⇒ 卡级断言必然假红；**official 复核 §2.2／§四 说的「零行」在卡级不成立，
实施的粒度订正是对的。**

护栏本身也核过，**不是空过**：

- 四个派生视图 `SCOPE_PRECISE_BINDINGS`（dict,125）/ `VALUE_CONSUMPTION_BINDINGS`
  （frozenset,23）/ `DIAGNOSTIC_ONLY_BINDINGS`（frozenset,102）/ `REJECTED_BINDINGS`（0）
  的键**实测就是 `(rule_card_id, slot_ref_id)` 元组** ⇒ `k in ba_refs` 有意义；
- `VALUE_CONSUMPTION_BINDINGS` 正是 A′ 门实际查的集合
  （`obligation_deriver.py:2898` 查 `VALUE_CONSUMPTION_AUTHORIZED_BINDINGS`，
  `test_binding_contract_registry.py:70` 把两者钉成相等）⇒ 闸对准的是真门；
- 变异对照 `test_mutation_ba_grid_authorization_turns_red` 先断言未变异时为空、
  再注入一行**同键**行、用**同一谓词**查得 `[9999]` ⇒ 非恒空恒绿。

### 加分：那条「更宽的卡级观察闸」堵住了第二个授权面（可能实施自己没意识到）

桶通道另有一套值消费授权 `C55_BUCKET_VC_AUTHORIZED_PAIRS`（4 对），
键形是 **(rule_card_id, artifact_key)** —— **与 A′ 的键形不同**，
`test_ba_grid_has_no_value_consumption_authorization` 的精确断言**扫不到它**。

我实取了这 4 对：它们各自解析到 A 表 **row 119 / 125 / 124 / 118**
（即桶通道的授权是**从 A 表 value_consumption 行派生的**，不是独立来源）。
⇒ 6 张 ba 卡没有 A 表 VC 行 ⇒ 结构上也进不了桶 VC 集合；
而只加 pair 不加 A 表行会落 `C55_BUCKET_VC_REJECTED` → `blocked`，**fail-closed**。
测试里那条**卡级** `vc == []` 断言恰好把这条路一起封了。

🟡 **应修（防御纵深）**：把 `C55_BUCKET_VC_AUTHORIZED_PAIRS` 也纳入零行断言，
并给那条卡级宽闸补一个变异对照（目前只有精确闸有对照）。

### 🟡 应修：重放「before」臂的卡侧来源是偶然属性，不是不变量

`replay_…py:63` 的 before 臂用 `card_role=None`，卡侧**实际来自冻结批的
`rule_slice.json`**（脚本 `run_cell` 同时改 doc 与 slice，而义务派生吃 slice）。
我实取了该批切片：四卡限定符确为 `ba` / 缺键、`recipients` 全 `ba`
⇒ 当前 before 臂**确实是术前态**，结论有效。

但这只因为该批生成于 #29 之前。若日后拿 #29 之后的批跑同一脚本，
before 臂会**静默**变成「卡 bd / 世界 ba」，比较含义完全改变而脚本不报错。
建议加一条前置断言：切片里射程四卡的 `actor_role_key` 必须是 `ba`／缺键，否则拒跑。

---

## 审核点⑥　金丝雀与判定影响 —— **过**

### 「12 栋只 1 栋构成判据」：**亲验属实**

在 `wave1_closing_seed401_20260804` 前 12 栋逐栋数射程四卡的 `satisfaction_status`：

```
BLD-HK-COASTAL-COMPOSITE-TOWER-RC-0010   {unknown:10, satisfied:12}   ← 唯一构成判据
其余 11 栋                                {not_applicable: 8} × 11
```

⇒ 记录与重核准记录写的诚实边界准确。这是**严重的样本代表性限制**，
但已被显式标注，且金丝雀加了前置闸（`canary_…py:355-362`：不构成判据返回 **2**，
不是 1）——符合「测试跑在缺陷不可能显现的输入上」这条既有教训的处置方式。

### 第五卡四格恒等 / 判定影响恒等 0：**成立**

结构侧亲验：第五卡的差异只有 `recipient_key` ＋ `interpretation_notes`，
**不带 `reporting.record.submitted` 的角色轴**（其 A 表 row 39 `qualifier_axis=None`、
`policy=diagnostic_only`、`verdict_permission=none`；桶表 row 196 同）。
⇒ 四格恒等是结构必然，不是巧合。

### 「10 栋 55 条 ba 格义务全 unknown、satisfied 0」：**独立复算命中**

我不用记录的口径转述，自己在冻结批上按「义务的 `evidence_fact_ids` 与该栋
`{artifact_key=record.inspection_log, actor_role_key=ba}` 事实 id 有交集」重算：

| 样本 | ba 格消费卡（6 张）的义务 | satisfaction | 原因码 |
|---|---|---|---|
| 前 **10** 个 fact_pack | **55** | **unknown 55 / satisfied 0 / violated 0** | `artifact_state_not_valid_evidence` 55/55 |
| 全 **30** 栋 | **168** | **unknown 168 / satisfied 0 / violated 0** | 同上 168/168 |

⇒ 实施记录的 55（10 栋）与 official 复核的 168（30 栋）**两个数都对**，
且确实是「口径同、样本不同」。**#33「只进分母不进分子」的实测面成立。**

（另附：同一口径下射程四卡在术前有 12 条 satisfied ＋ 8 条 `ambiguous_fact_binding`。
它们随 #29 迁到 bd 格，不属 ba 格记账——拆账时别混。）

---

## 审核点⑦　记录与作废注记完备性 —— **过，含必须修 1 ＋ 应修 2**

### 底稿四处作废注记：**齐备**

| # | 位置 | 形态 |
|---|---|---|
| 1 | `底稿:299-327` §4.2 | 标题划删线 ＋ 作废依据 ＋ 错在哪 ＋ `<details>` 原文保留 |
| 2 | `底稿:394-409` §4.3/4.4 | **「+14 行／+7.53%」高危错引点**——明写「本件从来没走、现在也不走」「拿它反对甲案是错引」，并保留仍成立的那半句 |
| 3 | `底稿:659` §4.5 | 数字作废 ＋ #33 记账表（分母 +1 / 三项 +0） |
| 4 | `底稿:783-800` §9.2 | 「该槽行数」行作废 ＋ 行级细尺三条 |

另 `底稿:801-808` 有一条**纠名**（不是作废）：指出「射程外零漂移」是**实施期自设**
的标准，底稿全文「零漂移」零命中。这条纠名是对的，且比实施记录 §3.3 的说法更准。

🟡 **应修 4**：实施记录 §3.3 仍逐字写「『零漂移』是底稿 §9.2 **写死的硬判据**」。
§三 虽标了「原样保留作沿革，读到那里请续读 §六」，但**没有指向那条纠名**，
读者会把一条自设标准误记成决策门批准过的判据。建议在 §3.3 加一行指针。

### 实施记录 §六 的判据更新表述 / 「行数 +1」新口径 / 防漂移文字：**都在**

- §6.2 判据更新：「行数 +1；新增 bd 格是独立采样；#33 只进分母不进分子——
  契约行／satisfied／卡数三个计数 +0（结构证明 ＋ 冻结批实测双证）」，
  并附行级细尺三条与③ 的未做声明。表述完整、边界诚实。
- §四 防漂移条款（#29 本体）＋ §6.4 防漂移条款（甲案，含「是**还原**不是修复」）
  两段都在，且 §6.4 明确写了那 4 卡将来仍要动。

### 🔴 必须修 2：`test_c55_consumption_rows.py:78` 的「组合数 1→1」没改

```
test_c55_consumption_rows.py:78
  #    同批由 `{…, actor_role_key: ba}` 换成 `{…, actor_role_key: bd}`（组合数 1→1）。
```

这条**正是 official 甲案复核 `:302` 逐字列出的改动项第 3 条**
（「注释里『世界侧…组合数 1→1』一句改写」），实施记录 §6.1 的五项改动面**漏了它**，
「显式不动」清单里也没有它。

危害不在运行时（断言本体四行全 bd，是对的），而在于：**这段注释本身就是为
「防旧结论复活」写的**——它现在自己成了那条被推翻结论的最后一个未加注副本。

### 🔴 同一句话还有第三份未加注副本：`replay_ba_bd_out_of_scope_drift.py:14`

```
replay_ba_bd_out_of_scope_drift.py:14（模块 docstring）
  世界侧 `reporting.record.submitted` 的角色取值是**整槽换掉**的（组合 1→1 替换）。
  凡是引用这个槽、且限定符写 `actor_role_key=ba` 的卡，**都会在换完之后取不到事实**
```

甲案之后这段**描述的正是被推翻的那条路**（而甲案就是为了让这些卡**不**取不到事实）。
脚本加了 `--both` 臂和形态打印，**但没回头改这段 docstring**。
「为什么必须逐卡比」这个结论仍然成立，失效的是它给出的机制。

### 全仓清点

「1→1 替换」的**未加注**副本现存 **3 处**：

| 位置 | 类型 |
|---|---|
| `重核准记录_29五卡_20260805.md:41` | 权威记录的论证前提 |
| `closure/tests/test_c55_consumption_rows.py:78` | 代码注释（复核已逐字点名要改） |
| `scripts/replay_ba_bd_out_of_scope_drift.py:14` | 验收脚本 docstring |

**已正确加注的**：底稿 §4.2/§4.3/§4.5/§9.2 四处、实施记录 §3.3（指向 §六）、
`test_ba_bd_recipient_alignment.py:122`（「🔴 沿革（别让旧结论复活）…该前提已实测证伪」
——这条是范本，另三处照它写即可）。

### 🟡 应修 5：`build_rulecard_zh_sidecar.py` 的订正注释说了没做的事

```
build_rulecard_zh_sidecar.py:12-13
  （🔴 行号 2026-08-05 现取订正：此前写的 89 / 157 是早已漂移的旧值，
    CLAUDE.md 与本文件两处都记着那对旧行号，一并订正。）
```

**订正后的行号我逐行核过，全对**：
`component_lattice.py:138` ＝ `return None  # stale_card_binding`；
`applicability_v3.py:356` ＝ `stale.append(cid)`；
`applicability_v3.py:193` ＝ `stale_rows += 1`。

但 **`CLAUDE.md:78` 仍写着 `component_lattice.py:89` / `applicability_v3.py:157`**
——「一并订正」没做。要么补改 CLAUDE.md，要么把这句话改成「CLAUDE.md 待同步」。

### 🟡 观察（不列必须修）

`项目跟踪表.md` / `技术与研究债.md` 均**无 #29 落地条目**
（件四批 1 有整行记录）。本件明示「未提交」，可在提交时一并补；
但按「跟踪表随手更新」纪律，现在是欠着的。

---

## 附加核点　与在途资产零冲突 —— **过**

### 期限锚案资产：**零触碰（机器证明，不是声明转述）**

上文的「14 个文件」封闭性结果直接给出结论：#29 的改动面**不含**
`obligation_deriver.py` / `validator.py` / `fact_binding.py` / `blueprint_deriver.py` /
`worldgen/sidecar.py` / `generator.py` / `generator_sampling.py`，
也不含 `test_deadline_anchor_binding.py` / `test_deadline_anchor_supply.py` /
`test_deadline_anchor_emission.py`。

`registry.py` 只有**一处 hunk**（就是 `reporting.record.submitted` 那一段）
⇒ 8 条 duration 条目与 `duration.delivery.deadline.to_ba` 的
`recipient_qualifier.actor_role_key=ba` **字节未动**
（另有 `test_layer3_deadline_anchor_recipient_stays_ba` 把它钉住，实跑绿）。

### 1a/1b 资产：**零触碰**

未跟踪侧与 `杂物箱/备份_29_untracked/` 逐文件比：**只有 `test_c55_consumption_rows.py`
一个文件有差异**。`rng_domains.py` / `test_rng_isolation.py` / `test_fsp_fragment_scope.py`
逐字节相同。

另核一处潜在的静默耦合：`test_rng_isolation.py:351` 用
`next(i for i, r in enumerate(records) if len(r["qualifier_axis_product"]) > 1)`
取「第一个多格轴积槽」。该槽新增第二格后**理论上可能改变这个取值**，
但 `reporting.artifact.submitted`（13 格）在注册表里排在
`reporting.record.submitted` 之前 ⇒ 取值不变，**未被静默改掉**。

### `registry_bundle_hash`：**三个值全部复算命中**

用 `validation.py:302-308` 的原式
（`_hash_payload({version, source_documents, registries=model_dump(mode="json")})`）
在三份 registry.py 上分别算：

| 形态 | registry.py 来源 | 实算 `registry_bundle_hash` | 记录声明 |
|---|---|---|---|
| 术前（#29 之前） | HEAD ＋ 备份 patch 重建 | `d6e0eb01e1d7aa8aa047842c626c9d24c02ec20846ad010f78345bf8d50c34ff` | `d6e0eb01…` ✔ |
| 替换路（#29 首版） | `杂物箱/备份_29甲案_registry_增量前.py` | `ced4d247b4f8e25020fcbe18fc0d2d992ef36595dd5e8dd13adc71eb7427630a` | `ced4d247…` ✔ |
| 甲案（现状） | 工作树 | `77f7ce747d9424211b9c078ee1d2e284e85d92e44d4b67395a3acd434be95815` | `77f7ce74…` ✔ |

⇒ 变化确由 registry.py 那唯一一处 hunk 引起，属**换池级的正当结果**。
「两案同批换池」是排程声明，本次无法核（也不该由审核门核）。

---

## 必须修清单

| # | 位置 | 内容 | 成本 |
|---|---|---|---|
| **1** | `团队文档/我的笔记/重核准记录_29五卡_20260805.md:41` | 「组合数 1→1（替换，不是新增）」已被甲案推翻，须加作废注记（结论不变，前提变了）。official 甲案复核 `:201` 已点名 | 三行注记 |
| **2** | `closure/tests/test_c55_consumption_rows.py:78` | 同一句「组合数 1→1」。这是 official 甲案复核 `:302` 逐字列出的改动项第 3 条，实施漏做 | 改一句注释 |
| **3** | `scripts/replay_ba_bd_out_of_scope_drift.py:14` | 模块 docstring 仍按「整槽换掉／组合 1→1 替换」描述机制；甲案后该机制正是被推翻的那条。脚本加了 `--both` 但没回头改 docstring | 改一段注释 |

> 三条是**同一件事的三份未加注副本**。范本已有：
> `test_ba_bd_recipient_alignment.py:122` 的「🔴 沿革（别让旧结论复活）」写法。
> 全部改完后，「1→1 替换」在仓库里就只剩带作废注记的版本。

## 应修清单（不阻断）

| # | 位置 | 内容 |
|---|---|---|
| 4 | `实施记录…§3.3` | 「零漂移是底稿 §9.2 写死的硬判据」——底稿 `:801-808` 已纠名为**实施期自设标准**；加一行指针 |
| 5 | `build_rulecard_zh_sidecar.py:12-13` ／ `CLAUDE.md:78` | 注释声称「CLAUDE.md 一并订正」但未做；CLAUDE.md 仍是 89/157（订正后的 138/356/193 我逐行核过，是对的）|
| 6 | `test_ba_bd_recipient_alignment.py` | ①把 `C55_BUCKET_VC_AUTHORIZED_PAIRS`（第二个授权面，键形是 (卡, artifact_key)，精确闸扫不到）纳入零行断言；②给卡级宽闸补变异对照 |
| 7 | `replay_ba_bd_out_of_scope_drift.py` | before 臂的「卡侧＝术前态」是冻结批的偶然属性；加前置断言，防日后拿 #29 之后的批跑而静默变义 |
| 8 | `重核准记录 §2.2` | 补一句：甲案下 row 124 补键**不只是收窄，是必要条件**（A′ 要求 `len(bound)==1`，该槽现供两行） |
| 9 | `重核准记录 §2.3` | 未披露 123/125/126 的 `normalized_rule_text` 也进了新指纹（实施记录已自陈，记录本身不完整） |
| 10 | 跟踪表 / 技术与研究债 | 无 #29 落地条目（件四批 1 有整行记录）；本件明示未提交，可提交时一并补 |

---

## 附：本次审核实际跑过的东西（可复现清单）

| # | 动作 | 结论 |
|---|---|---|
| 1 | `HEAD + 备份 patch` 重建术前工作树（48 文件，`git apply` 干净） | #29 真实改动面 ＝ **14 个文件**，零未申报 |
| 2 | 469 张卡逐卡 canonical 比对 | 恰 5 张有差异；`source_quote`/`source_section` 零差异 |
| 3 | `applicability_v3.rulecard_content_digests` 复算 469 指纹，对两表全表核 sha | A 125/125、桶 207/207，失配 **0** |
| 4 | `binding_contract_registry.registry_digest()` / `bucket_binding_registry.registry_digest()` | 与记录逐字相同；`STALE_ROWS=[]`、`DISABLED_REASON=None` |
| 5 | `check_applicability_release.py` × 2 池 | 各 **12/0** |
| 6 | `build_card_applicability_manifest.py --check` | 469 / 82 / 强校验全过 |
| 7 | `collect_rulecard_bundle_violations` | 恰 3（既存） |
| 8 | `replay_ba_bd_out_of_scope_drift.py --both`（20 栋） | 射程外 **0** |
| 9 | 同脚本对照臂（20 栋） | 射程外 **4**（对照有敏感度） |
| 10 | 冻结批 ba 格义务复算（10 栋 / 30 栋两口径） | **55/55**、**168/168** 全 unknown、satisfied 0 |
| 11 | 冻结批前 12 栋射程卡适用性 | **1 栋**构成判据、11 栋全 `not_applicable` |
| 12 | `validation._hash_payload` 在三份 registry.py 上复算 | 术前 / 替换路 / 甲案三个 `registry_bundle_hash` **全部命中** |
| 13 | 定向 pytest（6 个文件） | **83 passed** |
| 14 | 全量 pytest `agent_v1` | **3458 passed / 1 skipped / 11 xfailed / 0 failed** |

### 全量 pytest（本次亲跑，非转引）

```
$ PYTHONPATH=agent_v1/src python -m pytest agent_v1 -q -p no:cacheprovider
3458 passed, 1 skipped, 11 xfailed, 4 warnings in 810.01s (0:13:30)
```

**与实施记录 §6.5 声明的 `3458 passed / 1 skipped / 11 xfailed / 0 failed` 逐项相同**
（耗时 810s vs 记录 773s，本机并发跑了两个重放，属机器负载差异）。

另核：新建的 `test_ba_bd_recipient_alignment.py` **collect 到 23 条**，
三个新建/改动测试文件里 **`skip` / `xfail` 标记零处** —— 没有用跳过掩盖失败。

**边界**：Neo4j 相关测试是设计性跳过
（`tests/test_neo4j_client.py:86` 无条件 `pytest.skip`，另有 `neo4j_required` 标记），
两个提到 ollama 的测试全部 monkeypatch／只比配置字符串 ⇒
**本次全量 pytest 未连 Neo4j、未发起任何 LLM 调用**，符合工单边界。

---

*本审核只读；除本文件外未改动仓库任何字节。未连 Neo4j。*
