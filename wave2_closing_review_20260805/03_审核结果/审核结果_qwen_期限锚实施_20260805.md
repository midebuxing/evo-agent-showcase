# 审核结果（qwen 线）：期限锚案实施（2026-08-05）

**工单**：`审核工单_期限锚实施_20260805.md`。**独立审核**，未读其他线结果。
**方法**：不转述实施记录当证据——自建实施前基线（HEAD `8c386907` worktree＋`杂物箱/备份_期限锚_20260805.patch`，
基线缺的未跟踪文件 `rng_domains.py` 以现树版补齐；该文件的期限锚增量仅一条域串
`SIDECAR_DEADLINE_ANCHOR`，基线侧不消费它，补齐无害），与现树做**换行规范化全树 diff**
隔离出本单精确改动面；再亲跑红先行探针、50 栋配对池重建、B6 发射探针、
30 栋层三闭包纯重放（不连库）、期限锚全部测试与全量测试。验证产物全落 scratchpad，已清理。

**总判**：**七点全过，一处如实标注的口径差异（层一绝对行数），一处与本单无关的全量测试顺序敏感失败。**
乙12 表态见末节：**甲，挪换池批落，现状诚实歧义码作过渡**；另附一条决策输入（`.to_ba` 同形问题）。

---

## 核点 1：R1 修复面——过

- **改动面全树核净**：基线↔现树规范化 diff，非测试源码**只有四个文件**有实质差异：
  `closure/obligation_deriver.py`（163 行）、`closure/fact_binding.py`（17 行）、
  `worldgen/registry.py`（328 行）、`worldgen/sidecar.py`（164 行）。
  `validator.py`、`threshold_eval.py` 等**零实质差异**（形态 C 零改动路径成立）；
  `_canon_truthy` 本体（`obligation_deriver.py:423-434`）在 diff 中零触碰。
- **只动 same_day_as 分支**：diff 显示旧 `_canon_truthy(observed)` 调用从 same_day_as
  分支移除，换成 `isinstance(observed,(int,float)) and not isinstance(observed,bool)`
  的 `float(observed)==0.0`（`obligation_deriver.py:2185-2221`）；数值输入落 notes
  「same_day_as ⇒ elapsed duration == 0 day; threshold source = deadline relation
  structural constant」（判据来源显式，不静默）；非数值落 open/unknown。
  `_canon_truthy` 的 5 个残留消费点（`obligation_deriver.py:1559/1975/2394/3102/3260`）
  全在别的 kind 分支，逐一在 diff 外。
- **浮点/整数同形**：亲跑探针——`0` 与 `0.0` 同判 satisfied，`1`/`1.0`/`2`/`3.0` 同判 violated。
- **红先行真实**：新测试文件模块级 import `FORBIDDEN_DEADLINE_ANCHOR_SUPPLY`，
  实施前树上连 import 都不成立，故「新文件跑红」只能在常量先落的前提下成立；
  我改用更强的直接证据——把探针打到实施前基线代码亲跑：
  `0/0.0→violated、1/1.0→satisfied、2/3.0→open`（语义逐条反转，即 HEAD 曾红），
  现树探针翻成 `0/0.0→satisfied、非零→violated、布尔→open`。
  B1/B2 所断言的语义在 HEAD 上确红，成立。

## 核点 2：禁供钉死——过

- `FORBIDDEN_DEADLINE_ANCHOR_SUPPLY`（`obligation_deriver.py:2064` 一带）成员恰三条：
  `appointment.representative.supervision.made`／`investigation.detailed.commencement`／
  `repair.prescribed.started`，与工单/决议逐字一致；三锚经我独立核对全部 ∈ 锚点册 19 条
  （锚点册条数亲数=19，卡侧 15 锚 ⊆ 册）。
- **#13 措辞在**：常量注释逐条附守则原文与理由，#13 写「本单禁供＋供路待『提前量』语义裁定」，
  并注明 `before` 分支方向相反的具体后果（提前 2 日判 satisfied、提前 10 日判 violated）。
- **双向断言真双向**：判定侧 `test_deadline_anchor_supply.py` B4——误供带锚事实也恒
  unknown（关系 before/within 参数化）＋反向对照（非丙锚同形状必须能判，防通道整体失效的廉价绿）；
  世界侧 `test_deadline_anchor_emission.py::test_c4_forbidden_anchors_are_not_declared_world_side`
  与 B4 的 `test_b4_world_side_never_declares_a_forbidden_anchor`——扫的是**注册表声明**
  （生产面），与判定侧的**消费面**不是同一面。两侧独立，成立。

## 核点 3：绑定通道——过

- **级 0 优先级**：`_bind_deadline_fact` 先查 `fact_index.time_anchor_index`
  （`fact_binding.py` 新增，按 `provenance.time_anchor_key` 建、不过别名归一、不做载体过滤，
  注释说明两条"不做"的理由），命中即用，miss 才落槽/量表老通道；`FactIndex` 构造 diff 仅 +17 行。
- **碰撞三态**：0 条→落老通道；恰 1 条→用；>1 条→返回 `(None,"ambiguous")`，
  调用点落 `blocked/ambiguous_fact_binding`（`obligation_deriver.py:2122-2135`）。
  新通道无 `facts[0]`。
- **「>1→报错」落成 blocked 而非抛异常**：这是与决议 §三.2 字面的一处**已披露偏离**
  （实施记录 §五.2）。判其可接受：决议的实质是「拒绝任取、不得产出确定判定」，
  blocked＋既有心码达到该实质且不炸整批；消费者文案四处逐字适配未新增码。
- **`(fact,status)` 二元组无行为漂移**：生产调用点全仓唯一（`obligation_deriver.py:2122`），
  其余全是测试；`test_deadline_anchor_binding.py` 的 `_bind` 辅助断言 `status is None`，
  把「既有用例意外触发碰撞」变成当场炸。81 条期限锚测试全绿（亲跑）。
- **老通道保留 `facts[0]`**：属实（diff 中 4 处 `return facts[0], None` 等），
  与「层三零扰动」自洽，且实施记录 §五.3 已明示边界；丙类禁供闸只堵新通道亦已披露。
  记为已知边界，不判不过。

## 核点 4：世界侧接线——过

- **8 条注册表条目形状照真条目对齐**：与既有 duration 条目逐字段同形
  （`measurement_family`/`physical_bounds`/`precision_steps`/`carrier_domain`/`carrier_slot`/
  `rule_basis_refs`/`rule_card_threshold`/`cop_section`/`semantic_note`/分布四件）；
  全部 `granularity:"building"`、不打 `aggregation`（形态 C）；
  `distribution_source` 全标 `PLACEHOLDER_pending_distribution_authorization_20260805`。
  `rule_card_threshold` 与**卡侧权威逐条对过**（亲查 `rule_cards.json`）：
  甲1-4/7 `within <=7 day` 对应 §2.1.3(i)(j)(k)(l)(p) 卡、甲9 §6.4.4、甲10 §6.4.6，
  乙11 `same_day_as ==0 day` 且注明「卡侧 (p)/(q) threshold_regimes 为空、判据来自 relation
  结构常量」——(p)/(q) 两卡实测 regimes 真空，(r) 两卡 regimes 实测 `{operator:"==",value:0,unit:"day"}`，吻合。
  8 个 `carrier_slot` 全在布尔槽登记表（`registry.py:4263-4264` 一带可查）；
  `_build_sidecar_contract` ownership_map +8（diff 证实）。
- **R5 对齐**：`.to_person` 条目锚名去 `_and_mbi4`、unit `calendar_day→day`
  （`registry.py:2855-2856`）。卡侧对应真锚点即 `repair.completion_report.submitted_to_ba`
  （∈19 册），unit 卡侧写 `day`——改的是错的一方，正当。
- **回填只加一列**：注册表中既有槽只有 `.to_ba`（锚 `repair.prescribed.completed`）与
  `.to_person` 两条带 `time_anchor_key`，`_registry_time_anchor_key` 只读登记；
  弃用槽无 threshold ⇒ 恒 None 不回填。配对细尺亲跑证明：既有行非锚点列零变化、
  回填全部落白名单两槽（细尺 `verify_rng_isolation_pairing.py:576-584` 真有白名单判据，越界即 FAIL）。
- **楼级发射独立追加、seq_no 零漂移**：调用点在 `_emit_scope_declaration_rows` 之后、
  桶落成记录之前（`sidecar.py:1517-1524`），是最后一个写桶步骤；
  **亲跑配对池**得 `sidecar.records 逐字节相同`＋「既有行全部保留」——`fact_id`/`seq_no`
  零位移是直接实证，不靠读码。
- **槽名零冲突、零 procedure.\* 注入**：8 个新槽名对 `projection_runtime_mapping_v1.json`
  slot/measure 别名的键与值逐一无碰撞、无 `procedure.` 前缀（亲跑核对）。
  s2_1_3_r 触发器回归由层三全量重放覆盖（见核点 5：含触发器在内的全义务集逐条一致）。

## 核点 5：验收三层——过（层一绝对行数有口径差异，如实标注）

实施记录称验证池已删，我**全部重建亲跑**：

- **层一**：基线树（HEAD＋备份 patch）与现树各生成 50 栋 seed301 ctcov=on 池，
  跑 `verify_rng_isolation_pairing.py --step deadline`：**PASS**——20 个须相同单元逐字节一致；
  `world.meta` 仅 `registry_bundle_hash`/`deterministic_key` 变（注册表 +8 条的正当结果）；
  新行=400=8 槽×50 栋；回填 694 行全在白名单；J1 保留键率 1.0000、翻判 0、
  pass_bool 差异 0。
  ⚠️ **口径差异**：我的池基线 19,412 行、回填 694；实施记录写 12,650/400。
  绝对行数与其池形状有关（池已删，无法对出其数），但结构性判据（全保留/白名单/零漂移/+400）
  在独立池上逐条复现。判过，数字差异记录在案。
- **层二**：期限锚四文件 81 条测试亲跑全绿；B6 另用独立探针亲跑两栋楼：
  每栋恰 8 行、逐（楼,锚）恰 1、零 aggregation、零 fragment_id、granularity=building、
  `source_refs=[world_id]`、同栋确定异栋有差。B1-B5 断言非空（参数化值域、双向、反向对照俱在）。
- **层三**：不连库纯重放——用冻结批 `wave1_closing_seed401_20260804` 30 栋的
  `rule_slice.json`+`fact_pack.json`＋frozen applicability bundle（sha 与批清单一致）＋
  identity run catalog，以**现树代码**重跑 `validate_building_closure`
  （三开关与批一致），对冻结 `closure_validation_result.json` 逐条比
  （kind/双状态/双原因码/evidence_fact_ids/锚键）：**30/30 楼全等**；
  义务总数合计 **272,122**（0 增 0 减 0 翻）；deadline 义务合计 **603** 全同。
  触发器义务在全集比对内 ⇒ s2_1_3_r 回归（无数值进 `procedure.*` 布尔闸）亦覆盖。
- **全量测试**：亲跑 workflow_engine＋evo_agent_baseline＋agent_v1/tests：
  **3,379 过／1 失败／1 skipped／11 xfailed**（738 秒）。
  失败 `test_check_batch_acceptance.py::test_acceptance_artifacts_write_json_and_replace_block_idempotently`
  **单跑与整文件重跑均绿** ⇒ 顺序敏感 flake，内容为批验收工件幂等、与期限锚改动面零交集，
  判与本单无关（诚实边界：未证它在实施前树同样 flake）。
  实施记录报「0 失败/646 秒」，本次为 1 flake/738 秒，差异即此。

## 核点 6：三处旧断言改红——过（逐处核为「修断言」非「绕断言」）

1. `test_derivation.py::test_deadline_same_day_as_truthy→zero_day`：原用例喂布尔 True 断 satisfied，
   锁的正是语义反转本身；规范依据＝(r) 两卡 regimes 实测 `==0 day`（亲查卡包），
   新用例覆盖 0/0.0→satisfied、1.0→violated，覆盖扩大不缩小。修断言，正当。
2. `test_registry.py::test_to_person_slot_has_cop_confirmed_threshold`：原锁
   `unit="calendar_day"`＋册外孤儿锚名；卡侧权威（在册锚名＋unit `day`）是真的一方。
   修断言，正当（卡侧值我独立核过）。
3. `test_registry.py::test_sidecar_measurement_registry_present`：条数 11→19 与槽名集合 +8，
   与本单新增条目一一对应，纯记账更新。正当。

## 核点 7：位移量化口径——过

- **各锚义务数我独立数过**（直接扫冻结产物 30 栋 `closure_validation_result.json`）：
  23/2/30/30/14/93/30/30/60/60/12/60/30/66/63，与位移表逐格吻合，合计 603。
- 表内算术自洽：甲 252＋乙11 60＋乙12 60＝372 脱离；确定判定 312＝252＋60；
  372−312＝60 落 `ambiguous_fact_binding`（乙12）；丙 159、挂起 72 恒等。
- **「占位分布不可引」标注在**：实施记录 §三两条 ⚠️（satisfied/violated 拆分无依据、
  只能引结构性数字且为上界）＋草案首段「任何批级数字不得引用」。
- **乙11 记账在**：注册表 #11 条目注释（一条量承载两收件人、拆槽属规则扩展另案）
  ＋草案 Q3（两支永远同判、真实偏差建不出来）。
- **「8 条不是第 9 条」标签订正在**：实施记录 §五.1（甲 8 计数含 #6/#5 挂起与 #8 复用，
  实为甲 7＋#11＝8），注册表块注释「不在这里的锚点」清单同记。

---

## 乙12 表态：**甲**（`.to_person` 改楼级粒度），挪换池批落；现状诚实歧义码作过渡

逐答工单三问：

- **① 甲的换池级影响可接受**。批级数字验收本已挂换池批（决议 §一.6）——换池绕不开；
  `.to_person` 粒度改动正属"换池批才许动既有槽行数"的那类改动，此时动它有合法窗口、
  有配对尺看着（届时它是 allowed-change，其余单元仍逐字节锚）。语义上它也更正确：
  送交完工报告是楼级一次性行政事件，spec 草案 §3.1 点名的就是这类；碎片粒度是历史错位。
- **② 乙的双槽漂移钉不住**。乙＝另起楼级槽承载同一真实量：`.to_person` 要么仍带回填锚点
  （则 60 条照样歧义，新槽旧槽并存的判谁？还得再立一条"谁是权威"的规矩），
  要么撤锚点（则回填白做、`.to_person` 沦为死数据）。两种形态都要为同一量补第二份分布授权，
  漂移面从 1 槽变 2 槽。乙严格劣。
- **③ 现状可直接作过渡进换池批**。60/60 落 `ambiguous_fact_binding` 是诚实且**更精确**的码
  （锚点确实供上了、不准的是粒度），零捏造判定；消费者文案现成。换池批本来就要重立池锚、
  重走分布授权定稿，把甲挪进去一并落，池级改动只发生一次。

**另附一条决策输入（本审核新观察到，供换池批一并裁）**：`.to_ba` 与 `.to_person` 同形——
同为 §2.1.3(r) 楼级行政事件、同为碎片粒度采样。#8 的 93 条是碎片作用域，每碎片各绑本碎片行，
而逐碎片行是独立采样的 ⇒ **同一栋楼同一个"送交 BA"事件，机制上允许跨碎片判出不一致**
（位移表 93 条里 satisfied 70/violated 23 的混合与此相容）。这不是本单造成的（碎片采样是既有形状，
本单前这些义务全落 missing_time_anchor，无判定可言），但供给让它显形。
换池批裁 `.to_person` 粒度时，建议把 `.to_ba` 一起裁，别留半个同类错位。

---

## 尾巴（审核侧）

1. 层一绝对行数（12,650/400 vs 我复现的 19,412/694）池已删对不上，结构性判据复现——
   若日后要引绝对数，以带池留存的那次为准。
2. 全量测试那 1 条 flake 与期限锚无关，但它是批验收工件测试的顺序敏感项，台账可记一笔。
3. 「>1→报错」落 blocked 不抛异常＝已披露的合理偏离；若决策门认为须更烈，
   再议（现状不产出任何确定判定，实质已守住）。
4. 审核未触 Neo4j、未动仓库任何文件；scratchpad 池与基线 worktree 已清理。
