# 审核结果（官方线）：#25 真值落改实施（2026-08-05）

**性质**：审核门，只读。工单 `审核工单_25实施_20260805.md`。
**边界**：未连 Neo4j；未改仓库任何文件（本报告除外）；证据全部亲跑或钉到 `文件:行号`。
**#29 案材料（卡包/registry/c55）未混入**——射程核实见审核点 7。

---

## 总裁定

**七个审核点全部「过」。** 落改本体（29 行）、schema 编码、判据唯一入口、
端到端复现、A8 冻结、精裁材料标注、blind 与射程——逐条独立复算，
没有一条经不起复核。红先行探针经我在隔离目录用 HEAD 版评分器实跑，
**4 failed / 2 passed 五个症状逐位复现**，不是事后补的绿测试。

**但有 1 条须转决策门 ＋ 4 条必须修**（都不动那 29 行的值，也都不要求重裁方向）。
最重要的一条：**落改集的证据基础（`_reanchor` 池）与端到端验证批（`_fragcov2` 池）
是两个不同的池——同一个 `seed301` 标签、两个 `worldgen_run_dir`、内容不同——
而仓库里两边都在用，没有一处文档说过这件事。** 详见「发现一」。

在 `_reanchor` 池上，Q3 规则输出与落改后真值 **10/10 全对**、reason 里写的
`0/5`／`2/5`／`3/5` 逐位精确；在 `_fragcov2` 池上 **5/10 反号**。
落改本身没错，错的是**没人声明真值锚在哪个池**。

---

## 审核点 1：29 行落改正确性 —— **过**

### 1.1 diff 恰 29 行、hunk 白名单逐位

```
$ git diff --numstat -- .../applicable_normative_item_truth_v1.jsonl
29      29
$ git diff -U0 ... | grep -c '^@@'
13
```
13 个 hunk：`30,8 / 599,4 / 1118 / 1131,2 / 1396 / 1409,2 / 1674 / 1687,2 /
1952 / 1965,2 / 2230 / 2243,2 / 2521,2` ＝ 8+4+1+2+1+2+1+2+1+2+1+2+2 = **29** ✅

### 1.2 我不采信 diff，按 JSON 逐行独立复算

（脚本：把 `git show HEAD:` 版与工作树版按行解析后逐字段比对）

| 项 | 实测 | 判定 |
|---|---|---|
| 总行数 | 2770 → 2770 | ✅ A1 |
| 变更行集合 | 恰等于 29 行白名单，`missing=[]` `extra=[]` | ✅ A2 |
| 白名单外 2,741 行逐字节 | 字节不同 **0** 条 | ✅ A3 |
| 越界改键 | `[]`（只动 `applicable`/`reason`；`conditionality` 实际零改动） | ✅ A4 |
| 分布 | `{true:2327, false:443}` → `{true:2343, false:423, unknown_pending:4}` | ✅ A5 |
| sha256 | `86720974e83eecf6…` → `e12695727df73348…` | ✅ 与实施记录一致 |
| L2508 | 逐字节 identical | ✅ |

### 1.3 逐行新值对照决议 §一（29 行**全部**打印核对，非抽验 5 行）

**Q2（§3.1.1）20 行** ＝ `30-37 / 1131 / 1132 / 1409 / 1410 / 1687 / 1688 / 1965 / 1966 / 2243 / 2244 / 2521 / 2522`
- `false→true` **15** 行，且实施记录写的「伸出物 6 ＋ 招牌 9」我按 reason 主题复分账**逐位吻合**：
  - 伸出物 6：L30 / L32 / L1687 / L1965 / L2243 / L2521
  - 招牌 9：L34 / L36 / L37 / L1132 / L1410 / L1688 / L1966 / L2244 / L2522
- 仅改 reason（值本就 true）**5** 行：L31 / L33 / L35 / L1131 / L1409
- 20 行新 reason 全部统一到「§3.1.1 唯一前件＝樓宇被選定為目標樓宇；伸出物／招牌是被涵蓋範圍的**列举项**、不是前件」＋**（判据情形 1）** ✅

**Q3（§2.1.3(n)）9 行** ＝ `599 / 600 / 601 / 602 / 1118 / 1396 / 1674 / 1952 / 2230`

| 行 | 楼 | 旧值 → 新值 | 与决议 §一 |
|---|---|---|---|
| 599 | 0028 | `true` → `"unknown_pending"` | ✅ |
| 600 | 0008 | `true` → `"unknown_pending"` | ✅ |
| 601 | 0002 | `true` → `"unknown_pending"` | ✅ |
| 602 | 0047 | 仅改 reason（值仍 `true`） | ✅ |
| 1118 | 0007 | `false` → `true` | ✅ |
| 1396 | 0048 | `false` → `true` | ✅ |
| 1674 | 0033 | `false` → `true` | ✅ |
| 1952 | 0046 | `false` → `true` | ✅ |
| 2230 | 0006 | `false` → `"unknown_pending"` | ✅ |

**L2508（0009）零改动** ✅ —— 但它的 reason 有一处需要记账，见「发现二」。

### 1.4 顺带核了实施记录 §4.2 自己复算的那组数（决议 §三.3 的底稿订正）

| 判据 | 实施记录写 | 我实测 | 判定 |
|---|---|---|---|
| 三谓词（`signboard_present`/`canopy_present`/`cantilevered_canopy` 且 `applicable=false`） | 36 行、全部非 3.1.1 | **36 行、3.1.1 占 0** | ✅ |
| 分布 | `3.4.2(B)(a)/(b)/(c)/(e)` 各 6、`4.3.1(d)` 6、`4.3.1(k)` 6，**不含 `3.4.2(D)`** | **逐位相同**（6/6/6/6/6/6 ＝ 36，无 `3.4.2(D)`） | ✅ |
| 中文短语「懸臂」判据 | 41 行、全部非 3.1.1 | **41 行、3.1.1 占 0** | ✅ |

⇒ 底稿原写的 **56** 确实是 `15 + 41` 混了两把尺子，订正为 **36** 成立。
把作废的旧数换成自己能复算出来的新数，这一步做对了。

### 1.5 工单前提「两线复核逐位同集」抽核

两份复核文件里出现的行号集合**都恰好覆盖这 29 个行号、无第 30 个**
（`复核_25落改集_official`／`_glm` 各自 grep 落改行号，两边同集），
且都写有「恰好 29 行」的结论句。前提成立。

### 1.6 三态增减自洽

`true` +16 ＝ (+15 Q2) + (+4 Q3) − (−3 转挂起)；`false` −20 ＝ −15 −4 −1；
`unknown_pending` +4 ＝ 3（来自 true）+ 1（来自 false）。合计恒为 2770 ✅

---

## 审核点 2：schema 选型 —— **过**

### 2.1 「保 A3」这个理由成立（可机械验证）

`oneOf: [{"type":"boolean"}, {"const":"unknown_pending"}]`。
改成三值字符串枚举 ⇒ 现存 2,766 行的 JSON 布尔全部要重写成字符串 ⇒
A3「白名单外 2,741 行逐字节相同」**结构上不可能过**。
我实测 A3 差异为 **0 条**，故该理由不只是说辞，是唯一与 A3 相容的编码。✅

### 2.2 下游消费者能吃混合类型（全仓枚举，不是抽查）

`grep` 全仓 `applicable` 字段访问点（`.get("applicable")` / `["applicable"]`），
逐一核对归属：

| 消费者 | 处置 | 判定 |
|---|---|---|
| `score_clause_coverage.py` | 判据本体 `_truth_applicable_state` | ✅ |
| `probe_coverage_both_sides.py` / `audit_atrisk_truth_items.py` / `link_truth_expected_cards.py` / `audit_truth_modeling_gap_claims.py` | `from score_clause_coverage import _truth_applicable_state` | ✅ |
| `check_batch_acceptance.py:125-130,684` | **只取 path / sha256 / 路径同一性**，不读 `applicable` | ✅ 不受影响 |
| `assert_pool_truth_disjoint.py:44` | 只取 building_id，不读 `applicable` | ✅ |
| `test_applicable_truth_circumstance_consistency.py` / `test_truth_landing_25_frozen.py` | 显式三态 | ✅ |
| `build_precision_adjudication_material.py` | 按元组键查表，不做真值判断 | ✅ |

Python `json` 把 `true` 反序列化为 `bool`、把 `"unknown_pending"` 反序列化为 `str`，
两者都被唯一入口吃下并 fail-loud 兜底。**无残留真值判断。**

### 2.3 观察（不阻塞，但值得记）

常驻测试 `test_truth_landing_25_frozen.py::test_a9_every_row_conforms_to_schema`
自述「逐行过 schema」，实际只对 `scope_type / modality_zh / conditionality`
三个 **enum** 字段做检查（`:204-207`）——**唯独没有校验 `applicable`**，
而本次改的正是它的 `oneOf`。落改脚本 `apply_truth_landing_25_20260805.py:398-400`
里有这一条，但脚本只跑一次。
另：本环境 **`jsonschema` 4.25.0 可用**（实测 import 成功），
「把 schema 校验接上主链」本可真按 schema 驱动，现在接上的是手搓的部分复刻。

---

## 审核点 3：`_truth_applicable_state()` 唯一入口 —— **过**

### 3.1 六处暴露面真的全走它

五个脚本一律 `from score_clause_coverage import _truth_applicable_state`，
**无一处自写判据**（diff 逐个核过）；第六处（一致性测试）改的是断言不是判据。
`sys.path.insert` 引入的 `agent_v1/scripts` 与标准库**零同名模块**（实测 114 个模块，交集为空），无遮蔽风险。

### 3.2 fail-loud 实跑

```
_truth_applicable_state({"applicable": 1})     -> ValueError
                        ... 0 / 1.0 / 'yes' / None -> 全部 ValueError
```
✅ 权威数据上的未知取值不静默容错。

### 3.3 `pending_item_count` 出报表（实跑产物核，不看代码）

真跑评分器落盘 JSON 实测：每栋 **10/10** 带 `pending_item_count`；
`overall.pending_item_count = 4`；顶层另有 `truth_applicable_state_counts`
与 `truth_pending_note`；`pending_items` 非空的恰是 0002/0006/0008/0028 四栋。✅
「挂起量增长会静默缩小分母」这条被堵住了。

### 3.4 红先行探针的可信度 —— **我独立重放，不采信实施记录的自述**

把 `git show HEAD:agent_v1/scripts/score_clause_coverage.py` 拉进隔离目录
（`<tmp>/scripts/` + `<tmp>/tests/`，探针原文不改一字）实跑：

```
4 failed, 2 passed
  applicable_item_count == 2      （期望 1）
  applicable_item_recall == 0.5   （期望 1.0）
  KeyError: 'pending_item_count'
  AssertionError: assert 'not_modeled' is None
  AttributeError: module has no attribute '_truth_applicable_state'
```
**五个症状与实施记录逐位相同。** HEAD 版 `:851` 确为 `if item["applicable"]:`、
`:960` 确为 `if r.get("applicable")`，与决议引的行号一致。✅

且实施记录诚实标注的那条——`test_pending_row_stays_out_of_precision_denominator`
在旧代码上是**绿**的——我复现为 2 passed 之一，属实。
我进一步做了探针 docstring 里声称的**第二条变异**（把第三态并进精确率侧 `else`）：
实测该条**转红**（3 failed）。⇒ 这条测试不是摆设，它锁的是另一个方向。✅

**结论：探针是真变异验证过的红先行，不是落地后补的绿测试。**

---

## 审核点 4：端到端并排 —— **过**（另见「发现一」）

我用**当前评分器**分别喂 HEAD 版真值与落改版真值，各跑一次
`phase_i_fragcov2_seed301_20260729`，唯一变量是真值文件版本：

| 指标 | 改前（我实测） | 改后（我实测） | 实施记录 | 判定 |
|---|---|---|---|---|
| `applicable_item_count`（D） | 2324 | 2340 | 2324→2340 | ✅ |
| `covered_count` | 1805 | 1816 | 1805→1816 | ✅ |
| `missed_applicable_item_count` | 519 | 524 | 519→524 | ✅ |
| `applicable_item_recall` | 0.7766781411 | 0.7760683761 | 0.7767→0.7761 | ✅ |
| 精确率侧分母 | **443** | **423** | 443→423 | ✅ |
| `anomaly_false_item_evaluated` | **149** | **134** | 149→134 | ✅ |
| `pending_item_count` | 0 | 4 | 0→4 | ✅ |
| `buildings_gate_pass` | 0/10 | 0/10 | 0/10 | ✅ |

**八个数逐位复现，一个不差。**

### 4.1 「19 进 3 出」拆账 —— 我按组重算，并做了一个实施记录没做的检查

| | 组数 | covered | missed |
|---|---|---|---|
| 进入 D | **19** | 14 | 5 |
| 退出 D | **3** | 3 | 0 |
| 净 | **+16** | +11 | +5 |

- 进的 19 ＝ Q2 15（§3.1.1 两项 × 各栋）＋ Q3 4（0007/0048/0033/0046）
- 出的 3 ＝ Q3 转挂起的 0002/0008/0028
- 召回微降机制**成立**：进的边际召回 14/19 = **0.7368** < 0.7767，
  出的 3/3 = **1.0** > 0.7767 —— 两侧同向压低，故必降且降幅极小。

🔴 **我另做了一条实施记录没有的检查，而它比那两个预注册量强**：
**共同组（未进未出的 2,321 组）判定翻转数 ＝ 0**。
这才是「本次落改只动了该动的行、没有别的东西跟着动」的直接证据。

⚠️ 同时要指出：实施记录把该结论挂在「两条独立预注册量同时精确命中（423 与 134）」上，
**其中 423 不具备它宣称的证明力**——`applicable=false` 的行数是**真值文件自身的属性**，
在任何批上都会命中 423，它证明的是「落改集大小对」，不是「批上没别的东西动」。
134 才有批依赖。建议把结论改挂在「共同组零翻转」上。

---

## 审核点 5：A8 冻结与三条一致性测试 —— **过**

### 5.1 24 行我独立复算（同源判据：`applicable is True` 且 reason 自述判不适用）

| | 矛盾行 | 涉规范项 |
|---|---|---|
| HEAD（改前） | **28** | 7 项（含 `s2_1_3_n`） |
| 工作树（改后） | **24** | 6 项 |

改后 6 项恰为 §2.1.3 的 **(a)/(g)/(k)/(l)/(m)/(p)**，每项 **4 栋**；
`s2_1_3_n` **清零** ✅。与 `test_a8_residual_contradictions_are_all_outside_s2_1_3_n` 一致。

### 5.2 「两向必红」与非空性

断言是 `len(hits) == 24` 的**等式**⇒ 变大变小两向都红，结构保证；
非空性 24 > 0 成立（不是拿一个恒真的空集合当闸）。✅

### 5.3 三态处理后的 `is False` 逃逸补上了

原三条断言用 `is False`，第三态全逃逸。新增两条：
`test_applicable_values_are_within_three_states`（取值域）＋
`test_circumstance_1_never_pending`（情形 1 不得挂起）。
两条实跑绿，且**处置方式对**——没有只把 `is False` 抄一遍改成 `== PENDING`，
而是先锁取值域再锁情形冲突。✅

### 5.4 观察（不阻塞）

取值域闸写作 `any(x is v or x == v for v in (True, False, PENDING))`。
因 Python `1 == True`，实测：

```
probe=1 / 0 / 1.0   → 取值域闸判「合法」
同样取值喂唯一入口 → 全部 fail-loud ValueError
```
即：JSON 里若写成整数 `1`，**这道闸放行、而每一次评分都会炸**。
方向是安全的（生产端 fail-loud），但闸的 docstring 自称「顶掉打错字的第四态」，
对**数值型**不成立。一行可修：判据换成 `type(v) is bool or v == PENDING`。

---

## 审核点 6：精裁材料标注 —— **过**

### 6.1 +62 −0 逐文件核对

`8 / 9 / 8 / 8 / 11 / 8 / 10` ＝ **62 增 0 删** ✅（`git diff --numstat` 实测）

### 6.2 18 条目块 / 15 去重行，机械核实全过

- **条目块 18**：2+3+2+2+4+2+3 ＝ 18 ✅（与「15 行 × 两系列重叠」自洽）
- **去重真值行 15**：L30 / L34 / L1118 / L1396 / L1674 / L1687 / L1688 / L1952 /
  L1965 / L1966 / L2230 / L2243 / L2244 / L2521 / L2522
- **15/15 全部落在 29 行白名单内**，且每条标注写的新值与我 dump 的实际值**逐条一致** ✅
- 撤回 12 块 / 坐实 6 块；去重后撤回 11 行、坐实 6 块（L1687/L1688 两形都占，见下）

### 6.3 「坐实」标对了 —— 我打开原文核过全部 6 条

行 41 / 42（file 3）、行 29 / 30（file 5）、行 68 / 69（file 7）的原判
**literally 都是 `2. 裁定：真值标错`** ⇒ 标「坐实（结论不变、已被落改采纳）」而非「撤回」，
**正确** ✅。
特别地，行 68/69 的「关键分歧点」原文写的就是
「真值按『是否有招牌／伸出物』判不适用，系统按『楼被选定→须涵蓋』判定」——
正是 Q2 口径最终采纳的立场，二者互证。

### 6.4 行 67（L2230）的处理

标注原文：「`applicable` 由 `false` 改为 `unknown_pending`（真值不可判，第三态）……
**但真值并未转为 `true`，故不宣称「系统对」**」。
✅ **处理正确**——这是本批标注里最容易做错的一条，没做错。

### 6.5 跨系列矛盾已在注记层点明

L1687/L1688 在 `裁定_精149_021-030` 判「系统误评」、在 `裁定_精确率149_021-040` 判「真值标错」，
两系列此前互相矛盾。file 2 的注记**显式写出**这一点并择一（以落改后为准）。✅

### 6.6 三件遗留 —— **均不升级为阻塞**，理由逐条

| 遗留 | 判定 | 理由 |
|---|---|---|
| 头部四类统计仍是落改前口径 | **不阻塞** | 七个文件头部**都已显式声明**「原文与上方四类统计一律保留、未重算（统计是落改前口径）」——自曝的陈旧数比悄悄重算的新数安全 |
| 跨系列矛盾只在注记层点明 | **不阻塞** | 已点明并择一；升级为「统一重写两系列」属重裁，超出本案授权 |
| 149 行由 17 份材料重复覆盖未去重 | **不阻塞，但要提** | 残留实体是 **15 份 `原料_精149_*.md`**，仍写「实际行数：**149**」且零作废注记。但它们是**原料**（真值理由＋系统判定记录＋条款正文）不是**裁定结论**，风险是陈旧引文而非陈旧判决 ⇒ 归入必修第 4 条一并处理即可 |

---

## 审核点 7：W2 blind 与射程 —— **过**（传播面有必修项）

### 7.1 G1 / G2 / G3 我全部独立重跑

| 层 | 我的实测 | 判定 |
|---|---|---|
| **G1** `grep -rn "applicable_normative_item_truth" agent_v1/src/evo_agent_baseline/ --include=*.py` | **零命中** | ✅ 真值路径不进运行时包 |
| **G2** 三个运行时包（`agent/` `closure/` `retrieval/`）内 `evo_agent_baseline.eval` | 命中全部在 `closure/tests/`，且全部是**断言禁止 import 的测试文本本体**：`test_blueprint_deriver.py:1137` 与 `test_blueprint_state_eval.py:1677` 的 `_FORBIDDEN_MODULE_PREFIXES`、`test_identity_v2.py:874-876` 的字符串 fixture、`test_unknown_attribution_invariance.py:494` 的 `assert "import eval" not in src`。**无一处真 import** | ✅ |
| **G3** blind 断言测试实跑 | `test_unknown_attribution_invariance` + `test_blueprint_state_eval` + `test_source_coverage_matrix` ⇒ **111 passed** | ✅ |

**本案零触 blind 红线。**

### 7.2 射程外文件逐字节相同 —— 我用备份 patch 做了可复算的机械核

方法：把 `杂物箱/备份_25_真值落改_tracked.patch`（#25 动手前的 `git diff`）
与**当前** `git diff` 按文件切开逐段比对。差异段 ＝ #25 触及的已跟踪文件。

**实得恰 16 个**：

```
agent_v1/scripts/{score_clause_coverage, probe_coverage_both_sides,
                  audit_atrisk_truth_items, link_truth_expected_cards,
                  audit_truth_modeling_gap_claims,
                  build_precision_adjudication_material}.py        6
agent_v1/src/evo_agent_baseline/eval/{*.jsonl, *.schema.json}      2
agent_v1/tests/test_applicable_truth_circumstance_consistency.py   1
团队文档/我的笔记/裁定_精*.md                                        7
```

⇒ **卡包 / worldgen / closure 一个字节没动**，与实施记录自述一致。
这条比「我没碰」的自述强——它是可复算的，且能把 #29 那一大堆在途改动干净地排除掉。✅

### 7.3 §2.5「既存 SyntaxError」核实 —— 属实，但差点被我误判

`ast.parse(HEAD 版 link_truth_expected_cards.py)` **通过**（`ast.parse` 不检查
future-import 位置），差点让我判「实施记录说谎」。改用 `compile()`：

```
HEAD compile() SyntaxError: from __future__ imports must occur at the
beginning of the file (line 92)
工作树版 compile(): OK
```
✅ 既存缺陷属实（第二个模块级字符串字面量把 `from __future__` 挤到第 92 行），
修复零语义改动。

### 7.4 「443/149 作废」的传播面 —— **未清干净，这是必修**

grep 全仓实得，仍把这两个数当**现行常数**用的位置：

| 位置 | 原文 | 性质 |
|---|---|---|
| `团队文档/我的笔记/技术与研究债.md:72` | 「精确率侧**能测一个口径**：**149/443 ＝ 33.6%** 误评」 | 🔴 **台账是项目的可引数字权威库**，这里没改 = 作废只活在实施记录里 |
| 同上 `:73` | 「**204/443** 出处已查到……永久不可引」 | 同锚，需同批订正分母 |
| `agent_v1/scripts/score_clause_coverage.py:719` | 「实测 **399/443** = 90.1% 报 anomaly」 | 在 **#25 自己改的文件里** |
| `agent_v1/scripts/score_clause_coverage.py:730` | 「修正后数字变小（**149/443** = 33.6%）」 | 同上 |
| `agent_v1/tests/test_clause_coverage_reverse_gate.py:9,15` | 「**399/443** = 90.1%」「**149/443** = 33.6%」 | 测试 docstring |
| `agent_v1/tests/test_truth_third_state_denominator.py:140` | 「精确率侧反向闸（`applicable=false` 的 **443** 分母）」 | 🔴 **#25 本次新建的文件**，写进去的就是刚被自己宣布作废的常数 |
| `团队文档/我的笔记/原料_精149_*.md`（**15 份**） | 「实际行数：**149**」 | 原料，风险较低 |
| `团队文档/我的笔记/停止认证_四个毛病_20260803.md:14`、`停止认证结果_四个毛病_20260803.md:83` | 「误评 149/443」 | 历史报告，可只加一句作废注 |

（`apply_truth_landing_25_20260805.py:266` 的 `443` 是**改前**分布断言，正当，不算残留。）

---

## 🔴 发现一（最重要）：落改集的证据基础与端到端验证批是**两个不同的池**，文档里没有一处说这件事

### 事实（全部亲跑）

Q3 九行的新 reason 把世界证据**计数**写死进了权威数据自述，例如：

> `artifact.proposal.detailed_investigation`（本楼 **0/5** 取真，any_true 聚合）——**缺席**  （L599/600/601/2230）
> `artifact.proposal.detailed_investigation`（本楼 **3/5** 取真）……⇒ P 取真，判适用  （L602/1118/1674）

我把这十栋在两个 seed301 批上各数了一遍 `measure_key == artifact.proposal.detailed_investigation`
的 `value_json=="true"` 计数：

| 楼 | 落改后真值 | `baseline_batch_final_seed301` | Q3 规则应得 | `phase_i_fragcov2_seed301_20260729` | Q3 规则应得 |
|---|---|---|---|---|---|
| 0007 | `true` | **3/5** | true ✅ | 5/7 | true ✅ |
| 0048 | `true` | **2/5** | true ✅ | 3/6 | true ✅ |
| 0046 | `true` | **2/5** | true ✅ | 2/7 | true ✅ |
| 0047 | `true` | **3/5** | true ✅ | 5/7 | true ✅ |
| 0009 | `true` | **3/5** | true ✅ | 3/8 | true ✅ |
| **0033** | `true` | **3/5** | true ✅ | **0/6** | **`unknown_pending`** ❌ |
| **0002** | `"unknown_pending"` | **0/5** | pending ✅ | **2/8** | **true** ❌ |
| **0006** | `"unknown_pending"` | **0/5** | pending ✅ | **3/8** | **true** ❌ |
| **0008** | `"unknown_pending"` | **0/5** | pending ✅ | **4/8** | **true** ❌ |
| **0028** | `"unknown_pending"` | **0/5** | pending ✅ | **2/7** | **true** ❌ |
| | | **10/10 一致** | | **5/10 不一致** | |

- 在 `baseline_batch_final_seed301`（及 `phase_a`–`phase_g` 七个同池批）上，
  reason 写的 `0/5` / `2/5` / `3/5` **逐位精确成立**，Q3 规则输出与 29 行落改
  **10/10 全对**。⇒ **落改集在它自己的锚池上完全正确，方向无需重裁。**
- 在 `phase_i_fragcov2_seed301_20260729` 上，同一批槽的计数全变（分母从 5 变成 6/7/8），
  **5 栋应得值反号**：四栋挂起的建议书其实**在场**，判 true 的 0033 建议书**缺席**。
- `procedure.investigation.intention_notified` 同样漂移：实施记录 §3.2.3 与落改脚本
  `RULE_Q3` 都写「**唯一** notified=true 的 0009 栋」——在旧池成立（0009=1/1、其余 0/1），
  在 `phase_i` 变成 0007/0047/0008 三栋为真、**0009 为假**。
- 两份复核 `复核_25落改集_{official,glm}_20260805.md` 用的批是
  **`baseline_batch_final_seed301`**（`:246`/`:335`、`:5`/`:211`/`:248`）；
  而实施记录 §5.4 的端到端用的是 **`phase_i_fragcov2_seed301_20260729`**。
  底稿 `:238` 与 `:625` 两个批都出现过。

**「不同池」是硬证据，不是我的推断** —— 两批 `batch_manifest.json` 的 `worldgen_run_dir`：

```
baseline_batch_final_seed301        → …/qa_reports/_reanchor_50x1_seed301/gen_seed_301
phase_i_fragcov2_seed301_20260729   → …/qa_reports/_fragcov2_50x1_seed301/gen_seed_301
                                       pool_content_sha256 = d835cbab6675ad9b…
```
**同一个 seed 标签（301）、两个不同的池目录、内容不同。**
这正是本仓记过的「加一个世界槽就换掉整池种子」——`seed301` 这个名字**不能**用来判同池。

### 这不是要求重裁 29 行，但「哪个池是真值的锚」得有人拍

工单明令方向不重裁，而落改集在锚池（`_reanchor`）上 **10/10 正确**，
故审核点 1／4 按其各自的判据**均判过**。

⚠️ 但仓库现在**两边都在用、且没人声明**：

| 用 `_reanchor` 池 | 用 `_fragcov2` 池 |
|---|---|
| 真值全部 reason 的世界计数 | 实施记录 §5.4 端到端 |
| 两份复核的 fact_pack 对账 | `build_precision_adjudication_material.py:232` 写死的数据锚 |
| 落改脚本 `RULE_Q3` 里的「唯一 notified=true 的 0009 栋」 | 149 行精裁材料、`batch_acceptance` |
| 实施记录 §尾巴 5「本案改的是 seed301 **旧池**的阅卷面」 | |

**「真值到底锚哪个池」是决策门问题，不在审核门裁定范围**，我不替它拍。
审核门能确认的是：**这个状态不能留着不声明**——因为
①若锚 `_reanchor`，那 §5.4 的端到端与整套 149 行精裁材料都跑在错池上；
②若锚 `_fragcov2`，那 Q3 的 5 行值是错的。两条都不是小事，而现在两条都没人说。

除去这个待拍事项，本案自身还有**三处口径缺失**是可以且应当当场补的：

1. **九条新 reason 写死了具体世界计数却不点名批/池。** 这是权威数据的自述，
   而项目自己的引用纪律（实施记录 §5.4 末尾还重申了一遍：「引用必须同时点名
   `batch_root` ＋ 真值文件版本」）在这九行上没有执行。
   **下一个人拿最新批一核就会得出「真值错了」——我刚才第一反应就是这样**，
   花了三轮量测才排除。这正是本仓记过的
   「加一个世界槽就换掉整池种子，对比须控制池」那个坑的下一次发作。
2. **§5.4 的端到端跑在与证据基础不同的池上，且没有声明。**
   它作为**分母／管线**的并排验证仍然有效（我八个数全复现），
   但它**不是**对 Q3 落改世界锚定的验证，而实施记录的行文容易被读成后者。
3. **`test_q3_pending_rows_are_exactly_the_four_proposal_absent_buildings` 把四栋
   冻进了常驻断言**，docstring 写「两线复核 fact_pack 对账吻合」却不写是哪个批。
   这条断言不读批产物所以永远绿，但它冻住的是一个**只在某个未命名池上为真**的事实。

---

## 发现二：L2508 的 reason 用的正是本案宣布非法的那条推理

`L2508`（0009 栋）决议判「不动（唯一现文合规行）」，实施记录 §3.1 表格写
「**值与 reason 均合规**」。值确实合规（该栋 proposal 在场，第一分支命中）。
**但 reason 不合规**，原文：

> 前件 P：有意進行詳細調查。`procedure.investigation.intention_notified`
> 本楼 1 条取真、全批 3/30 楼有正向记录 **⇒ P 成立 ⇒ 适用**。

这**就是**决议 §二删掉的 `ELIF NOTIFIED is true → applicable=true` 分支，
也正是新 Q3 规则明写「禁止以 `intention_notified` 反推 P（**正负两向均非法**）」的那件事。
实施记录 §3.2.1 自己讲得很清楚：**「口径只活在裁定文档里、不落在数据自述里，
下一个人照样会用它」**——L2508 就是这句话的现成反例。

实施者是知道的：`test_q3_reasons_exclude_the_notice_artifact_slot:180-181` 专门写了
`missing = [b for b in missing if not b.endswith("0009")]` 把它豁免掉。
豁免是对的（不扩范围），**但实施记录「值与 reason 均合规」这句话是错的**，
应改成「值合规、reason 走的是已作废的旧推理，按不扩范围原则不动，已在测试里显式豁免」。

---

## 发现三：决议 §一 的行文把 L1952 归错了类（实施是对的）

`决议_真值落改_20260805.md` §一 Q3 那句写：

> L1118/1396/1674/**2230** `false→`按 Q3 判据……；L602/**1952** 仅改 reason。

但 **L1952（0046）改前是 `false`**（我 dump 实测），而 0046 的
`artifact.proposal.detailed_investigation` 在锚池上 **2/5 取真** ⇒ 按决议自己
刚写下的 Q3 判据（proposal 在者 → `true`），它**必须**变值，不可能「仅改 reason」。

实施照的是 **底稿 A5**（`底稿_真值落改_20260805.md:246` 写「L1952 false → **改 true**（波及）」、
`:295` 表格「Q3 `false → true` | 4 | L1118 / L1396 / L1674 / L1952」），
而决议那句自己就注明「逐行值见底稿 A5」⇒ **实施跟的是决议指定的权威子引用，没有越权，
落改值正确**（我实测 L1952 `false→true`、L2230 `false→unknown_pending`）。

问题只在于**决议正文留了一处自相矛盾的分类在案**：下一个人若照决议 §一 的字面
重新推导落改集，会得到一个不同的文件（L1952 停在 `false`，族内分布变 true=5/false=1/pending=4，
A7 会红）。建议在决议里加一行订正，别让两个版本的分类并存。
（当前有 A7／A8 兜住，不构成运行风险，故列为建议不列为必修。）

---

## 必须修清单（4 条，都不动 29 行的值、都不要求重裁）

| # | 事项 | 位置 | 为什么必须 |
|---|---|---|---|
| **1** | Q3 九行 reason（及落改脚本 `RULE_Q3` 常量、`test_q3_pending_rows_...` 的 docstring）**补上证据所锚的批/池名**，并写明「fragcov 之后的池上不成立」 | 真值 L599-602/1118/1396/1674/1952/2230；`apply_truth_landing_25_20260805.py`；`test_truth_landing_25_frozen.py` | 权威数据自述写死了世界计数却不点名批，与本仓引用纪律直接冲突；下一个人按最新批复核必然误判「真值错了」（我已实测走了这条弯路） |
| **2** | 实施记录 §5.4 补一句「端到端所用批 ≠ 真值证据基础池」，并把「没有别的东西跟着动」的证据从「423 与 134 双命中」改挂到 **共同组零翻转**（或并列） | `实施记录_25_真值落改_20260805.md` §5.4 | `423` 是真值文件自身属性、任何批都命中，不具备它被赋予的证明力 |
| **3** | 订正实施记录 §3.1 表格里 L2508 的「**值与 reason 均合规**」 | 同上 §3.1 | reason 用的正是本案宣布两向非法的 `intention_notified` 反推；测试已显式豁免 0009，文档却说它合规，两处对不上 |
| **0**（转决策门） | 声明真值锚哪个池（`_reanchor` vs `_fragcov2`），并按结论处理连带面 | 见发现一表 | 两边都在用、都没声明；两种结论各自蕴含一处必须返工的地方 |
| **4** | 把 `443 / 149` 的作废落到台账与代码注释：`技术与研究债.md:72-73` 必改；`score_clause_coverage.py:719,730`、`test_clause_coverage_reverse_gate.py:9,15`、**`test_truth_third_state_denominator.py:140`（本案新建文件）** 同批；15 份 `原料_精149_*.md` 加一行作废注 | 见 §7.4 表 | 台账是本项目「可引数字」的权威库；作废只写在实施记录里，等于没作废。本案新建文件里就写着刚被自己宣布作废的常数 |

### 建议修（不阻塞）

- `test_a9_every_row_conforms_to_schema` 补上 `applicable` 的 `oneOf` 校验；
  本环境 `jsonschema 4.25.0` 可用，建议直接改成 schema 驱动，
  「把 schema 校验接上主链」这句话才字面成立。
- 取值域闸改 `type(v) is bool or v == PENDING`——现写法因 `1 == True` 放行整数
  `1/0/1.0`，而阅卷器对同样取值 fail-loud，两道闸口径不一致。
- 订正 `决议_真值落改_20260805.md` §一 里 L1952 的归类（见发现三）。

---

## 附：全量 pytest —— 我独立跑，与实施记录逐位相同

```
$ PYTHONPATH=agent_v1/src python -m pytest agent_v1 -q
3475 passed, 1 skipped, 11 xfailed, 4 warnings in 836.46s (0:13:56)
```

| 项 | 实施记录 | 我实测 | 判定 |
|---|---|---|---|
| passed | 3475 | **3475** | ✅ |
| skipped | 1 | **1** | ✅ |
| xfailed | 11 | **11** | ✅ |
| failed | 0 | **0** | ✅ |
| 耗时 | 814 秒 | 836 秒 | 计数一致，耗时差属机器噪声 |

⚠️ 口径说明：工作树同时含 #29／期限锚／rng 隔离等**其它在途改动**，
故全量结果反映的是**合并态**，不是 #25 单独的回归面。
#25 射程内的测试我已单独跑过：
`test_truth_third_state_denominator`(6) ＋ `test_truth_landing_25_frozen`(9) ＋
`test_applicable_truth_circumstance_consistency`(5) ＋ `test_clause_coverage_dedup`(4) ＋
`test_truth_modeling_gap_adjudications`(4) ＝ **28 passed**。
