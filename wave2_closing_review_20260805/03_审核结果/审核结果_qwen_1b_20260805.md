# 审核结果：1b fsp 接线恢复实施（qwen 线，2026-08-05）

**性质**：审核门（代码对不对）。方向已由三线门全票定案，本次不重裁方向——**未发现方向问题**，主体审实施。
**审法**：只读＋亲跑。重建了 1b 前基线（`杂物箱/备份_1b_fsp_20260805.patch` 反演到临时目录，
逐文件 diff 出 1b 净改动），自跑单测、变异对照、两池生成＋字节配对、位移量化。
全部数字为亲跑输出，不转述实施记录。
**总结论：过**（七个审点全过；无「必须修」；2 条建议、1 条存疑，见末节）。

---

## 审点 1：前置清点可信度 —— 过

亲跑 AST 扫 `_compute_derived_flags_for_condition` 函数体（`generator.py:2703-2939`）：

- 形参 10 个，被读 7 个：`condition / drainage / ubw / fire / repair_assessment /
  measurements / fsp_estimate`；**`fragment / mechanism / building` 零读取**——与实施记录一致。
- 函数体内顶层调用仅 `_compute_max_danger_index`（只吃 condition/drainage/ubw/fire，
  全片段级）与 `_compute_verification_failed`（只吃三个数值入参）。
- 六个状态对象来自 `_build_per_fragment_state_lookup`（`generator.py:2626-2660`）：
  mechanism 按 `fragment_id`、drainage/ubw/fire 按域 id 反解出的 `fragment_id`、
  repair 按 `fragment_id`、measurements 按 `measurement.target_ref == fragment_id` 收
  （:2657-2659）——结构上不可能跨片段。
- row 4/5 同源下游判断成立：`repair_required` 析取链含 `risk_building_safety_emergency`
  （:2787），`maintenance_pre_next_cycle_required` 含 `not risk_building_safety_emergency`
  （:2799）——改 row 1 的 fsp 一处，三行同归位，无第二处接线可做。
- A 类 6 旗标 OR 汇总写在 `WorldBundle.derived_outcomes`（:3044-3062）、不回写
  condition，无构件级串扰。

「唯一楼级量是调用方塞进来的 `fsp_estimate`」成立，清点可信。

## 审点 2：接线正确性 —— 过

1b 净改动（对 1b 前基线 diff）核对：

- **逐片段读量测**：新增 `_FSP_MEASUREMENT_SLOT_ID = "ratio.fsp.structural_performance"`
  （:2668）与 `_lookup_fragment_fsp_measurement`（:2671-2688），入参是
  `_build_per_fragment_state_lookup` 已按本片段收好的量测列表；`populate_derived_flags`
  循环里逐 condition 调用（:2980），作为 `fsp_estimate` 传入（:2991）。
- **无量测落 `not_applicable` + `no_assessment`**：row 8 死分支激活（:2860-2865），
  不是沿用旧值也不是静默跳过；`index.fsp.estimate` 无量测时不写键且显式 `pop`
  （:3013-3016）。亲跑 50 栋池实测：5 条无量测 condition 全部落该分支（见审点 6）。
- **`risk.building_safety.emergency` 同批片段级**：row 1 与 row 8 共用同一个
  片段级 `fsp` 变量（:2746、:2767-2771），同文件同批；无量测时 `fsp = 1.20`
  （spec 06 §10 fsp_true 公式常数项），`fsp < 0.75` 分项恒不触发。
- **零新公式核对**：`_compute_fsp_true`（`generator_sampling.py:112-119`）
  `clip(1.20 − 0.30·max_sev − 0.10·age_norm, 0, 2)` 与旧楼级三行逐字同构，仅作用域
  从全楼换成本片段；自跑配对中 `world.measurements` 逐字节不变（见审点 4），
  「零新公式、零新系数、零新注册表」的机器判据成立。
- W1-003 占位记录过滤（`value_enum == "not_applicable"` 或 `value_num` 空 ⇒ None）
  与同函数 `valid_tv` 判据同口径（:2827、:2681-2687）。

## 审点 3：单向性硬断言与变异对照 —— 过

- 断言 ②（`test_fsp_fragment_scope.py:130-153`）对照的是**复刻的已删除楼级公式**
  （`_legacy_building_level_fsp` :68-78），与 1b diff 里的旧三行逐字一致——是
  「对照 1b 前产物」，不是自己对自己。数学方向（片段 max ≤ 全楼 max ⇒
  fsp_片段 ≥ fsp_楼级 ⇒ 只许 True→False）成立。
- 非空护栏：`assert forward > 0`（:151）。亲跑变异实验（把 fsp 退回楼级 max，
  monkeypatch 不改仓库）证实护栏能红：退楼级后 forward = 0，护栏触发。
- **变异对照四层亲跑全红**：① 索引/旗标与本片段量测不符 **44** 条；
  ② 5/5 个 seed 无正向翻转（空护栏触发）；③ 剥离量测后旗标/原因码/索引键三项断言
  共 15 处违例（5 seed × 靶片段 3 断言；实施记录按 5 个靶片段计「5 条」，口径不同
  方向一致）；④ 加片段扰动 **5/5** 个 seed。(294 行测试文件逐条读过，断言非空转。)

## 审点 4：allowed-change 集移动最小性 —— 过

- `STEP_ALLOWED_CHANGE["1b"]` = `_FSP_DERIVED_UNITS` 恰好 3 个单元
  （`verify_rng_isolation_pairing.py:126-130,176`）：`buildings.derived_outcomes` /
  `condition.derived_outcomes` / `repair_assessment.writeback`——fsp 派生族的直接落点
  与两条文档化的传导单元。`sidecar.*`、全部 `proj.*`、`world.measurements`、
  `condition.core`、`ras.core`、`world.meta` 等 20 个单元仍在须相同集；
  其余 step 的集合未被 1b 触碰（与 1a 实施记录各步「须相同单元数」16/12/16 对得上）。
- **亲跑配对**（自生成两池：基线＝monkeypatch 复辟楼级、候选＝现码；seed 301 /
  50 栋 / ctcov on / fragment 4 / building_workers=1）：
  - 20 个须相同单元全部逐字节 PASS，含 `sidecar.entries`（本池未发作，池相关）、
    `world.measurements`、`world.meta`；
  - 唯一变动单元 `condition.derived_outcomes` 差异 **297/347** 行（347−297=50
    的自洽解释合理）；两单元「允许但未变」由脚本 `[注意]` 节输出；
  - 两侧 `deterministic_key` 同 = `46e48b3a6142…f3f08e`，与实施记录一致；
  - `[参考] expected_verdict 翻判 = 0`（347/347 保留键）、`threshold pass_bool
    逐行差异 = 0/11646`、全部 6 个 `proj.*` 单元逐字节相同 ⇒
    「1b 是指标换锚不是真值换锚」与三线门定案**互证成立**。

## 审点 5：两处实施中自主决定 —— 均过

**a. 白名单甲条目（`test_slot_alias_normalization_scan.py:120-126`）＝按护栏本意补的白名单**：

- 白名单甲的语义是「两侧同源，过不过归一逐字节同结果，每条必须写清为什么」。
  新条目写明：比的是硬编码字面量 `ratio.fsp.structural_performance`（W1 Step 8
  自产的世界侧量测槽名），与被比的 `measurement.slot_id` 同源同侧。
- 三个事实亲核：① 生产代码用 `==` 精确比较（`generator.py:2683`），未改 `in` 之类
  写法绕扫描器；② `workflow_engine` 全包无 `evo_agent_baseline` 的 import
  （仅注释提及），卡侧名结构上进不了该层；③ 该名不在别名表键里也不在值里
  （亲跑 16 键核对）。
- 同文件已有同形先例（`_compute_derived_flags_for_condition` 条目，:127-132），
  格式与理由密度一致。

**b. 显式 `pop`（:3016）＝正确，且确为测试逼出**：

- 调用面亲核：`populate_derived_flags` 生产调用点唯一（`generator.py:3249`，
  `generate_world_bundle` 末尾）；`risk_index_values` 全仓仅本函数两处写入
  （:3000、:3014），字段缺省空 dict（`models.py:140`）⇒ 生产主链字典恒空，
  pop 零行为差异，说法成立。
- 重跑语义真实存在：`test_derived_flags.py:139-140` 幂等复查、新测试
  `_strip_fsp_measurements` / `_with_extra_fragment` 都在已填世界上重跑。
  断言 ③ 的「无量测 ⇒ `_FSP_INDEX` 缺席」在无 pop 时必红——pop  guarding 的
  路径被测试实跑覆盖，不是装饰品。

## 审点 6：位移量化口径 —— 过

亲跑量化（同口径：seed 301 / ctcov on / 50 栋 / fragment 4 / 内存池；
旧 ＝ 复刻楼级公式只换 fsp 一个输入，其余输入逐条同源）：

| 量 | 亲跑值 | 记录值 |
|---|---|---|
| 旗标翻转 | **124/347**（119 True→False ＋ 5 bool→not_applicable ＋ **反向 0**）| 同 |
| 涉及片段/楼数 | **124 片段 / 42 栋** | 同 |
| row 1 `emergency` 翻转 | **0** | 同 |

- 判据随输出打印：配对脚本把开关态/判据/锚定批三项打印为自身输出
  （:571-587）；量化脚本参数即口径。
- 归因亲核：亲跑测得本池有量测 fsp 值域 **0.8308–1.1084**，低于 0.75 者 **0** 条 ⇒
  「row 1 翻 0」归因成立且确为**池相关**（0.75 低于值域下界，新旧口径下该分项
  都不可能触发）——实施记录已自注「引用这个 0 必须带这句」，诚实边界在位。
- 方向佐证：旧口径 324/347 置「低于安全」旗 vs 新口径 200/347，只多报不少报，
  与单向性恒等式同向。

## 审点 7：尾巴清单完备性 —— 过（另补 1 条存疑，见下）

记录所列 5 条尾巴全成立。复核未发现漏报，旁证：

- `index.fsp.estimate` 下游消费者：`agent_v1/src` 与 `agent_v1/scripts` 零命中
  （历史批 fact_pack 里的该槽是旧产物）；法规卡包（`agent_v1/regulations/`）
  零引用 ⇒ 键缺席不击穿任何现存消费面；
- 框架不跑生成期门（`validation.py:350` 调 `generate_world_batch` 未开
  `apply_gate`），无门反馈通道；
- `fallback_reasons` 在闭包层只进解释通道不改判定
  （`fact_binding.py:108-112` 契约明写「缺省 False＝行为逐位不变」）；
- sidecar 仅经 `repair_required` → `H.repair_need` 一条链
  （`sidecar.py:1063-1068`），即尾巴 2 已记的潜伏耦合，本池未发作（sidecar
  逐字节 PASS 实证）。

## 分级清单

**必须修**：无。

**建议**：

1. 白名单甲新条目补一句失效警示（对照同表前两条的「若将来……此条理由立刻失效」
   句式）：扫描器自己的诚实边界 1 明说白名单理由随代码演进而腐化，
   「改到白名单里的函数时须回来重判」。
2. 实施记录 §二可补一句 row 1 无量测取 1.20 与 spec 06 §10 的锚定关系出处
   （现仅代码注释里有），方便后续引用。

**存疑**：

1. row 1 无量测时 `fsp = 1.20`（`fsp < 0.75` 恒不触发）是 1b 激活的**新政策分支**，
   只有代码注释背书（「spec §10 default fsp_true initial」——§10 给的只是公式常数项，
   未规定无量测时的用法）；spec 06 §11 row 1 无 unknown_policy 列，三线裁定清单也
   未覆盖该点。该选择方向上与「没评估过不得伪装成评估过且不合格」的战役本意一致
   （不臆造紧急），但**「无量测 ⇒ 按完好取值」这个政策本身没有 spec 明文**。
   若 W2/闭包将来把 row 1 纳入消费，建议提请一次裁定确认；不阻塞本单。

## 方向重裁声明

无。实现与三线门定案（逐片段接线、同批改 emergency、激活死分支、指标换锚）
逐项吻合，未发现需要重开方向讨论的事实。

---

## 附：亲跑证据索引

| 项 | 命令/路径 | 关键输出 |
|---|---|---|
| 1b 净改动重建 | `git archive HEAD` ＋ 反演 `备份_1b_fsp_20260805.patch` → 临时目录逐文件 diff | generator.py 149 行 diff；sampling 21；scan 16；derived_flags 36（CRLF 归一后）|
| AST 形参扫描 | 自写脚本 | read 7 / unread `fragment·mechanism·building` |
| 新测试＋旧回归 | `pytest test_fsp_fragment_scope.py test_derived_flags.py` | 44 passed |
| 全量测试面 | `pytest agent_v1/src/workflow_engine agent_v1/tests` | **1486 passed, 2 xfailed**（与记录同）|
| 跨包零耦合 | `pytest agent_v1/src/evo_agent_baseline` | **1858 passed, 1 skipped, 9 xfailed**（与记录同）|
| 槽名归一扫描 | 含在上行全量面 | 通过（807 子集含 scan 测试）|
| 变异对照四层 | 临时脚本（monkeypatch 退回楼级 max）| 44 / forward=0 / 15 / 5-of-5 全红 |
| 两池字节配对 | 自生成两池 → `verify_rng_isolation_pairing.py --step 1b --pool-seed 301 --pool-count 50 --ctcov on` | 20 须相同 PASS；297/347 差异；J6 翻判 0；pass_bool 0/11646；两侧 deterministic_key 同 |
| 位移量化 | 临时脚本（同口径，判据打印）| 124/347 = 119＋5＋0 反向；42 栋；fsp 值域 0.8308–1.1084；row 1 翻 0 |

（临时产物在 `<用户目录>\AppData\Local\Temp\opencode\fsp_review\`，含两池目录
`pool_legacy` / `pool_current`，可复核后删。）
