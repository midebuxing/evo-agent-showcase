# 审核结果（glm 线）：1b fsp 接线恢复实施（2026-08-05）

**审核性质**：审核门（代码对不对）。方向已三线门全票定案，不重裁。
**材料**：`实施记录_1b_fsp_20260805.md` + 工作区改动本体 + 关键测试文件 + 亲跑。
**结论一句话**：实施七项审核点全部通过，无「必须修」项；「位移量化」与「变异对照四层红」属实施记录转述、未亲跑 50 栋池，但代码层证据已支撑定性结论。

---

## 逐条结论

### 1. 前置清点可信度 — 过

**实施记录声称**：AST 扫 `_compute_derived_flags_for_condition` 证明「唯一楼级量是 fsp_estimate」，`building`/`fragment`/`mechanism` 形参未被读。

**亲验**（剥掉 docstring 与注释后做 `\b<name>\b` 词边界扫描，`agent_v1/src/workflow_engine/worldgen/generator.py:2703-2939`）：

| 形参 | 函数体内提及次数 |
|---|---|
| `condition` | 3 |
| `fragment` | **0** |
| `mechanism` | **0** |
| `drainage` | 8 |
| `ubw` | 9 |
| `fire` | 3 |
| `repair_assessment` | 11 |
| `measurements` | 2 |
| `building` | **0** |
| `fsp_estimate` | 3 |

三个楼级上下文形参确实零读 —「唯一楼级量是 fsp_estimate」成立。前六个对象来自 `_build_per_fragment_state_lookup`（:2626-2660）按 `fragment_id` 取；`measurements` 按 `measurement.target_ref == fragment_id` 收（:2657-2659）。结构上不可能跨片段。**未触发「发现第二处即停下回报」**。

### 2. 接线正确性 — 过

逐条核 `generator.py`：

- **读本片段 `ratio.fsp.structural_performance`**：`_lookup_fragment_fsp_measurement`（:2671-2688）入参是 `_build_per_fragment_state_lookup` 已按 `fragment_id` 收好的本片段量测列表；W1-003 的 `not_applicable` 占位记录（`value_enum=="not_applicable"` / `value_num` 空）被显式排除（:2684-2685），与 `verification.test.failed` 的 `valid_tv` 判据同口径。✅
- **无量测片段落 `not_applicable` + `no_assessment`**：:2860-2865，`if fsp_estimate is not None: ... else: "not_applicable" + fallback_reasons["assessment.fsp.below_required_safety"]="no_assessment"`。该分支在 1b 前是死分支（调用方总能算出楼级 max），改读片段量测后第一次真正生效 — 与实施记录吻合。✅
- **`fsp` 局部变量在 row 1 与 row 8 同源**：:2746 `fsp = fsp_estimate if fsp_estimate is not None else 1.20`，:2767-2771 row 1 与 :2860-2861 row 8 都用同一个 `fsp`。无「同一个 fsp 在同一函数里同时有两个作用域」。✅
- **`risk.building_safety.emergency` 同批改片段级**：:2767-2771 直接读片段级 `fsp`，未沿用楼级聚合。✅

### 3. 单向性硬断言 — 过（带保留）

- **断言②**：`test_flag_flips_are_true_to_false_only`（`test_fsp_fragment_scope.py:130-153`）复刻了被删的旧楼级三行公式作反事实基线（:68-78），断言 `reverse == []`（旧 False → 新 True 不许出现）且 `forward > 0`（非空护栏）。5 个 seed 参数化全过（亲跑 31/31 通过）。
- **「对照的是 HEAD 产物」判据**：严格说这里对照的是「**复刻的旧公式**」，不是直接对 HEAD 跑。但旧码已从 HEAD 删除、无法对 HEAD 直接跑；复刻自旧码逐字（test docstring :69-78 明示），是合理等价形式，且 docstring 显式声明了这点（:21-24「这与 test_rng_isolation.py 顶上『不复刻实现』的纪律不冲突 — 复刻的是一段已经删除、不会再演进的历史代码」）。
- **变异对照四层独立能红**：实施记录转述，**未亲验**（scratchpad monkeypatch，不在 CI 路径）。但断言②的 `forward > 0` 非空护栏本身是独立的「护栏非空」证据 — 若实现接错片段，`forward == 0` 会让断言②红。**建议**把变异对照四层固化为可选 CI 测试（现状是 scratchpad 一次性产物，重构后没法回归）。

### 4. allowed-change 集移动的最小性 — 过

`verify_rng_isolation_pairing.py:126-130, 176`：

```python
_FSP_DERIVED_UNITS: Tuple[str, ...] = (
    "world.buildings.derived_outcomes",
    "world.fragment_states.condition.derived_outcomes",
    "world.fragment_states.repair_assessment.writeback",
)
...
"1b": _FSP_DERIVED_UNITS,
```

- 只把三项 fsp 派生族单元移入「按语义核」集。**没顺带放宽** `world.measurements` / `world.coverage_relations` / `sidecar.entries` / 任何 `proj.*`。注释逐单元写明预期变动内容与「红了意味着什么」（:105-125）。✅
- 与「指标换锚非真值换锚」互证：**代码层结构性成立** — `_evaluate_world_predicate` 对 `non_null`/`value_equals`/`value_in` 一律 `return True`（`regulation_projection_executor.py:921-931`），派生 flag 不进 world predicate 通道，故 `expected_verdict` 结构上对 fsp 翻转不敏感。50 栋实测 0/347 是实施记录转述、未亲跑（工单边界「不跑批」），但代码层证据足够支撑定性结论。✅

### 5. 两处实施中自主决定 — 过

#### 5a. 白名单甲新增条目

`test_slot_alias_normalization_scan.py:120-126`：

```python
("src/workflow_engine/worldgen/generator.py", "_lookup_fragment_fsp_measurement"): (
    "感知层 worldgen 内部（1b / 波次二 #31 新增）：比的是硬编码字面量 "
    "`ratio.fsp.structural_performance`--W1 Step 8 自己刚写出来的**世界侧**量测槽名，"
    "与被比的 `measurement.slot_id` 同源同侧。该名不在别名表键里；且卡侧名按分层单向"
    "红线根本不进入 workflow_engine（该包不 import evo_agent_baseline），"
    "此层结构上不存在卡/世界分叉。"
),
```

格式与既有 8 条白名单甲一致（`(file, function) -> 理由`）。理由是「同侧同名（W1 自产自比）+ 分层单向（workflow_engine 不 import evo_agent_baseline）」，符合白名单甲"结构上不需要归一"的本意 — **不是绕开扫描器**。亲跑 `test_slot_alias_normalization_scan.py` 17 条全过。✅

#### 5b. `index.fsp.estimate` 改有条件写后加显式 `pop`

`:3013-3016`：

```python
if fragment_fsp is not None:
    condition.derived_outcomes.risk_index_values["index.fsp.estimate"] = round(fragment_fsp, 4)
else:
    condition.derived_outcomes.risk_index_values.pop("index.fsp.estimate", None)
```

- **正确性（亲验）**：对 seed=11 下一个无量测片段预填假陈值 999.0，重跑 `populate_derived_flags` 后该键被清成 None — `pop` 实际生效。生产主链 `populate_derived_flags` 在 `generate_world_bundle` 仅调一次（`:3249`），字典初始为空，零行为差异；它守的是纯函数重跑契约（`test_pure_function_no_input_mutation:134-146` 正是重跑场景）。✅
- **副作用**：`.pop(key, None)` 键不存在时不抛错，安全。✅
- **调用面核**：grep `populate_derived_flags(` 在 `src/` 内仅 `generator.py:3249` 一处生产调用（其余都是测试）。✅

### 6. 位移量化口径 — 部分过（代码层过、数字未亲验）

- **判据随输出打印**：`verify_rng_isolation_pairing.py:29-36` 明示「把开关态 / 判据 / 锚定批三项全部当成输出的一部分打印」，`--ctcov`/`--pool-count`/`--pool-seed` 必填无缺省。✅
- **归因「本池无 fsp<0.75 片段」⇒ emergency 翻 0**：池相关，非结构性质。实施记录已明确标注「这是池相关不是结构性质：换一个含 fsp<0.75 片段的池，该分支会翻」（实施记录 :142-143）。引用 0 时带这句即可。✅
- **124/347、反向 0**：实施记录转述，**未亲验**（要跑两个 50 栋池配对，工单边界「不跑批」）。但「反向 0」与断言②（`reverse == []`）同向 — 5 seed × 10 fragment 已亲跑，未出现反向。✅（定量数字未亲验）
- **三个百分比随 `_FSP_FLOOR_PROXY=0.95` 移动**：实施记录标为 DEBT-021 工程口径值，已挂尾巴 3。✅

### 7. 尾巴清单完备性 — 过

实施记录列 5 条尾巴：

1. 下游判定位移未跑批（只证到 flag 变、真值与 sidecar 字节不变，未跑闭包批）— 真欠账。
2. 含 fsp<0.75 片段的池上传导链形态未验（本池该链全程 0）— 真欠账。
3. `_FSP_FLOOR_PROXY=0.95` 无 spec 锚（DEBT-021）— 真欠账。
4. spec 08 §5 projection executor 读派生旗标、实测 W2 零消费（spec-code 缺口）— 真欠账，已台账另立。
5. 本单未提交 — 提交时机归用户。

**未发现明显漏报**。可补一条小尾巴（建议级）：变异对照四层红只在 scratchpad 跑、未固化为 CI 测试 — 重构后没法回归。

---

## 分级汇总

| 级别 | 项 |
|---|---|
| **必须修** | 无 |
| **建议** | ① 变异对照四层红固化为可选 CI 测试（现状是 scratchpad 一次性产物）；② 测试断言②对照「复刻的旧公式」而非直接对 HEAD 产物 — 这是合理等价形式，但若旧码细节被忘掉、复刻逻辑漂移会让断言②失真，建议在 test docstring 里保留旧码出处引用（现状已有 :69-78，OK）。|
| **存疑** | ① 124/347、emergency 翻 0、proj 全绿 0/347 — 均实施记录转述、未亲跑 50 栋池；工单边界「不跑批」故未亲验。代码层结构性证据（`_evaluate_world_predicate` 对所有 assertion 一律 True）已支撑「指标换锚」定性结论。|

---

## 总判

**过**。七项审核点全部成立：前置清点机器可验、接线逐行核通过、单向性断言带非空护栏、allowed-change 集最小性 + 代码层互证、两处自主决定均合理（白名单甲格式对、pop 亲验生效）、尾巴清单完备。

「必须修」零项；「建议」与「存疑」均为可在后续工单处置的非阻塞项。1b 可进入提交阶段（提交时机归用户）。
