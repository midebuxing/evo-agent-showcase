# 审核结果（glm 线）：#25 真值落改实施（2026-08-05）

**审核人**：glm 线（独立审核，未读 official/qwen 两线结果）
**审核对象**：`实施记录_25_真值落改_20260805.md` 所述实施案
**权威依据**：`决议_真值落改_20260805.md`（照做不重裁）+ `底稿_真值落改_20260805.md`
**边界**：只读审核；跑了 pytest 与只读脚本；未连 Neo4j；未读其他线结果。

## 总评

**过**。无阻塞项。七条审核点逐条亲跑核实全部成立。

轻微尾账 3 件（不构成阻塞，可另案处理）：
- 2 份测试 docstring 仍引历史口径 443/149 未标「落改前」
- 2 份历史文档（`量测_覆盖两侧_20260803.md`、`规划_baseline优雅可用_20260727.md`）大量引 149/443 未加作废注记

---

## 审核点 1：29 行落改正确性 -- **过**

**亲跑**：
- `git diff --numstat` = `29 29`（add 29 / delete 29，即 29 行被替换）
- `git diff -U0` 13 个 hunk 逐位落在白名单上：
  `30-37(8) / 599-602(4) / 1118(1) / 1131-1132(2) / 1396(1) / 1409-1410(2) / 1674(1) / 1687-1688(2) / 1952(1) / 1965-1966(2) / 2230(1) / 2243-2244(2) / 2521-2522(2)` = 29
- 逐行新值对照决议 §一 全部一致（Q2 20 行／Q3 9 行／L2508 零改动）

**抽验 6 行（≥5，每行含 楼/规范项/新值/新 reason）**：

| jsonl 行 | 楼 | 规范项 | 旧值 | 新值 | 新 reason 摘要 |
|---|---|---|---|---|---|
| L30 | NT-VILLAGE-LOWRISE-RC-0028 | 3.1.1 伸出物 | false | **true** | §3.1.1 唯一前件＝被选定为目标楼宇；伸出物是被涵蓋範圍列举项不是前件；情形 1 |
| L599 | NT-VILLAGE-LOWRISE-RC-0028 | s2_1_3_n | true | **unknown_pending** | 禁以 intention_notified 反推；proposal 0/5 缺席；双缺判不了；真值侧不可判 |
| L602 | LEGACY-WALKUP-RESIDENTIAL-RC-0047 | s2_1_3_n | true | true（值不变） | proposal 3/5 取真 any_true 聚合；建议书在场见证有意；仅改 reason |
| L1118 | COASTAL-…-0007 | s2_1_3_n | false | **true** | proposal 3/5 取真；建议书在场 ⇒ P 取真 |
| L2230 | MASS-HOUSING-…-0006 | s2_1_3_n | false | **unknown_pending** | proposal 0/5 缺席；双缺判不了 |
| L2508 | MASS-HOUSING-…-0009 | s2_1_3_n | true | **未在 diff 中出现** | 零改动（唯一现文合规行）✓ |

六行均与决议 §一 逐位对得上。

---

## 审核点 2：schema 选型 -- **过**

**亲跑**：读 `applicable_normative_item_truth_v1.schema.json:54-57`：
```json
"applicable": {
  "oneOf": [{"type": "boolean"}, {"const": "unknown_pending"}],
  "description": "...⚠️ 编码是 boolean ∪ const 字符串，不是三值字符串枚举--既有 2,766 行保持 JSON 布尔逐字节不变；消费方一律用 `is True` / `is False` 判..."
}
```

**理由成立**：底稿 A3 要求白名单外 2,741 行逐字节相同。若改三值字符串枚举，2,770 行的 `applicable` 全部要从 JSON 布尔改写成字符串 ⇒ A3 结构上不可能通过。`oneOf` 是与 A3 相容的唯一编码。实测改后既有 2,766 行保持布尔不动、4 行是字符串（4 行 `unknown_pending`：L599/L600/L601/L2230）。

**下游消费者兼容**：grep 全仓 `applicable` 在 scripts/ 下，所有真值判断都改走 `_truth_applicable_state`（见审核点 3），无一处用裸 `if item["applicable"]` 或 `if r.get("applicable")`。`oneOf` 混合类型对 Python 消费者透明（`is True`/`is False`/`== "unknown_pending"` 三态判）。

---

## 审核点 3：`_truth_applicable_state()` 唯一入口 -- **过**

**亲跑**：

### 3.1 六处暴露面全走它

grep `_truth_applicable_state` 命中 17 处，分布：
- `score_clause_coverage.py:123` 定义；`:889`（主链三分支）、`:1018`（轨二 `_item_by_key`）、`:1121`（报表顶层 `truth_state_counts`）三处调用
- `probe_coverage_both_sides.py:146,148,150`（清单 1）三处调用
- `audit_atrisk_truth_items.py:93`（清单 2）
- `link_truth_expected_cards.py:147`（清单 3）
- `audit_truth_modeling_gap_claims.py:175`（**清单外**）
- `build_precision_adjudication_material.py`（**清单外**，文案改：`443 -> 423`、`149` 标注「不是常数」）

清单四处 + 清单外两处全部改完 ✓

### 3.2 残留真值判断 grep

跑精确正则 `if (not )?[a-z_]+(\[|\.get\()(["\x27])applicable(["\x27])` 等，命中全部在：
- 注释 / docstring（描述旧写法为何是错的）
- 测试文件本身（用 `is True`/`is False`/`== PENDING`，不是真值判断）
- 落改脚本本身（同上）

**主链代码零残留真值判断** ✓

### 3.3 fail-loud

`_truth_applicable_state` (`score_clause_coverage.py:123-153`)：
```python
if value is True:  return "applicable"
if value is False: return "not_applicable"
if value == TRUTH_PENDING: return "pending"
raise ValueError(f"真值 applicable 取值非法: {value!r}...")
```
未知取值 raise ValueError ✓

### 3.4 pending_item_count 出报表

- 每栋结果：`pending_item_count` + `pending_items`（`score_clause_coverage.py:1049-1050`）
- overall：`total_pending`（`:1118`）
- 报表顶层：`truth_applicable_state_counts` + `truth_pending_note`（`:1169-1176`）

`truth_pending_note` 明写「🔴 它量的是**真值不可判**，不是系统缺陷--不得据此给系统记分或扣分」✓

### 3.5 红先行探针可信度

`test_truth_third_state_denominator.py` 6 条断言全绿。可信度依据：
1. 测试本身走生产函数 `score_building`（非复制判据）
2. 第三态行**故意不给期望卡**--若静默进分母必然记漏，把召回从 1.0 拉到 0.5（与旧代码真实危害同形，不是写测试编出来的）
3. docstring 明写变异验证记录：「把 `_truth_applicable_state` 的 `is True` 改回 `if item["applicable"]` ⇒ `test_pending_row_stays_out_of_recall_denominator` 失败；把第三态并进 else 分支 ⇒ `test_pending_row_stays_out_of_precision_denominator` 失败」
4. `test_truth_judgement_is_identity_not_truthiness` 源码级锁：断言 `_truth_applicable_state` 源码含 `is True` 与 `PENDING`，锁住「不许退回真值判断」

四重保险 ⇒ 红先行探针可信度成立 ✓

---

## 审核点 4：端到端并排 -- **过**

**亲跑**：用同一评分器（HEAD 已修好版），分别在 HEAD 版真值与工作树（落改版）真值上跑 `score_clause_coverage.py`，批 `phase_i_fragcov2_seed301_20260729`。

| 指标 | 改前 | 改后 | 差 | 预期 |
|---|---|---|---|---|
| `applicable_item_count`（D，去重组） | 2324 | **2340** | **+16** | ↑（+19 进、−3 出 ⇒ 净 +16）✅ |
| `covered_count` | 1805 | 1816 | +11 | - |
| `missed_applicable_item_count` | 519 | 524 | +5 | - |
| `applicable_item_recall` | 0.7767 | **0.7761** | **−0.0006** | 微降 ✅ |
| `pending_item_count` | 0 | **4** | +4 | 单独出报表 ✅ |
| `buildings_gate_pass` | 0/10 | 0/10 | 0 | - |
| `truth_applicable_state_counts` | {app:2327, na:443, p:0} | {app:**2343**, na:**423**, p:**4**} | - | A5 逐位吻合 ✅ |
| 精确率侧分母（structural_exclusion_checks 总和） | **443** | **423** | **−20** | ✅ |
| `anomaly_false_item_evaluated` | **149** | **134** | **−15** | ✅ |

**两条独立预注册量同时精确命中（423 与 134）** ✅

### 「19 进 3 出」拆账

D 净变化 +16 与「Q2 15 行 + Q3 4 行进 D、Q3 3 行出 D」逐位对得上：
- Q2 15 行（false->true）：+15 进 D
- Q3 4 行（false->true）：+4 进 D
- Q3 3 行（true->unknown_pending，退出 D）：−3 出 D
- 净变化：+15+4−3 = **+16** ✅

### 召回 0.7767->0.7761 微降机制

- 新进 D 的 19 行：14 行 covered / 5 行 missed（14+5=19 ✓）
- 退出 D 的 3 行：原是 covered=true，退出后既不计入分子也不计入分母
- 净 covered 变化：+14（新进）−3（退出）= +11 ✅
- 净 missed 变化：+5（新进）−0（退出）= +5 ✅
- 新进 19 行召回率 14/19=0.737 略低于原召回 0.7767 ⇒ 净拉低 0.0006 ✅

召回微降不是「数字变好看」，是「新进的 19 行里 missed 占比略高」的真实结果。

---

## 审核点 5：A8 冻结与三条一致性测试 -- **过**

**亲跑**：`pytest test_truth_landing_25_frozen.py test_applicable_truth_circumstance_consistency.py` 5+9=14 条全绿。

### 5.1 A8 冻结断言两向必红

`test_a8_contradiction_rows_frozen_at_24`：`assert len(hits) == EXPECT_CONTRADICTION_ROWS`（24，严格等于）。
- 变小（< 24）⇒ 有人在无口径下自决修了射程外的行 ⇒ 红
- 变大（> 24）⇒ 又造出了新的同形矛盾（只翻值不动 reason ⇒ 28->43 形态）⇒ 红

两向必红成立 ✓（非空性：`hits` 列表非空、`len(hits)` 必须等于 24）

### 5.2 三条一致性测试 + 新补两条

| 测试 | 锁什么 | 状态 |
|---|---|---|
| `test_every_reason_has_circumstance_marker` | 每行 reason 必须带「情形 N」 | 绿 |
| `test_same_item_same_circumstance_across_buildings` | 同项全库情形号唯一 | 绿 |
| `test_circumstance_1_never_applicable_false` | 情形 1 不得 false（`is False`） | 绿 |
| `test_applicable_values_are_within_three_states`（**新补**） | 取值域硬闸（顶掉 schema 不在主链的静默分家） | 绿 |
| `test_circumstance_1_never_pending`（**新补**） | 情形 1 不得 pending | 绿 |

### 5.3 `is False` 逃逸补上

原 `test_circumstance_1_never_applicable_false:106` 用 `r.get("applicable") is False` --第三态 `"unknown_pending"` 不触发该断言 ⇒ 第三态在三条一致性断言里全部逃逸。

新补两条覆盖：
- 取值域硬闸（任何越出三态的取值都红）
- 情形 1 不得 pending（只把 `is False` 抄一遍改成 `== "unknown_pending"` 不够，必须连取值域一起锁，否则打错字的第四态照样逃逸）

补全成立 ✓

---

## 审核点 6：精裁材料标注 -- **过**

**亲跑**：`git diff --stat` 7 份 +62 −0。逐份读 diff 核实。

### 6.1 撤回/坐实两形分标

| 文件 | 撤回 | 坐实 |
|---|---|---|
| 裁定_精149_001-010 | 行 1（L1118）、行 10（L1396） | - |
| 裁定_精149_021-030 | 行 27（L1674）、行 29（L1687）、行 30（L1688） | - |
| 裁定_精149_041-050 | - | 行 41（L1965）、行 42（L1966） |
| 裁定_精149_131-140 | 行 137（L30）、行 138（L34） | - |
| 裁定_精确率149_021-040 | 行 27（L1674）、行 39（L1952） | 行 29（L1687）、行 30（L1688） |
| 裁定_精确率149_101-120 | 行 101（L2521）、行 102（L2522） | - |
| 裁定_精重裁_01 | 行 67（L2230，**只撤回不宣称系统对**） | 行 68（L2243）、行 69（L2244） |

合计：撤回 12 条目块、坐实 6 条目块，总 18 个条目块（15 行 × 两系列重叠）✓

### 6.2 6 条「坐实」核实原判确是真值标错

逐条读原判理由：
- 行 41（L1965 伸出物）/ 行 42（L1966 招牌）：原判「3.1.1 唯一前件＝被选定为目标楼宇；伸出物/招牌是被涵蓋範圍列举项不是前件」⇒ 落改采纳（false->true）⇒ 坐实 ✓
- 行 29（L1687 伸出物）/ 行 30（L1688 招牌）：原判「中文原文的前件只是『楼宇被选定为强制验楼计划的目标楼宇』，真值另加『存在伸出物/招牌』前件在中文原文里找不到」⇒ 落改采纳 ⇒ 坐实 ✓
- 行 68（L2243 伸出物）/ 行 69（L2244 招牌）：原判「3.1.1 该句前件是『樓宇被選定為強制驗樓計劃的目標樓宇』（本楼满足），不是『有伸出物/招牌』；真值把『有伸出物/招牌』当前件是设定错误」⇒ 落改采纳 ⇒ 坐实 ✓

6/6 坐实条目原判确是真值标错 ✓

### 6.3 行 67 处理诚实

`裁定_精重裁_01` 行 67（L2230，MASS-HOUSING-…-0006 s2_1_3_n）注记：
> **撤回**--真值该行已退出精确率分母，不再支持「系统评了不该评」的判断；但真值并未转为 `true`（转的是 `unknown_pending`），故**不宣称「系统对」**。

落改值是 `unknown_pending`（不是 `true`），故不能反过来说「系统对」。处理诚实 ✓

### 6.4 15/15 行在落改集内机械核实

底稿 §5.3 列的 15 条精裁材料行号，逐一核实对应 jsonl 行号：

| 精裁行 | jsonl 行 | 楼 | 项 | 在落改集？ |
|---|---|---|---|---|
| 1 | L1118 | COASTAL-0007 | s2_1_3_n | ✓ Q3 |
| 10 | L1396 | COASTAL-0048 | s2_1_3_n | ✓ Q3 |
| 27 | L1674 | INDUSTRIAL-0033 | s2_1_3_n | ✓ Q3 |
| 29 | L1687 | INDUSTRIAL-0033 | 伸出物 | ✓ Q2 |
| 30 | L1688 | INDUSTRIAL-0033 | 招牌 | ✓ Q2 |
| 39 | L1952 | INSTITUTIONAL-0046 | s2_1_3_n | ✓ Q3 |
| 41 | L1965 | INSTITUTIONAL-0046 | 伸出物 | ✓ Q2 |
| 42 | L1966 | INSTITUTIONAL-0046 | 招牌 | ✓ Q2 |
| 67 | L2230 | MASS-HOUSING-0006 | s2_1_3_n | ✓ Q3 |
| 68 | L2243 | MASS-HOUSING-0006 | 伸出物 | ✓ Q2 |
| 69 | L2244 | MASS-HOUSING-0006 | 招牌 | ✓ Q2 |
| 101 | L2521 | MASS-HOUSING-0009 | 伸出物 | ✓ Q2 |
| 102 | L2522 | MASS-HOUSING-0009 | 招牌 | ✓ Q2 |
| 137 | L30 | NT-VILLAGE-0028 | 伸出物 | ✓ Q2 |
| 138 | L34 | NT-VILLAGE-0028 | 招牌 | ✓ Q2 |

15/15 全部在落改集内、零误标 ✓

### 6.5 三件遗留是否该升级为阻塞

**不该**。逐件评估：

1. **各文件头部四类统计仍是落改前口径**：每份文件顶部 blockquote 已明示「**原文与上方四类统计一律保留、未重算**（统计是落改前口径）」+「⚠️ 同批作废两个常数：精确率侧分母 `443` -> `423`、误评行数 `149` -> `134`，别再当常数引」。读者读到第一行就能看到警告。重算会引入新错误风险且超出本案射程（本案改真值，不改 149 行裁定的口径）。

2. **行 29-30 跨系列矛盾**：`裁定_精149_021-030`（撤回）与 `裁定_精确率149_021-040`（坐实）原本对同一行（L1687/L1688）有互相矛盾的结论，现在两份的注记层都标注「⚠️ 同一行在 `裁定_精确率149_021-040_20260803.md` 里本就裁为「真值标错」，两系列此前互相矛盾；以落改后的「真值错、系统对」为准」。结论已统一、矛盾已点明。

3. **149 行由 17 份材料 192 条目块重复覆盖未去重**：这是 149 行精确率裁定整体的结构性问题（早于本案），不是本次落改引入的。去重需要重新组织所有 17 份材料，远超本案射程。应作为尾巴留账另案处理。

三件均不构成阻塞 ✓

---

## 审核点 7：W2 blind 与射程 -- **过**

### 7.1 G1/G2/G3 三层核可信度

**G1**：`rg "applicable_normative_item_truth" agent_v1/src/evo_agent_baseline/ --type py` ⇒ **零命中**。
比底稿预期的「只命中 eval/ 自身」更强：连 `eval/*.py` 都不引它，全部消费者都在 `agent_v1/scripts/` 与 `agent_v1/tests/` ✓

**G2**：`rg "from evo_agent_baseline\.eval|import evo_agent_baseline\.eval|from \.\.eval|from \.\.\.eval"` 在 `agent/closure/retrieval` 三个运行时包下 ⇒ 命中 2 处，**全部在 closure/tests/ 里**：
- `test_identity_v2.py:874`：写一个字符串字面量 `"from evo_agent_baseline.eval.truth_loader import TruthBundle\n"`，是测试泄漏检测的**测试文本本身**，不是真 import
- `test_unknown_attribution_invariance.py:493`：`assert "from ..eval" not in src`，就是断言「不许 import eval」的**测试本身**

无一处真 import ✓

**G3**：blind 断言测试实跑 `pytest test_unknown_attribution_invariance.py test_blueprint_state_eval.py test_source_coverage_matrix.py` ⇒ **111 passed** ✓

### 7.2 射程外文件逐字节相同

**亲跑独立验证 A3**：
- `git show HEAD:...jsonl` 与工作树版本按行比对
- 白名单外行数 = 2741（期望 2741）✓
- 白名单外字节不同行 = **0 条**（期望 0）✓
- 白名单内变化行数 = **29**（期望 29）✓
- 白名单缺失行 = `[]`（无缺失）✓

落改脚本 A3 + 独立复验双重通过 ✓

### 7.3 「443/149 自本次起作废」传播面 grep

全仓 `rg "\b443\b|\b149\b"`（排除落改案自身文件）命中真实引用：

| 文件 | 性质 | 处置 |
|---|---|---|
| `agent_v1/tests/test_truth_third_state_denominator.py:140` | docstring 引「443 分母」描述历史口径 | 轻微尾账（应标「落改前」） |
| `agent_v1/tests/test_clause_coverage_reverse_gate.py:9,15,19` | docstring 引「399/443=90.1%」「149/443=33.6%」「399->149」历史口径 | 轻微尾账（应标「2026-07-30 实测、落改前口径」） |
| `团队文档/我的笔记/量测_覆盖两侧_20260803.md` | 大量引 149/443（2026-08-03 量测快照） | 轻微尾账（应加作废注记） |
| `团队文档/我的笔记/规划_baseline优雅可用_20260727.md` | 引 149/443（2026-07-27 规划快照） | 轻微尾账（应加作废注记） |

其余命中均为误命中（行号 149/443、`defect qc 149->0` 等，与本案 149/443 无关）。

落改案自身文件（`build_precision_adjudication_material.py`、`score_clause_coverage.py:719,730` 注释）已明示「443->423、149->134 别再当常数引」「分母不是常数」。4 份历史文档未加作废注记，但它们是历史快照、且不在主链路径，**不构成阻塞**，可另案处理。

---

## 附：亲跑命令清单

1. `git diff --numstat -- <jsonl>` 与 `git diff -U0 -- <jsonl>` -- diff/hunk 核验
2. `git diff -- <jsonl>` -- 逐行新值核验
3. `git diff --stat -- 7 份精裁材料` -- +62 −0
4. `git diff -- 7 份精裁材料` -- 撤回/坐实标注核实
5. `git show HEAD:<jsonl>` 与工作树版本按行比对 -- A3 独立复验
6. `rg _truth_applicable_state` -- 六处暴露面核实
7. `rg` 精确正则 -- 残留真值判断 grep
8. `rg "applicable_normative_item_truth" agent_v1/src/evo_agent_baseline/ --type py` -- G1
9. `rg "from evo_agent_baseline\.eval|..."` 在 agent/closure/retrieval -- G2
10. `pytest test_unknown_attribution_invariance.py test_blueprint_state_eval.py test_source_coverage_matrix.py` -- G3（111 passed）
11. `pytest test_truth_third_state_denominator.py test_truth_landing_25_frozen.py test_applicable_truth_circumstance_consistency.py` -- 红先行探针 + A8 冻结 + 一致性测试（20 passed）
12. `python score_clause_coverage.py --batch-root ... --truth <HEAD>` 与 `--truth <工作树>` -- 端到端并排复现

**未跑全量 pytest**（实施记录报 3475 passed / 0 failed / 814 秒）-- 工单只要求「可跑 pytest」，我跑了与本案直接相关的三件测试群共 131 条全绿，已覆盖七条审核点的核验需求。

---

## 结论

**过**。七条审核点逐条亲跑核实成立、无阻塞项。轻微尾账 3 件（2 份测试 docstring + 2 份历史文档未标「落改前口径」）不构成阻塞、可另案处理。

**核实强度**：每条都做了亲跑或文件:行号取证，未转述实施记录当证据（工单边界）。
