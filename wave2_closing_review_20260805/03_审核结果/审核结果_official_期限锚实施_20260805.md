# 审核结果（official 线）：期限锚案实施（2026-08-05）

**性质**：审核门（代码对不对）＋乙12 分叉表态。只读审核，未改仓库任何文件，未连 Neo4j。
**审核对象**：工作树相对 HEAD `8c386907` ＋ 备份 patch `杂物箱/备份_期限锚_20260805.patch` 的**净增量**
（我用 `git worktree` 在 scratchpad 复原了「动手前」的树，再逐文件 diff，把本单改动与工作树里
其它未提交工作分离出来，故下文所有行号与判断都只针对本单）。

---

## 总裁定：**有条件通过**

代码本体正确、可落。**四项必须修**（其中一项是验收判据本身的缺陷，见 §审核点 5）。
判定侧与世界侧的核心机制我全部独立复现通过；被我否掉的不是实现，是**验收判据的射程**
与**三处陈述的口径**。

---

## 一、我实际亲跑了什么（不转述实施记录）

| # | 动作 | 结果 |
|---|---|---|
| A | 复原「动手前」树（HEAD＋备份 patch），逐文件 diff 隔离本单净增量 | 4 个源文件 ＋ 2 个既有测试 ＋ 3 个新文件 |
| B | 新测试 41 例实跑 | 41 passed |
| C | **红先行复验**：把 B1/B2 用例搬进「动手前」树跑 | 6 failed / 1 passed —— HEAD 确实红（详下，但红的**原因**与记录所写不同）|
| D | **R1-only 回滚复验**：保留绑定通道、只把 `same_day_as` 分支还原成 `_canon_truthy` | 7 failed，且逐条复现「0 判 violated / 1 判 satisfied / 2·3 落 open」——记录所述反转属实 |
| E | **层一独立配对**：两侧各自生成池（count=8 / seed=301 / ctcov=on / fragment=4），跑 `--step deadline` | **PASS**，20 个既有单元逐字节一致 |
| F | 冻结批 `wave1_closing_seed401_20260804` 30 栋实数 | 义务总数 **272,122**、`kind=deadline` **603**、按锚点分组 15 行**逐行等于**记录的「义务数」列 |
| G | 冻结批 fact_pack 扫描 | 1,393 条事实，带 `provenance.time_anchor_key` 的 **0 条** |
| H | 冻结批义务作用域实数 | #8 的 93 条**全部碎片作用域**、#7 的 14 条碎片作用域，其余 13 个锚全部楼级——与记录 §四.3 逐条吻合 |
| I | 锚点册 / 别名表 / 卡侧 deadline 关系表实取比对 | 见审核点 2/4 |
| J | **乙12 两方案实跑对照**（构造 FactPack 直调 `_bind_deadline_fact`） | 见 §乙12 表态 |
| K | 全量 pytest（`workflow_engine`＋`evo_agent_baseline`＋`agent_v1/tests`） | 见 §附录 |

---

## 二、七个审核点逐条

### 审核点 1：R1 修复面 —— **过**（附一条口径订正）

- **只动 `same_day_as` 分支**：✅ 隔离 diff 显示 `evaluate_deadline` 里 `within`/`before` 两个分支
  （`obligation_deriver.py:2154-2183`）逐字节未动，只有 `:2185-2224` 换了通道。
- **`_canon_truthy` 本体零触碰**：✅ 亲跑对比「动手前」与现树的 `:415-450`（含函数定义体）**完全相同**；
  消费点由 6 处降为 5 处（`:1559 / :1975 / :2394 / :3102 / :3260`），减掉的正是 deadline 那一处。
  记录写的「另有 5 个跨 kind 消费点」在**改后**为真。
- **浮点/整数同形**：✅ `obligation_deriver.py:2199-2200` 用
  `isinstance(observed,(int,float)) and not isinstance(observed,bool)` → `float(observed)==0.0`，
  布尔被显式排除；用例 `test_deadline_anchor_supply.py:117` `[0, 0.0]` 与 `:135` `[1, 1.0, 2, 3.0]` 覆盖两型。
- **红先行是否真实**：✅ **HEAD 确实红**（动作 C：6/6 失败）。

🔴 **但必须订正一处口径**（实施记录 §二 层二 B1/B2 那一句）：
记录写「实测全红（**0 判 violated、1 判 satisfied、2/3 落 open/missing_time_anchor
——语义反转逐条复现**）」。**在纯 HEAD 上这句不成立**：我实测 6 条**全部**失败在
`assert obl.closure_status == 'open' != 'closed'`，即**全部堵在绑定失败**（provenance 通道
当时还不存在，用例喂的带锚事实在老通道下必 miss），一条都没走到比较分支，**语义反转一次都没被复现**。
只有在动作 D 的配置（绑定通道已在、只回滚 R1）下才逐条复现出记录描述的那组读数。
⇒ 红先行作为**门**是过的；但**「红的原因」被写成了另一件事**。记录须写明测的是哪一个配置。
附带：`test_b1_same_day_boolean_is_not_a_verdict` 在纯 HEAD 上**是绿的**（也是因为绑定先失败），
在动作 D 配置下才红——典型的「测试跑在缺陷不可能显现的输入上」，值得在记录里点名。

### 审核点 2：禁供钉死 —— **过**，但「双向」措辞不成立，须订正

- **三锚成员对**：✅ 亲取锚点册 19 条，`appointment.representative.supervision.made` /
  `investigation.detailed.commencement` / `repair.prescribed.started` 三条**全部在册**
  （`obligation_deriver.py:2062-2068`；用例 `test_deadline_anchor_supply.py:277-282` 已把这条钉死）。
- **#13 措辞在**：✅ `obligation_deriver.py:2050` 逐字有「**本单禁供 ＋ 供路待「提前量」语义裁定**」，
  并写清了 `>=` vs `before` 分支写死 `<=` 的方向相反。
- **禁供闸的实际效力**：✅ 我亲取卡侧 25 条 deadline，**`relation=="before"` 的恰好只有这三个锚**
  （#13 offset=7 day；#14/#15 offset=None）。⇒「R2 本单不修、由禁供闸挡住」这条**结构上成立**，
  不是靠运气。

🔴 **「判定侧＋世界侧双向断言」有一半是同一面测两遍**：
`test_deadline_anchor_supply.py:286-301`（判定侧文件里的世界侧断言）与
`test_deadline_anchor_emission.py:102-113`，两者都是
`_build_registry_bundle()` → 遍历全部 registries → 读 `rule_card_threshold.time_anchor_key` → 断言不含丙类，
**同一张表、同一个字段、同一个谓词**。⇒ `test_deadline_anchor_supply.py:311-313` 自述的
「由 … 与 … **双向钉死**」不成立。
✅ 真正的第二面**确实存在**，只是不是这两条：`test_b4_forbidden_anchors_stay_unknown`
（`:304-329`，打求值器闸）＋反向对照 `test_b4_forbidden_gate_is_the_only_difference`（`:332-348`，
防「provenance 通道整个不工作」廉价通过）。这一对是有效的，故**禁供本身有效**，
要修的是**测试 docstring 里那句不实的自述**。

### 审核点 3：绑定通道 —— **过**

- **级 0 优先级**：✅ `obligation_deriver.py:2276-2298`——先查 `fact_index.time_anchor_index`，
  命中恰 1 条即 `return anchored[0], None`；0 条才落到 `:2300` 起的 canonical slot/measure 老通道。
  `fact_binding.py:201-205` 建索引时**不过别名归一**（锚点命名空间与槽名空间分开，正确）。
- **碰撞策略 0/1/>1 三态**：✅ `:2293-2298`；`>1` → `(None,"ambiguous")` → `:2123-2134` 落
  `blocked / ambiguous_fact_binding`，**新通道里没有任何 `facts[0]`**。
- **既有调用面零漂移**：✅ 全仓 grep，`_bind_deadline_fact` 的**生产调用点只有一个**
  （`obligation_deriver.py:2122`），其余全在测试里。老通道的三处 `return` 只是加了 `, None`，
  语义不变；provenance 索引在存量数据上恒空（动作 G：冻结批 1,393 条事实**零**带锚），
  ⇒ 不注入时行为逐位相同。
- **绑定成功回写**：✅ `test_deadline_anchor_supply.py:249-262`。

⚠️ 诚实边界（记录 §五.3 已声明，我复核属实）：**老 slot/measure 通道仍是 `return facts[0]` 任取**
（`obligation_deriver.py:2309-2315`）。记录给的不堵理由我实测坐实：冻结批里
`repair.prescribed.started`（3 条/栋）与 `repair.prescribed.completed`（1 条/栋）**确实带 `evidence_fact_ids`**、
notes 是 `relation=… needs duration/timestamp; observed … unusable`——即今天经别名桥绑着布尔门行，
堵老通道会改动这 159 条的证据链，与层三零扰动判据直接冲突。**理由成立**，但这条 `facts[0]` 是留在仓里的债，
须进台账（不是本单必修）。

### 审核点 4：世界侧接线 —— **过**，一处引证硬错须修

- **8 条形状对齐**：✅ 亲跑取 19 条记录逐字段对比：新 8 条相对既有 duration 条目
  **多 `granularity`、少 `recommended_mean`/`recommended_sigma`**，其余字段全齐。
  少的两个不是缺陷——`generator._has_typical_distribution` 对 `uniform` 只要 `typical_bounds`
  （既有 11 条全是 normal 家族故必带 mean/sigma），实跑 8 条全部走 PathA、样本落 `typical_bounds` 内，
  没有一条掉进中点兜底。
- **`granularity:"building"` 射程**：✅ 亲跑——`sidecar_measurement_registry` 19 条里
  `granularity=="building"` **恰好是这 8 条，此前 0 条**（既有 11 条连该键都没有 ⇒ 默认 fragment ⇒
  `sidecar.py:279` 那条 `continue` 对它们逐字节无影响）。
  ⚠️ 近邻风险已排除：`sidecar_bool_slot_registry` 里有 18 条 `granularity=="building"`，但
  `sidecar.py:197` 的 `_collect_sidecar_measurement_slots` 只认 measurement 表，射程正确。
- **零 `aggregation` 标记**：✅ 8 条整条序列化后无任何 `aggreg` 子串。
- **楼级发射步骤真独立追加 / `seq_no` 零漂移**：✅ **结构＋实测双验**。结构：`building_buckets` 的三个
  写入点依次是 `_sample_sidecar_bool_slots_for_building`（`sidecar.py:1472`）→
  `_emit_scope_declaration_rows`（`:1517`）→ 新步骤（`:1522`），新步骤**排在最后**。
  实测（动作 E）：细尺按 `(runtime_id, entry_type, seq_no)` 对行，**基线 3,174 行零丢失、非锚点列零漂移**。
- **回填只加一列、值零变化**：✅ 同上，细尺把「`time_anchor_key` 只许 null→非 null 且只许落在
  `duration.delivery.deadline.{to_ba,to_person}`」写成判据（`verify_rng_isolation_pairing.py:207-213, 576-586`），
  第三个槽被回填会当场红。实测回填 114 行、全在白名单两槽。
- **槽名冲突**：✅ 亲跑——8 个新 slot_id 与 `slot_aliases`(16) / `measure_aliases`(4) 的**任何键、任何值**
  零相撞，对 `projection_runtime_mapping_v1.json` 裸子串扫描出现次数均为 0；**零个以 `procedure.` 开头**。
- **零 `procedure.*` 数值注入**：✅ 实测新增行只落在 8 个新 `duration.*` 槽（动作 E 输出）。
  ⚠️ 补一条记录没说的：8 条的 `carrier_domain` 都是 `procedure` ⇒ 行落进 `procedure_gate_state` **桶**。
  这不是新行为——既有 5 个 duration 槽同样是 `carrier_domain=procedure`，先例在。
- **R5 对齐**：✅ 亲取锚点册：`repair.completion_report.submitted_to_ba` **在册**、
  `repair.completion_report_and_mbi4.submitted_to_ba` **不在册**；`registry.py` 全部 10 个 distinct
  `time_anchor_key` 全在册、零孤儿。`unit` 由 `calendar_day` 改 `day` 与卡侧一致。

🔴 **硬错（必须修）**：`registry.py:2990` 的引证 `§2.1.3(i) L849` **行号是错的**——
`MBIS_CoP_2023.md` 的 L849 是空行，§2.1.3(i) 正文在 **L858**。同一批里 #9/#10 的 `L1861`/`L1865`
我实取核对**精确正确**，说明是单点笔误、不是系统性偏移。但按 CLAUDE.md「页码/行号须去法规原文实取，
编一个就是造假来源」这条，它得改。
✅ 其余 11 处中文引文我逐条比对原文（L858/860/862/864/871/873/1861/1865）**逐字一致、条款号全对**。

⚠️ 两条不是错但须记：
1. **#9 引文丢了前件**。§6.4.4 全句是「**如樓宇業主委任另一名註冊檢驗人員監督樓宇修葺工程**，
   獲委任的註冊檢驗人員須…」，注释从「獲委任的…」起引——丢掉的正是这条义务的触发前件。
   与 CLAUDE.md 记的「引出句丢失」同形。
2. **两个 `carrier_slot` 声明了但世界永不产出**：`procedure.repair_supervising_ri.appointment.completed`（#9）
   与 `procedure.repair.revision_proposal.submitted_to_ba`（#11）在 `ownership_map` 有、
   在 `sidecar_bool_slot_registry`（52 条实采清单）**没有**（我亲跑逐条查，其余 6 个双表齐备）。
   ⇒ `registry.py:3194-3196` 那句「#11 世界侧原本没有承载槽（**只有布尔门**
   `procedure.repair.revision_proposal.submitted_to_ba`）」**把一个不产出的东西陈述成存在的**，须改写。

### 审核点 5：验收三层可信度 —— **不过**（层一/层三过，层二 B6 判据射程不足）

**层一：✅ 独立复现 PASS。** 我自己各生成一侧池跑细尺，20 个既有单元逐字节一致，
只有 `world.meta` / `sidecar.entries` / `proj.cohort_manifest` 变，与记录的判据集一致；
新增行 = 8 栋 × 8 槽 = 64 行，**逐（楼,槽）恰 1 行**（这同时是 B6 在**池级**的独立佐证，比单测强）。
`registry_bundle_hash` 76caf0af→d6e0eb01、`deterministic_key` 6b984a3c→3a0a8013
——**本单确实是换池级改动**，记录 §二那段说明成立。
⚠️ 口径缺一项：记录只写「count=50 / seed=301 / ctcov=on」，**没写 `fragment_count_per_building`**。
我用 4 得到每栋 ~7 行 `.to_person`，记录的池是每栋 4 行 ⇒ 两池的碎片规模不同。
按脚本自己的「判据打印通则」，这一项也该进锚定批，否则层一池不可精确重放。

**层三：✅ 我用另一条路独立坐实。** 原复算脚本已随 scratchpad 删除、不可重跑，但结论可结构化证明：
①动作 F 实数冻结批 `kind=deadline` **603 条、全部 `open/unknown/missing_time_anchor`**；
②动作 G 实测该批 fact_pack **零条**带 `provenance.time_anchor_key`
⇒ provenance 索引全空 ⇒ 新通道惰性；且 603 条全堵在绑定前，**R1 改的比较分支一次都到不了**
⇒ 「不注入时零扰动」是**必然**而不只是观测。③义务总数我实数 = **272,122**，与层三③ base 一致。

**层二：🔴 不过。** B1-B5 断言真实非空、我已复跑；问题在 **B6**：

`test_deadline_anchor_emission.py:151-162` 的 `_emit()`（`:133-148`）只调
`_sample_building_deadline_anchor_facts`，而该函数只取 `granularity=="building"` 的记录
（`sidecar.py:320-332, 366`）。**注册表里另有 2 条已登记锚点、却是 fragment 粒度的槽**
——`duration.delivery.deadline.to_ba`（锚 `repair.prescribed.completed`）与 `.to_person`
（锚 `repair.completion_report.submitted_to_ba`），它们走逐碎片路径而该路径 `sidecar.py:308`
**同样回写 `time_anchor_key`**（这正是本单「回填两例外」要打通的东西）。
⇒ **B6 结构上看不见带锚行总体的 20%**，而**乙12 翻车的恰恰就是这 20%**。
反向闸 `:87` 写 `len(records) >= 8`，而真实带锚条目是 **10** 条 ⇒ 两条碎片级带锚槽整个消失也不会红。

这不是「一个用例写窄了」——B6 被决议 §附记明确定为**形态 C 合法性的承重前置**
（「红了回退形态 A」）。它现在的射程是「楼级发射器的输出」，而它要保证的命题是
「同（作用域,锚）候选唯一」。**两者不是一回事**，差额就是 §四那 60 条。
乙12 是靠位移量化偶然撞出来的，**不是靠这道闸**——同形状将来会静默复发。

📌 附：`emission.py:165-176 / 179-183 / 186-192 / 195-199` 四条都是
`for row in _emit(): assert …`，**内部无非空守卫**，`_emit()` 返回空则四条一起真空绿；
同文件 `:153`/`:206` 勉强兜底，但每条单看是真空的。建议一并收紧。

### 审核点 6：三处旧断言改红修复 —— **过**（三处都是「修断言」不是「绕断言」）

1. `test_derivation.py::test_deadline_same_day_as_truthy` → `test_deadline_same_day_as_zero_day`：
   ✅ 原用例喂 `value=True` 布尔断言 satisfied，**确实把 R1 那个反转当规格写着**；
   新用例断言 `0.0→satisfied`、`1.0→violated` 并加验 `operator=="=="`，**是加强不是放松**，
   而且它走的是**老 slot 通道**（`slot_id="anchor.visit"`），顺带守住了老通道没被改坏。
2. `test_registry.py::test_to_person_slot_has_cop_confirmed_threshold`：
   ✅ 原断言锁 `unit="calendar_day"` 与 `repair.completion_report_and_mbi4.submitted_to_ba`，
   我亲取锚点册证实后者**不在 19 条里**、卡侧真锚是 `repair.completion_report.submitted_to_ba`
   ⇒ **原断言锁的确是错的一方**。改法正当。
3. `test_registry.py::test_sidecar_measurement_registry_present` 条数 11→19 ＋ 槽名集合：
   ✅ 我亲跑实数 19 条，8 个新槽名逐字对得上。这是纯计数事实。
4. `test_deadline_anchor_binding.py` 的二元组适配：✅ 只新增 `_bind` 辅助（`:90-100`）＋ 6 处调用换名，
   **原有断言一条没删、没改阈值、没加豁免**。`:99` 的 `assert status is None` 属实能当场炸。
   ⚠️ 但该文件的事实全由 `fixtures.make_fact` 造、`provenance` 默认 `{}` ⇒ `time_anchor_index` 恒空
   ⇒ ambiguous 分支**在本文件结构不可达**。诚实说法是「给将来加带锚事实留的静默改语义防线」，
   不是「本文件检验了碰撞策略」——记录那句「顺带把意外触发碰撞策略变成当场炸」应照此收敛。

### 审核点 7：位移量化口径 —— **过**（一处口径须补）

- **按锚点分组的义务数**：✅ **我实数了，15 行逐行等于记录的「义务数」列**
  （93/66/63/60/60/60/30×5/23/14/12/2，合计 603）。分组小计也对：
  甲 8 锚 = 252、乙11 = 60、乙12 = 60 ⇒ 372；丙 = 30+66+63 = 159；挂起 = 12+60 = 72；372+159+72 = 603 ✓。
  satisfied 167 ＋ violated 145 = 312 = 252 + 60 ✓。
- **「占位分布不可引」标注**：✅ 记录 §三 与 `分布授权草案` 双处都标了
  `PLACEHOLDER_pending_distribution_authorization_20260805`，并明写「可引的只有脱离条数与确定判定条数
  这两个结构性数字」。草案 §二/§四把回填点也列了。这条做得对。
  ⚠️ satisfied/violated 的 167/145 我**无法复现**（原脚本已删），但记录自己已声明不可引，故不构成问题。
- **乙11「一条量两个收件人」记账**：✅ `分布授权草案` §Q3 逐字在（(p) 該名由他人代為進行訂明修葺的人 /
  (q) 註冊承建商，两支永远同判、真实偏差建不出来），且 `registry.py:3198-3202` 的条目注释里也写了。
- **「8 条不是第 9 条」标签订正**：✅ 记录 §五.1 在。**但权威数据文件里那句话没跟着改**——
  `registry.py:3193` 仍写「故本条是甲类**八锚之外新增的第 9 条**」。按「锚数」读它不算错，
  但§五.1 恰恰是说这个「甲 8 ＋ 第 9 条」的说法是要订正的那个。两处并存会再骗一次人，须统一。

---

## 三、必须修清单（4 项）

| # | 事项 | 位置 | 性质 |
|---|---|---|---|
| **M1** | **B6 判据射程扩到「全部带锚条目」（10 条），不止 `granularity=="building"` 的 8 条**；反向闸 `>= 8` 同步改成绑定真实集合。理由：B6 是形态 C 的承重前置，现在结构上看不见让乙12 翻车的那一半 | `test_deadline_anchor_emission.py:87, 133-162` | 验收判据缺陷（本单最重） |
| **M2** | 引证行号 `§2.1.3(i) L849` → **L858** | `registry.py:2990` | 权威数据文件里的造假来源 |
| **M3** | 改写两处**与实测不符**的陈述：①`registry.py:3194-3196` 把一个 `sidecar_bool_slot_registry` 里不存在的布尔门陈述成存在；②`test_deadline_anchor_supply.py:311-313` 自述「双向钉死」而两条是同一面 | `registry.py:3194-3196`、`test_deadline_anchor_supply.py:311-313` | 陈述与实测不符 |
| **M4** | 实施记录三处口径订正：①红先行那句注明测的是**哪个配置**（纯 HEAD 上 6 条全堵在绑定失败，反转一次未复现）；②层一锚定批补 `fragment_count_per_building`；③`registry.py:3193`「第 9 条」与 §五.1 的订正统一 | 实施记录 §二、`registry.py:3193` | 口径 |

**不列入必修、建议进台账**：老 slot/measure 通道仍留 `return facts[0]`（`obligation_deriver.py:2309-2315`）；
`emission.py` 四条真空可绿的循环断言；#9 引文丢触发前件；#9/#11 两个 carrier_slot 声明而不产出。

---

## 四、附加决策点：乙12 的 60 条 —— 表态

## **表态：甲（`.to_person` 改楼级粒度），但挪到换池批与分布授权一并落；本单以现状过渡。**
## **前置条件：M1 必须先补，否则过渡期没有闸看着它。**

### 🔴 先纠一条：**乙按记录写的形态（「纯追加、不动既有槽」）实测无效**

我构造了三份 FactPack 直调 `_bind_deadline_fact`（锚 `repair.completion_report.submitted_to_ba`，
`relation=same_day_as`）：

| 场景 | 构造 | 实测返回 |
|---|---|---|
| 乙（记录写法） | 1 条楼级新槽行 ＋ 7 条 `.to_person` 碎片行，**两者都带该锚** | `(None, 'ambiguous')` —— **没解决** |
| 乙′ | 同上，但把 `.to_person` 的锚点登记**摘掉** | `('BLD', None)` —— 绑上 |
| 甲 | `.to_person` 改楼级 1 行/栋 | `('BLD', None)` —— 绑上 |

机制清楚：楼级作用域的索引不按碎片过滤（`validator.py:1187` 主索引取整包，
`:1410-1414` 只有碎片索引才过滤），所以只要 `.to_person` 还声明着这个锚，
它那 7 行就仍在候选里，候选数 1→8，碰撞策略照样触发。
⇒ **乙不是纯追加，它必须同时对既有槽做减法**：摘掉 `.to_person` 的 `time_anchor_key`
（顺带把本单刚做的 R5 改名一起作废），`.to_person` 随即成为无锚死数据。
记录 §四给乙的代价栏（「纯追加、层一保持绿」）**低估了**。

### ① 甲的换池级影响是否可接受 —— **可接受，且代价已经付了**

**本单本身就已经是换池级改动**：我实测 `registry_bundle_hash` `76caf0af…` → `d6e0eb01…`、
`deterministic_key` `6b984a3c…` → `3a0a8013…`。所以「甲会换池」不是新增代价，是**同一次换池里的另一格**。

甲的真实爆炸半径可以界定，不是未知数：1a-i′ 槽级子 rng 下，`.to_person` 只是从
`SIDECAR_NUMERIC(world, fragment, slot)` 换到 `SIDECAR_DEADLINE_ANCHOR(world, slot)`，
**其它槽的流一个字节不动**（这正是 1a-i′ 存在的理由）；`world.*` 实体表与 `proj.*` 也不受影响
（新旧两侧都不进任何规则阈值——动作 E 实测 `proj.threshold_evaluations` 逐字节相同、
`pass_bool` 逐行差异 0）。变的只有 `.to_person` 自己的行数（每栋 ~7 → 1）与取值。

甲会红的只有一处：层一细尺那条「基线每一行都必须在候选里存在」。但那是**本步的纯净性判据**
（`verify_rng_isolation_pairing.py:552-556`），不是架构不变量，在换池批里本就不适用。
语义上甲更正确——「同日送交完工报告」是楼级一次性行政事件，逐碎片抽本就是建模走样
（决议 §二引的 spec §3.1 正是这条）。

### ② 若选乙，双槽漂移怎么钉

先看上面那张表：乙必须变成乙′。若仍选乙′，钉法只有一条**结构化**的：

- 加静态闸「世界侧注册表里每个 `time_anchor_key` **至多被 1 个槽声明**」——这条闸能同时挡住
  「两个槽承载同一真实量」和「将来有人给第三个槽补同一个锚」；
- 并且**必须先做 M1**（B6 扩到全部带锚条目），否则粒度混搭的碰撞照样看不见。

但请注意：这两条闸做完之后，甲**自动满足**、乙′ 才勉强合规——
**能证明乙′安全的机制，恰好是让甲变得没有风险的那套机制**。加上乙′要废掉刚落的 R5、
留一个无锚死槽、还要另走一次分布授权，我认为乙没有站得住的理由。

### ③ 现状能否当过渡直接进换池批 —— **能，而且应该**

- 现状是**诚实的**：60/60 落 `blocked / ambiguous_fact_binding`，**零条被捏造成确定判定**。
  相比 base 的 `missing_time_anchor`，这个码信息量更高（锚确实供上了，不准的是粒度），
  且复用既有原因码、零新码、零消费者文档改动。
- 甲的实质是换池级改动，本就该与分布授权、批级数字**同一次换池**落——分两次换池是白付一次代价。
- 🔴 **但过渡有一个硬前置：M1 先补**。乙12 是靠位移量化偶然撞出来的，不是靠闸；
  过渡期若不把 B6 的射程扩到全部带锚条目，同一形状会在换池批里静默复发，
  而那时它已经混在一堆真数字里，比现在难查一个量级。

---

## 五、我没验的 / 验不了的（诚实边界）

1. **satisfied / violated 的 167/145 无法复现**——原复算脚本 `deadline_displacement.py` 已随 scratchpad 删除。
   记录自己已声明该拆分「完全由占位均匀分布决定，没有任何依据、不得引用」，故不构成审核障碍，
   但它意味着 §三那张表的右半边**没有留存证据**。
2. **备份 patch 不完整**：`杂物箱/备份_期限锚_20260805.patch` 是 `git diff`，
   **不含未跟踪文件**。`worldgen/rng_domains.py` 与 `scripts/verify_rng_isolation_pairing.py`
   都是未跟踪文件且本单都改了 ⇒ **这两个文件的动手前状态没有备份、无法还原**。
   （不影响本单结论，但「动手前的完整工作树 diff」这个描述不准确，下次要 `git stash -u` 或另存副本。）
3. 未连 Neo4j、未跑换池批、未跑大模型批（决议 §一.6 本就挂在换池批）。

---

## 附录：全量测试

`workflow_engine` ＋ `evo_agent_baseline` ＋ `agent_v1/tests` 一次跑齐（我自己跑的，非转述）：

```
3380 passed, 1 skipped, 11 xfailed, 4 warnings in 752.54s (0:12:32)
```

✅ **与实施记录声称的「3,380 通过 / 1 skipped / 11 xfailed / 0 失败」逐项吻合**（耗时 752s vs 记录 646s，机器差异）。

新增三个 deadline 测试文件单跑：**41 passed**（`test_deadline_anchor_supply.py` 21 例
＋ `test_deadline_anchor_emission.py` 14 例 ＋ `test_deadline_anchor_binding.py` 6 例），
与记录的 21/14 一致。
