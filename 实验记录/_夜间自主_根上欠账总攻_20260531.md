# 夜间自主：根上欠账总攻（2026-05-31 晚 起）

> **被压缩/唤起的我先读这个。** 用户授权："一次全都补上，我不想决策"——我自主拍板推进，醒来可推翻。

## 使命
把项目根上欠账尽量一晚补齐：DEBT-044/045（数据层两根）、DEBT-039/040（双源 KG）、真训技能闭环（evo 最大的根）、slot 0.19 下钻。感知层/真实数据不碰（外部约束）。

## 我替用户拍的决策（醒来可推翻）
0. **【用户追加授权 2026-05-31 深夜】今晚全自动，spec 裁决也由我直接做**。裁决原则：最小改动、忠实上位设计意图（a8 原文 + 用户既有拍板）、可回滚；每处 spec 修改在 spec 文件内留修订记录（日期+理由+"夜间自主裁决"标记），醒后可复核可推翻。
1. DEBT-045：走 ③评测下沉阈值级 + ①标定诊断出方案；**②若诊断显示聚合语义是最优修点，我直接裁决并改 spec 09 §3（留修订记录）**。
2. DEBT-044：按 spec 11 §3.3 修 src；若 spec 语义两可（candidate 粒度），**我直接裁决取与 spec 文本冲突最小的方案并同步澄清 spec 原文**（留修订记录）。
3. KG 灌库：先侦察现状再灌（防污染教训：绝不跑 `run_baseline_e2e_smoke`）；跟踪表可能 stale（RuleCard=397 可能已在库），以库实际为准。
4. 真训技能：件1 预算约束（`EVO_CLOSURE_QUERY_BUDGET`，patch 已在 `run_paired_real_pipeline.py`）+ 训练池跑批收轨迹 → induction → Gate → 注入对照。烧 token 跑批并发 6 + 流式落盘 + 可续跑。
5. 长 LLM 跑批我自己 Bash 后台跑（子代理跑长 LLM 会栽的教训）；确定性/诊断/修码活交编排子代理。

## 波次计划
- **波1（并行6路，进行中）**：A 重投影 4 seed ／ B 阈值级评测（DEBT-045③）／ C 标定诊断（DEBT-045①）／ D slot 0.19 下钻 ／ E DEBT-044 修根+测试 ／ F Neo4j+KG 侦察
- **波2（依赖波1）**：KG 灌库执行；DEBT-044 复核；件1 训练池跑批启动（后台，烧 token）
- **波3**：induction 真训技能 → 五道 Gate → 注入对照小规模验证
- **收尾**：实验记录 + 跟踪表/债文档双向同步 + 晨报

## 纪律（本会话血泪教训）
- **信落盘文件，不信惊慌**：GBK 乱码≠注入；判异常先逐条证伪（两次注入幻觉教训）。
- 数字必须真跑出来（落盘+Read 验证），绝不编。负结果照实记。
- 并行 Bash 会连坐取消 → 关键命令串行；被取消的 Write 不落盘 → Read/Glob 确认再声称。
- Neo4j 只在审过计划后写入；动 src 必配测试不破坏现有套件。

## 关键路径/事实速查
- 完整真值（seed42）：`杂物箱/w2_truth_full_noreject_gen_seed_42/`；重投影脚本 `杂物箱/reproject_w2_no_rejection.py`
- 数据池：`agent_v1/experiments/qa_reports/release_batch_evo_v11_data_pool_20260527/gen_seed_{1,7,42,99,123}/`
- Neo4j：bolt://127.0.0.1:7687 neo4j/<本机密码已脱敏>；sixthsense key `杂物箱/.sixthsense_key`，model deepseek-v4-flash-h，强制 stream+thinking-disabled，并发≤6
- 跑批主脚本：`杂物箱/run_paired_real_pipeline.py`（含件1 patch：EVO_CLOSURE_QUERY_BUDGET/EVO_CLOSURE_BATCH_FAMILIES；truth 覆盖 env EVO_TRUTH_DIR_OVERRIDE）
- 阈值真值健康：threshold_evaluations pass_bool ~30% fail / 70% pass（区分度在这层）

## ⚠️ 波2 挂死事件（2026-06-11 复盘）
波2 Workflow（wf_aaa9036d-42f）在 04:55 挂死——5 agent 各写到一半 jsonl 但 journal 无 completed，主对话长时间无交互→Workflow runtime 被回收。**教训：长任务不靠 Workflow（违反"长LLM任务自己Bash后台跑"既有教训）。** 溯源审计（agent a17c9ca36b）结论：
- **无残留需回滚、库未污染**（generator.py 是 5/27 codex 确定性修复非标定patch；registry.py patch 已 checkout 干净；那批 M 文件多为 3周前旧未提交改动）。
- **波2 已实质完成**：W2-1 metrics 口径修复 ✅（+7行，67 eval测试全过）。
- **波2 真没做完（需重做）**：W2-2 法规溯源（纯读未产报告）／W2-3 KG治理（只写了孤儿纯函数 derive_manifestation_flags 未接线未写库）／W2-4 标定验证（registry patch 已恢复但未产 s25_validation_report）／W2-5 技能边loader（skill_edge_loader.py 没生成）。
- 重做策略：纯分析/写码路（W2-2/W2-5）可子代理；连库写库长任务（W2-3）+ 跑生成池长任务（W2-4）**我自己 Bash 后台掌控**。
- 提交分组：06-11 簇分两 commit（波1-E DEBT-044 六文件 / 波2-W2-1 metrics 三文件）；旧残留独立。

## 状态（随手更新）
- 2026-06-11：波2 重做（挂死后）。W2-1 metrics 口径 ✅(67测试)。W2-2 法规溯源 ✅（**纠正波1-D"砍注册表"路线：15 slot 补卡13/砍0/存疑2 全有法规依据**；pull_test operator 反向定责 agent 卡错、chloride>0.8 非bug是trigger阈值、repair_mortar==7 谓词对是生成端产非7值、delivery==0 走拆分接线）。W2-4 标定 ✅（**S2.5 实测 verdict pass 31.36% 正中带，偏差-1.12**，7family均匀、5-bin健康、src精确恢复）。W2-5 技能边loader ✅（实现+15测试+dry_run；揪出库内9Skill缺结构化scope只能文本恢复4条边、kg.yaml db名evo_agent_baseline vs实际neo4j不符）。W2-3 KG治理 ✅（报告 杂物箱/kg_treatment_report.md）：清S00001污染批→重灌1500正批(无撞键/法规完好)→旗标72000满灌→**sidecar首次灌库(held_out 300楼,1200+67200)**→**技能边真灌4条APPLIES_TO(DEBT-039首次嵌边)**。库终态317757节点全S00042。**B路死结部分解开**：FactPack程序状态键0→19，但threshold义务仍100%open(触发器拿到程序slot仍判open)→新follow-up：下钻evaluate_trigger程序slot解析。

## ☀️ 晨报（用户醒来先读）

**一句话**：根上六大欠账，今晚补掉/推进了五个，全部基于真跑数据、决策有据可查、库与代码无污染。

**实打实的成果**：
1. **DEBT-044 修根完成** ✅：coverage-rejection 楼内裁剪→楼级取舍，772+337 测试全过，spec 11 补澄清段。
2. **DEBT-045 找到可落地解** ✅：S2.5 标定档真流水线验证 **verdict pass 31.36%**（从全 fail 救活到四态有意义），patch 备档。三修法方向全探到底（①标定可行/②不需动spec/③评测脚本就绪待sidecar）。
3. **5 seed 完整真值齐** ✅：apply_coverage_control=False 重投影，各 48000 条。
4. **metrics 口径 bug 修** ✅：slot recall 分子并入 measure_keys（0.217→0.470），67 测试过。
5. **法规溯源纠偏** ✅：波1-D"砍注册表"路线被否决——15 个零引用 slot 全有法规依据该补卡（补卡13/砍0/存疑2）；pull_test operator 反向定责 agent 卡错。
6. **KG 治理完成** ✅：清掉 5/31 误跑 run_baseline_e2e_smoke 的 3000 楼污染批；重灌 1500 正批；sidecar 首次灌库；**DEBT-039 技能→法规首次嵌边（rule-skills-kg-rag 结构成形）**。
7. **DEBT-039/040 跟踪表 stale 大幅修正**：法规侧 397 卡早在库、事实侧已灌——旧"几乎0%覆盖"论断作废。

**需要你拍/知晓的（晨起复核）**：
- 17 项夜间裁决见上方"波1后我的裁决"表（spec 11 §3.4 是我直接改的，带修订记录）。
- **B 路阈值评测覆盖率仍 0** 的最后一道闸：sidecar 灌了、程序事实进 FactPack 了，但 threshold 义务仍卡 depends_on_open_trigger——需下钻 evaluate_trigger 对程序 slot 的解析（疑似类 DEBT-041 错位，或程序步骤真未发生）。这是 verdict 维度端到端打通的最后一公里。
- S2.5 标定要全量落地需对正池重投影（会失去与旧池论文可比性，是否换血你定）。
- 待提交：本次改动分两 commit（波1-E DEBT-044 / 波2-W2-1 metrics），加 KG 治理脚本（杂物箱）。

**事故复盘**：波2 Workflow 04:55 挂死（主对话长时间无交互→runtime 回收）。教训：长任务用 Bash 后台不用 Workflow。挂死未造成损失（溯源审计确认无残留回滚、库未污染），四路已重做完成。
- 2026-05-31 晚：锚点立。波1 六路并行启动（wf_57194099-d80）。
- 波1 收齐（6/6 成功，87万子代理词元）。要点：A 5/5 seed 完整真值齐（seed1/7/123 有零星 pass 1/1/3 条）；B 阈值级评测覆盖率=0 的三层漏斗（L1 检索 23.1%/L2 operator 反向实锤/L3 触发器卡死=sidecar 程序状态事实没灌库）；C 真凶=duration.delivery.deadline==0 压死 97.6%（该 slot 5/10 已拍板废弃但规则仍绑旧 slot），margin 翻转证伪，推荐 S2.5 标定档（预期 pass 32.5%）；D slot 0.19 三层错配（修1 指标口径一行→0.470，修2 检索兜底→0.663，修3 制卡治理→1.0），事实检索零漏取；E DEBT-044 已修根（楼级取舍方案甲，772+337 测试全过）；F 库里法规侧 397 卡早已在（DEBT-039 表述过时）、**发现 5/31 误跑 run_baseline_e2e_smoke 的 3000 楼污染批（S00001，12 个 building_id 撞键覆写正批）**、技能嵌边 5 类边 schema 已定义 loader 未实现。

## 波1 后我的裁决（全自动授权，醒来可推翻）
| # | 事项 | 裁决 | 理由 |
|---|---|---|---|
| J1 | DEBT-044 candidate 粒度（方案甲楼级 vs 乙 fragment 级+W1 重采样） | **维持已实施的方案甲** | spec §3.3"外层请求新 W1 candidate"且 W1 生成单元=楼；甲已全测试通过、真值完整性达成；乙需 W1 编排大改（归 DEBT-031 gap4 留案） |
| J2 | rule-blind 审计白名单新增 validation.py→regulation_coverage_control 回链 | **认可** | 与用户 2026-05 拍板的 executor 回链同类；路径专属；桶/比例不回流 W1，rule-blind 本质未破 |
| J3 | 是否用修根代码重投影 v11 旧池 | **不重投影** | apply_coverage_control=False 的 5 seed 完整真值对实验等价（全量保留）；修根价值在未来新批次 |
| J4 | C 标定档位 | **S2.5**，但不直接动正池：先 N=2000 平行小池验证 pass 率落带（patch registry→生成→投影→实测→恢复 src 留 patch 档），通过后产 calibrated 平行池，旧池保留（论文可比性） | agent C 自己声明模拟是近似需小批实测；正池换血会让全部既有结论失去可比性 |
| J5 | delivery 拆分接线（正解） | 立案进 DEBT（接 DEBT-020 round5 尾巴），今晚不做 | 涉 W2 接线+分布族扩展，超一晚合理范围 |
| J6 | B-L3 触发器卡死 | **走灌 sidecar 程序状态事实**（held_out 子集），不做"触发器假定激活"评测模式 | 后者偏离生产行为；与 F 的 sidecar 未灌缺口同根，一修双得 |
| J7 | B-L1 检索命中 23.1% | **不替 agent 补短板**（这正是技能价值空间/evo 要改进的对象），评测照实；但 D 发现的命名错位锚（scope.component.covered vs in_scope 等 5 对）属数据/制卡对齐 bug，归治理 | 分清"评测对齐"vs"替 agent 补短板"边界 |
| J8 | operator 反向×2 + chloride 谓词方向 + mortar 等值脆性 | **立案并今晚溯源 MBIS 法规原文定责**（波2-W2 任务） | 实锤数据 bug 候选，定责后才能改 |
| J9 | D修1 metrics.py 分子并入 measure_keys | **改**（一行级+测试）；历史跑批指标不回填（口径以记录为准） | 指标口径 bug，threshold 义务身份在 measure_keys |
| J10 | D修2 检索兜底 | **不做**（同 J7） | 保持 agent 真实弱点可测 |
| J11 | D修3 15 个 slot 路线（砍注册表 vs 补卡） | 今晚**逐 slot 对照法规原文判定**（波2-W2 任务出判定清单），实施按清单后续 | 需法规定责不能拍脑袋 |
| J12 | 污染批处置 | **方案 A：全清事实侧+重灌**；执行前先补 rebuild_fact_kg.py 白名单缺 ManifestationFlag 的坑 | 撞键已覆写正批属性，结构性污染 |
| J13 | 灌库规模 | **先 1500 验证再扩**（F 报告推荐）；sidecar 灌 held_out 评测子集（兼修 J6）；12000 全量观察 heap 后另定 | 防 heap 教训重演 |
| J14 | research_kg 废弃骨架 | **移垃圾箱**（agent_mvp_已归档有同构副本） | 守垃圾箱规则 |
| J15 | 旗标派生接线进 loader 主流程 | **接线**（先查 fact_loader.py 未提交改动是什么再动） | 根治胜于手动回填 |
| J16 | 技能嵌边 loader（DEBT-039 真缺口） | **今晚实现**（schema 已定义 5 类边；先写+单测，真灌排在 J12/J13 之后） | rule-skills-kg-rag 核心，论文设计承诺 |
| J17 | spec 11 candidate 粒度澄清 | **我直接改 spec 11 补澄清段**（记修订记录：日期+裁决+理由+方案乙留案） | 用户授权 spec 裁决 |
