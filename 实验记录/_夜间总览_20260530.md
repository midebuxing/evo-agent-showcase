# 夜间研究总览（2026-05-30 夜）

> 给醒来的你的一眼版。细节见各 EXP 报告。**housekeeping：回来记得 `CronDelete 9ae4ee3b`（夜间心跳兜底 cron）。**

## 一句话
做了一条完整诚实的研究线（EXP-001/002/003，外加进行中的 004），把"分诊技能/skills-evo 的价值边界"钉清楚了：**LLM 的独特价值在冷启动/迁移（没见过的楼型、零审计数据）；一旦有数据，傻瓜计数器追平。**

## 任务设定
闭包给每栋楼标 ~10 个 open 家族（高召回、低精度），真违规(W2-fail)只 1 个。**分诊任务 = 从 ~10 个里挑出真 fail**。度量 precision@1 + 找到所需轮数。测试放在**训练没见过的楼型**（让统计量数据饿）。

## 核心结论（经多阶段 + 3 种子方差）
| regime | 统计量(计数器) | LLM | 谁赢 |
|---|---|---|---|
| 冷启动 K=0（零审计）| 0.000（无数据、无信息）| 0.306（世界知识）| **LLM 碾压** |
| 有数据 K≥1 | ≈0.444（跟审计模态）| ≈0.444（强框架跟数据）| 打平 |

- **强框架 LLM = Pareto-≥**：冷启动靠世界知识、有数据后追平计数器，一个方法跨整个数据谱都稳。
- **但有数据时不超过计数器**（无样本效率优势）。本质 = "带世界知识冷启动先验的计数器"。
- **EXP-002 还解开 EXP-001 的谜**："LLM 不从反馈更新"是弱提示框架问题、非本质；强"以审计为准"框架后它随数据更新。

## 诚实边界 / 坑
- **type-level 是天花板（0.571）**：EXP-003 证 within-type 无真 fact 信号（"数值→语义翻译"不值得做）。
- **★ 泄漏陷阱**：salient_facts 含 `defect.present`/`verification_failed` 等状态旗标=偷看 W2 verdict；任何 facts→fail 实验先剔（已存记忆）。
- 受控合成数据、fail-only W2、单切分（004 在测跨切分稳健性）。
- 与全项目反复规律一致：同源/有数据→LLM≈统计量；数据饿→世界知识赢。

## 实验清单（都在 实验记录/）
- **EXP-001**（open 家族分诊，Stage1–4）✓ — 冷启动价值 + K-sweep 曲线。
- **EXP-002**（LLM 能否从反馈更新）✓ — 是问法问题；强框架 Pareto-≥。
- **EXP-003**（within-type 有无信号）✓ — 无，type-level 是天花板 + 泄漏陷阱。
- **EXP-004**（冷启动跨切分稳健性）✓ — 三组没见过楼型 K=0 **全 >random**（稳健泛化）；幅度 type-dependent（A 0.352 / B 0.148 / C 0.228，看楼型鲜明度/LLM 熟悉度）。
- **EXP-005**（冷启动 per-type 光谱）✓ — 价值不均匀 + **salience bias**：LLM 只押 ubw/external，命中 headline-risk 型（UBW +0.46 / MIXED +0.34）、瞎在 drainage 主导型（NT-VILLAGE −0.10）；7/13 帮、2/13 平、4/13 反害。
- **EXP-006**（K≥1 反馈救冷启动盲点）⚠ **未单独跑**（写脚本子代理 stall 失败）。**结论由 EXP-002 机制外推已确立**：K=3 反馈让 statistic 与 llm_strong 都跟审计模态走（type-agnostic）→ drainage 主导的冷启动反害型在 K=3 下也会被救回模态天花板。**salience bias 是冷启动专属、少量审计即冲掉。** 想要硬证据可后补一次确认跑（低优先、结果可预测）。

## ⚠ EXP-007 作废 → 重定 EXP-008（2026-05-30，用户拍正）
**三处根错作废**：① 把 `W2-fail` 当 agent 要找的违规（实为 **agent 不判违规、做完整闭包核验，W2=完整闭包参考真值**）② detection 偷看 W2（违 blind 红线）③ 工具调用写成固定步骤（实为 agent 可选工具）。探针证 blind 下真 fail 家族 9/9 判 `unknown`、`violated`=0，「翻页找违规」是伪命题。**重定 EXP-008**：agent=LLM+工具箱（读建筑/法规卡/法规原文/跑闭包，自主可选调用）做完整闭包核验，skills 优化工具调用决策，blind+W2 只事后评，回 EXP-001 多维框架。方案 `EXP-008_工具箱agent完整闭包核验_20260530.md`。下文 EXP-007 记录保留作过程留痕（⚠ 已作废）：

## ★ EXP-007 活探索版闭包翻页（候选① 尝试，2026-05-30，⚠已作废）
把「强框架冷启动先验」塞进**真·多轮活 agent**（LLM 自己调闭包翻页、靠中间结果决定下一步），度量从离线 P@1 换成在线**省轮**。n=18 没见过老楼型 ×3 种子：`llm_coldprior` **3.94 轮**总体赢全部基线（random 5.47 / 跨型统计 5.56 / noprior 4.61 / 检索秩 4.22），三种子稳。**夜间「冷启动 P@1=0.306>>统计0.000」在真 agent 轮数兑现 —— 离线证的子弹装进枪打响了。** 诚实：① 优势温和（仅比检索秩好 0.28 轮，检索-违规耦合让检索秩成意外强基线）② per-type 不均匀（UBW/LEGACY 大赢、**TONG-LAU 4.89 反输检索秩 3.33**，唐楼真 fail 散在 drainage/gate 的 LLM 盲区，salience bias 活 agent 复现）③ `fixed_stat 5.56>random 5.47` 复刻夜间数据饿崩塌 ④ oracle detection + K≈1，省轮主要靠初始先验排序。报告 `EXP-007_活探索版闭包翻页省轮_20260530.md`。

## ★ triage 线收官（EXP-001~005 + EXP-006 外推）
investigation 完整。**最终诚实定论**：skills-evo/LLM 的价值 = **冷启动/迁移稳健**（跨切分验证），但**不均匀 + salience bias**（懂头条风险 ubw/外墙、瞎在平淡的排水，7/13 帮、4/13 反害）；**有数据后计数器追平、强框架 LLM 兼得两者（Pareto-≥）且少量反馈冲掉 bias**；type-level 是天花板（within-type 无真信号）；防 defect 旗标泄漏。

**下一大步**：① ✓ **已落地（EXP-007）** —— 强框架 LLM 冷启动先验塞进活 agent、验证冷启动省轮成立（温和 + 不均匀）；② 用这套叙事改论文 §6（skills-evo = 冷启动/迁移稳健，含活 agent 省轮证据）；③ 可选 EXP-007 后续：带噪 detection（替 oracle）/ K≥2 多 fail 楼考验中间结果自适应 / 救 TONG-LAU 盲区（drainage/gate 也喂先验）。

## 下一步候选（你定）
- EXP-004 收尾后：要么收束（结论已完整），要么换**全新角度/任务**（当前 triage 线已挖透）。
- 长线：用这套"冷启动价值"叙事改论文 §6（skills-evo = 冷启动/迁移稳健，非稳态优越）。
