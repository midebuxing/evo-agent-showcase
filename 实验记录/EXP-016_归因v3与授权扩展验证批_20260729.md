# EXP-016 归因 v3 + 责任登记表 + 27 条精确目标授权 —— 验证批

**日期**：2026-07-29
**批次 ID**：`phase_e_verify_seed301_20260729`
**状态**：批已跑完（30/30，0 失败）；全批对账与对抗复核进行中，结论区待补。

---

## 一、三锚（已核实，可引用）

| 锚 | 值 |
|---|---|
| 池 | `agent_v1/experiments/qa_reports/_reanchor_50x1_seed301/gen_seed_301`（`pool_content_sha256` 已记入 manifest） |
| commit | `c02f2e75740e4eafacd94052c6edb32f7bb4a589` |
| `code_state_sha256` | `76d2479847c23431ba818289507a599895fe4f0263196bee631b755389cf2118` |
| 库 | `s25smoke`（隔离库；第一栋 `--wipe` 全量灌库） |
| 适用性 bundle | `13c3ccc6d17646f4a47ef5afbcefb19fa404658e97beae0ee6c4b4a428b2f50d` |
| 档位 | **确定性地板档**（`--llm` 未加）——管线下界，**不得用于五道门验收** |

⚠️ `code_state.workspace_clean = false`，`dirty_path_count = 40`。
**这是预期的**：本项目长期在未提交状态跑批，`code_state_sha256` 哈希的正是
「HEAD commit + 全部未提交改动的路径与内容」，锚就是为脏工作区造的。

## 二、本批相对上一批（`phase_d_cards72_seed301_20260728`）改了什么

同池、同库、同档位，唯一变量是代码与权威数据：

1. **归因策略 v1/v2 → v3**：规则顺序改为「继承 → 上游触发器堵死 →
   无槽时验证器码优先 → 槽轴 → 结构兜底」。新增原因码
   `upstream_trigger_blocked`（判据 `trigger_state == "blocked"`）与 `non_slot_handle`。
   **动因**：批 D 的 36,194 条 `no_slot_declared` 经逐条核实 **100% 是误判**
   ——每条义务身上都已带着验证器给出的更具体原因码，而结构兜底判据排在了它们前面。
2. **责任归属登记表接线**：59 槽（55 `system_unresolved` / 4 `professional_input_required`），
   带 `professional_action`。批 D 的 `professional_input_required` **恒为 0**。
3. **精确目标授权 55 → 82**（+27 张已裁定为 `precise_target` 的中置信卡），
   `card_applicability_manifest` 与 `applicability_bundle` 已重建。
4. **卡包契约闸接进批跑前置**（fail-closed + 3 条显式豁免，带解除条件）。
5. `SLOT_ID_RE` 词根白名单 +5（`artifact`/`assessment`/`fire_safety`/`maintenance`/`ubw`，
   有蓝图依据）；`family_index.card_ids` 补齐 13 族 / 55 卡。

## 三、开跑前钉下的可证伪预测

| 预测 | 依据 |
|---|---|
| 授权扩展排除 **8,942** 条 unknown、保留 **1,023** 条 | 落盘前磁盘态复验（`ApplicabilityBundle.early_exit` 真实路径） |
| `professional_input_required` ≈ **334** 条 | 离线用同一登记表在批 D 产物上重算 |
| `no_slot_declared` = **0** | v3 规则顺序下结构兜底只在验证器两码皆空时触发 |
| 判定零变化（除早退导致的义务不产出外） | 归因层是纯旁路，调用点在 `allow_stop` 之后 |

⚠️ **我此前有一次预测错得离谱**：用事实侧 `component_type_key` 当片段身份的替身，
预测排除 12,914 条，运行时实测 8,942（差额 3,972 = 身份清单里标 `unknown` 的片段，
`8,942 + 3,972 = 12,914` 精确闭合）。**上表的 8,942 是修正后、用权威身份清单算的。**

## 四、批级实测（已确认）

```
完成 30 / 失败 0
义务总数        176,066   （批 D 183,580，−7,514）
closed          76,961    closed_ratio 0.4371
open            29,595
blocked         69,510
applicability_v3  授权卡 82 / 可信身份 fragment 142（= 200 − 58 身份 unknown）
```

⚠️ **义务总数下降 7,514 与「排除 8,942 条 unknown」不是同一个量**
——早退让部分义务**根本不产出**而非产出后标不适用。两者关系待对账确认，
**引用时不得混用**。

### 消费者文档（「优雅可用」的直接交付物）

| 节 | 批 D | 批 E | 差 |
|---|---:|---:|---:|
| 未闭合项 | 760.7 KB | 18.9 KB | −741.8 |
| 建议补充 / 检查资料 | 287.3 KB | 8.8 KB | −278.5 |
| unknown 归因 | 23.8 KB | 30.7 KB | +6.9 |
| **单栋合计** | **1,072.3 KB** | **58.8 KB** | **−94.5%** |

全批 30 份合计 1,746 KB，均 58.2 KB，最大 60.8 KB。

**批后又做了一次去重（尚未跑进产物）**：原实现把整段样板解释**重复打进每一行**，
改成抽公共后缀在表上方说一次。用**新渲染器**对批 E 同一份数据重渲染 30 栋：
**1,746 KB → 1,423 KB（−18%），均值 58.2 → 47.4 KB**，零信息损失。

⚠️ 这个数是**离线重渲染**得出的，**不是批产物**——下次跑批才会落进产物。
（本项目栽过一次：拿离线重渲染的数当基线去比批产物，差点误报回归。）
⚠️ 过程中我自己犯了两个错，都是量出来才发现的：①公共后缀在**全部分组**上算，
而只渲染前 N 组，一个离群组就把后缀打没（实测省 0.2 KB）；
②`_longest_common_suffix` **自身有索引 bug**（`ref[n-1-m]` 里 `n` 每轮在缩），
**两条输入时恰好不触发**，单元层无感。已补至少三条输入的单测 + 变异验证。

⚠️ **别拿「36 KB」当基线**——那个数是瘦身当天**离线重渲染**出来的，
且那次渲染还没有新原因码与责任分节。**批 D 的真实产物是 1,072 KB。**

首次出现的「需要专业人员提供」行动项（首栋，13 条归并为 4 项）：

| 条数 | 槽 | 请提交 |
|---:|---|---|
| 6 | `scope.component.covered_by_large_attached_signboard` | 提交现场量测记录，并附标注展示面积、被遮盖外墙范围的相片或位置图。 |
| 4 | `procedure.investigation.proposal.refused_by_ba` | 提交建筑事务监督对详细调查建议不予认可的书面通知、电子回执或可核验档案编号。 |
| 2 | `procedure.person.decides_to_proceed_with_investigation` | 提交有关人士在获悉拒绝后仍决定继续详细调查的签署指示、会议决议或书面确认。 |
| 1 | `procedure.person.informed_of_ba_refusal` | 提交向有关人士发送拒绝通知的邮件／信函记录及送达、签收或收悉确认。 |

## 五、全批对账（四个维度已完成，第五个在途）

### 5.1 预测 vs 实测

| 预测 | 实测 | |
|---|---|---|
| 授权排除 **8,942** 条 unknown | **−8,942**（27 卡 13,937 → 4,995） | ✅ 精确 |
| 「身份==目标」保留 **1,023** 条 | **1,023**，键级集合 D∖E=0、E∖D=0、零状态变化 | ✅ 精确 |
| `no_slot_declared` = 0 | **0** | ✅ |
| 判定零变化 | satisfied 1,154→1,154、violated 220→220 **逐格不变** | ✅ |

### 5.2 unknown 全链路守恒（口径：义务条数，两批各 30 run 全量）

```
义务总数 −7,514 = 消失 9,133（D-unknown 9,116 + D-closed 17）+ 新增 1,619（全部 closed）
unknown  −9,189 = 不产出 −9,116 + 产出后转非 unknown −73（58 NA + 14 satisfied + 1 violated）
```

**归因分解**（三处改动各自的贡献）：

| 来源 | Δunknown |
|---|---:|
| 27 条精确目标授权（一阶） | **−8,942** |
| ↳ 根触发器被排后**级联消失**的子义务 | −174 ⚠️ 推测 |
| 别名桥 `defect.drainage.misconnection.present → drainage.misconnection.present` | **−247** |
| 归因 v3 / 责任登记表 | **0**（只换标签，不改数量） |
| **合计** | **−9,189** ✅ |

⚠️ 那 174 条：`9,116 − 8,942 = 174`，而消失集里 `inherited_from_root` **恰好 174 条**，
数字严丝合缝，但「一阶＋级联」这个解释**未证**，标为推测。

别名那 −247 的落点：`inspection.drainage...report_wrong_connection_to_ba` −154、
`investigation.drainage...misconnection_trigger` −93；按槽
`defect.drainage.misconnection.present` −128。
这两张卡的**义务总数也降了**（600→513、360→331）——不是丢义务，是别名接通后
触发器终于能求值、**求值为假时下游动作义务本就不该产出**。

### 5.3 🔴 最容易被误读的一格：`no_slot_declared` 归 0 是**标签退休**，不是缺口修复

迁移矩阵（双侧均 unknown 的 99,105 条联查）：

```
no_slot_declared 36,194 → upstream_trigger_blocked 22,992 / missing_satisfaction_binding 3,881
                          artifact_not_modeled_upstream 2,251 / missing_artifact_evidence 1,649
                          missing_measurement 1,016 / missing_time_anchor 591 / ...
                          随早退消失 3,534
```

**底层世界缺口一个都没被归因 v3 修复。** 价值在**诚实**（不再对 36,194 条说
「系统从未针对它查过事实」这句假话），不在**减少**。

附带：`non_slot_handle` 零命中是**正确的**——它与 `no_slot_declared` 同在
「验证器两码皆空」分支，而实测 100% 的无槽义务都带验证器码 ⇒ 该分支结构上不可达。
`attribution_input_missing` 0，30 栋报警全 0。
归因守恒：键集与 unknown id 集**逐 run 完全相等**，`degraded` 全 False。

### 5.4 验收③「适用条款不漏」：召回一格没动

召回 **0.7483 / 过门 0/10**，与批 D **逐位相同**；585 条漏项构成一字节没变
（`retrieved_no_evaluation` 543 / `not_modeled` 30 / `wrong_structural_na` 12），
两批唯一差异是 2 行 `evaluated_unknown → evaluated_determinate`。

**结构性原因**：今晚四项改动没有一项能进入③的判据面——
归因只改 unknown 的标签，而阅卷器把 `evaluated_unknown` 记作**已覆盖**；
卡包两批同为 470 张、`candidate_rule_cards` 同为 470/栋，检索面没动。

🔴 **`wrong_structural_na` 12→12，不构成警报，但有一条擦边警告**：
结构 NA 行 6,283 → 7,844（**+24.8%**），抹掉 8,959 条实义记录；
其中 **14 处落在真值项作用域内、涉 8 个去重真值项**，全部楼级、
全部指向同一张卡 `rc.mbis.repair.drainage.ri.repair.s5_6_4_list_periodic_maintenance_components.c01`，
状态一格未翻——**属擦边未死**。
⇒ **再加一批精确目标授权，这 8 项是最先会掉的。**

### 5.5 责任二分（验收②的另一半）

`professional_input_required` **0 → 334**（30 栋合计），
按原因码分布 `inherited_from_root` 182 + `slot_not_supplied` 152，
恰好来自 4 个槽；`responsibility_registry_present` 30 栋全 True。

### 5.6 ⏳ 在途

不变量与红线自检（判定权红线跨批逐义务比对、文案成立率独立复核、
新静默失效横向对账）——结论落定后补。

## 六、诚实边界

1. **确定性地板档，不是基线本体**——不得用于五道门 / 收官验收。
2. 与批 D 可比的前提是同池同库同档位；**换阅卷口径即不可比**。
3. 本批仍有 3 张卡 `slot_role_map` 为空（语义裁定已完成，见
   `团队文档/我的笔记/裁定_三张空slot_role_map卡_20260729.md`，
   处置须改卡本体、走完整同步链，本批未做），批跑前置以显式豁免放行。
