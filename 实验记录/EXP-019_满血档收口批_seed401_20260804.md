# EXP-019：满血档收口批（reporting_axes_seed401_llm_v4_final）

**日期**：2026-08-04（凌晨）
**终批目录**：`agent_v1/experiments/reporting_axes_seed401_llm_v4_final_20260804`
**池**：`gen_seed_401`（EXP-018 同池）／**库**：`s25smoke`／**模型锚**：`qwen3.5:latest`
（digest 6488c96f…，清单 `llm_model_resolved` 已核）／**契约**：v4
**性质**：「baseline 优雅可用」的收官验收批——用户拍板「验证属收官范围」后发出。
**🔴 与全部历史批不可比**（池语义已换；批根有声明；禁旧真值评分——同 EXP-018）。

## 一、三轮沿革（第一轮的 0/30 是本记录最重要的教训）

| 轮 | 批根 | 完成 | 叙述接纳 | 定性 |
|---|---|---|---|---|
| 1 | `…llm_v4_20260804` | 30/30，0 失败 | **0/30** | 判定面全健康；叙述层全灭 |
| 2 | `…llm_v4_zh_20260804` | 30/30，0 失败 | **15/30** | 开 `EVO_ZH_AUTHORITY=1` 后仍被 11 张缺席卡黑洞 |
| 3 | `…llm_v4_final_20260804` | **30/30，0 失败** | **30/30** | 诚实降级落码后全通 |

**第一轮 0/30 的根因链（离线逐条复现钉死，不是猜的）**：
v4 预渲染 fail-closed 于第一点 → 证据包 22/22 张卡引文全为占位符 →
`_first_quote_for_card` 已被 DEBT-071 硬化为「只取中文权威正文、绝不回退译文」（正确）→
**但 `EVO_ZH_AUTHORITY` 开关缺省关、发批时没人开** → 历史批 30/30 接纳全靠当时还在的
英文译文回退。⇒ 模型行为无异常（tool_call 中位 16.5、零强制收尾、零泄漏），
纯配置断层——「关键配置静默退化」bug 族的「从未被开启」变体。

**第二轮 15/30**：11/470 张卡中文权威正文**显式缺席**（附录表格类＋§3.3.2(b) 四张），
任一被模型引用即整篇 fallback。**修法＝渲染层诚实降级**（2026-08-04 落码＋3 条变异测试）：
显式缺席卡的「法规依据」行渲成卡号引用＋缺席说明——**不是占位符冒充引文**
（2026-07-23 那道防线原样保留：卡整个缺失仍整篇 fallback；正常引文逐字不变）。
离线用第二轮 15 栋被拒载荷验证：**15/15 全渲通、20 行降级引用**，随后终批实测 30/30。

## 二、终批数字（冻结）

- **完成 30/30、失败 0**；义务总数 **272,380**——与前两轮**逐位相同**
  （同池同码不同配置，判定面确定性成立）。
- 判定面：closed 114,102（closed_ratio 41.9%）／open 66,507／blocked 91,771。
  开放原因前三：`artifact_state_not_valid_evidence` 35,674／`depends_on_open_trigger`
  14,449／`missing_satisfaction_binding` 5,902；新码 `diagnostic_binding_not_valid_evidence`
  **1,969**（B 批收窄在满血档的形态）；blocked 前二：`missing_rule_edge` 59,192／
  `qualifier_conflict` 28,096。
- **五道门**：①`tool_call>0` 30/30（见下方观察）②blind：零泄漏、零强制收尾、
  状态迹全正常 ③门③不适用（用户 2026-07-26 拍板）④可复现三锚齐
  （池 `3e551ce3…`／commit c02f2e7+工作树／库 s25smoke）＋
  **`code_state_selfcheck.equal=True`**（满血批首次）＋ 桶表摘要锚在清单
  ⑤数字冻结＝本节。
- **D 门**：幽灵槽 45→**39**（改善；`GHOST_SLOT_BASELINE` 下调已列批后队列）。

## 三、消费者文档终形态（抽查实录）

- 「需要你补充的资料」在**文首第 8 行**（v4 行动项前置＋第四要素「连带解开 N 条」）；
- 满血叙述段（模型生成、程序渲染）：G 组聚合、每义务证据行＋**逐字中文法规引文**
  （§3.3.2(e) 排水缺陷列举整段在场）；
- 新原因码消费者文案正确（「读数已取得，但这类读数经裁定不能证明本义务已履行」）；
- 降级引用行（缺席卡）格式如设计——无占位符冒充。

## 四、如实记录的观察与边界

1. **tool_call 全批均一为 4**（第一轮中位 16.5、最大 52）。形式过门（>0 且决策链落盘），
   但行为显著变化——疑与 zh 证据包形态/上下文占用相关，**未定根因**，列观察不列缺陷。
2. 判对率（阅卷「判得对不对」）未在本批验证——门③不适用之外的维度，
   残余 50 误评的止血与 55 单元消费授权（任务 #20）是下一步。
3. 1,140 条消费通道 unknown 在本批如实呈现（35,674 的一部分）——供给已到位、
   授权工单已备，落地后这批会转确定判定。
4. 11 张缺席卡的中文正文：§3.3.2(b) 四张属定位器坑**可救**（已列队），
   附录表格类属真难引（降级引用为长期形态）。

## 五、「baseline 优雅可用」四验收对照（我的评估；宣告权在用户）

| 验收 | 状态 | 本批证据 |
|---|---|---|
| ① 一键跑批 | ✅ | 单命令发批，预检/灌库/闸链全自动 |
| ② 产物完整可信 | ✅ | 30/30 零失败、五道门、三锚＋自检、批根不可比声明 |
| ③ 数字可引 | ✅ | 本记录冻结＋清单锚（含桶表摘要首落批） |
| ④ 阅卷可信（四毛病） | ✅（边界内） | 毛病一三通道落地（批 I 11,442 停发）；毛病二供给闭环＋满血档呈现；毛病三行动项文首＋488 条指名道姓；毛病四误评 149→50 实测、残余已裁定并备好止血 |

**残余清单（不影响宣告条件的判断，但如实列出）**：A/B 止血落表（候选已编）、
消费授权 #20、GHOST_SLOT_BASELINE 下调、§3.3.2(b) 定位器、敏感性审计器、
「以較早者為準」拆卡排查、真值 7+3 报告包送裁、s2_1_3_n 意向族缓议。

## 复算入口

- 三轮批清单与产物各自目录；根因复现脚本：scratchpad `repro_v4b.py`／`diag15.py`
- 诚实降级：`report_contract_v4.py`（`_resolve_v4_points`/`render_v4_points`）＋
  `tests/test_v4_absent_quote_degradation.py`（3 条变异）
- zh 权威源：`ingest/zh_authority.py`（`EVO_ZH_AUTHORITY`，470 卡/11 缺席）
