# EXP-008｜满血 LLM 基线首跑（五道门验收）

**日期**：2026-07-08 立项（用户目标钩子："完成刚才讨论出的 baseline，使用本地大模型，有部署"）
**定义回链**：基线本体 = 满血 LLM 档（`agent_v1/docs/glossary.md`"满血版 vs evo 探索版"节，2026-07-08 拍板）；
完成判据 = 五道门：①`tool_call>0` 审计+决策链落盘 ②blind 审计 ③verdict 有区分度 ④三锚定可复现 ⑤数字冻结。
**授权**：用户出门前明示"决策没必要等我，和 codex 讨论一下就可以"——设计决策由主会话+codex 合议直落。

## 方案：四战役结构

| 战役 | 内容 | 关联门 |
|---|---|---|
| A 供给侧-决策D | 流程槽粒度语义（行政槽楼级采样+进度槽聚合派生），spec 草案→codex 两轮评审→实现→重生成池→批跑 v8 | ③ |
| B 供给侧-artifact | missing_artifact_evidence 2,229 绑定层错位诊断修复（疑第八例"登记了没接线"）| ③ |
| C LLM 档就绪 | 本地模型接入验证 + 审计载体确认 | ①② |
| D 首跑+验收 | 满血档 30 栋批跑 → 五道门逐项 → 数字冻结 | ①-⑤ |

## 战役 C 结果（2026-07-08，先行完成）

**基建盘点**（子代理，48 工具调用）：LLM 档全套现成——`llm_client.py`（OpenAI 兼容，默认
`http://127.0.0.1:11434/v1` + `qwen3.5:latest`，env `EVO_AGENT_LLM_*` 可覆盖）；
`llm_orchestrator.py` 11 工具循环（取事实/取规则/闭包核验/查悬开/钻义务/查卡/查条款/搜法规/
查 fragment/按槽取值/交报告）；`run_baseline_e2e_smoke.py --llm` 开关；分叉点
`run_orchestrator.py:320-430`；**判定权两档共守**（`allow_stop` 无论哪档都由确定性验证器出，
LLM 只编排+写自然语言报告）；审计全套（`llm_tool_call_count`/`llm_tool_log`/`llm_turns`/
`llm_forced_finalize`/`status_trace`/泄漏审计 6 项自动跑）。

**本地部署探活**：Ollama 11434 活，`qwen2.5:7b` + `qwen3.5:latest` 两档；基线用 qwen3.5。

**单栋冒烟（本地 qwen3.5，seed213 池，s25smoke）**：✅ 端到端通——`tool_call_count=21`，
链路合理（取事实→取规则→闭包核验→查悬开→钻义务×13→查卡原文×2），泄漏审计干净。
发现：8 轮上限打满被强制收尾（`llm_forced_finalize`），模型没来得及自主 `finalize_report`。
处置：`max_tool_iterations` 兑现 env 覆盖（`EVO_AGENT_LLM_MAX_TOOL_ITERATIONS`，3cd1dab）——
满血档按"查询次数不设限"用高位兜底（64）跑，默认 8 主线不变。

**满血档运行参数（D 战役用，先记）**：
`EVO_AGENT_LLM_MAX_TOOL_ITERATIONS=64`｜`EVO_AGENT_LLM_MODEL=qwen3.5:latest`｜
`EVO_AGENT_NEO4J_DATABASE=s25smoke`（或 v8 后新库）｜池与 commit 锚定待 D 战役定格。

## 战役 A 进展

- 草案 v1（行政槽楼级采样 + 进度槽聚合）→ codex 一审"**需先修正后实施**"，四处硬伤：
  ①`investigation.started` 楼级化错（W0 采样依赖部位缺陷上下文+法规 §4.2 绑定缺陷范围）
  ②"广播"机制现有 DAG 不存在（`sidecar_upstream_state` 是 fragment 局部变量）
  ③"W2 完全不消费流程槽"过头（数值 sidecar 有联结+产 `sidecar_join_status`）
  ④`building_aggregate` 载体新值会被 DTO 字面量校验拦 + 绑定优先级不识别。
- v2 修订：①移聚合组 ②§3.4 采样循环反转设计 ③健康门加两监控 ④载体用 `"building"` 字面量+
  `aggregation` 标记+fragment 作用域过滤。
- codex 二审裁定"**有条件可**"：a 反转与 round6/7 条件层可共存（条件公式只吃传入上游 dict），
  重构落点=_sample_sidecar_bool_slots_for_fragment + 外层循环；b **证伪我方"building 先于
  sidecar"断言**（实为同级 rank 3，会混绑 ambiguous）→ 处方拆级 building=3<sidecar=4；
  c 过滤落点 validator._fragment_index、只滤 aggregation=='building'（勿伤普通楼级事实）；
  d 跨粒度边三条不全 → 补五例（intention_notified/supervision_representative.planned ⇐
  inspection.completed；repair.completed ⇐ revision_required 广播向；final_inspection ⇐
  repair.completed+supervision 三槽）。
- 处方 b/c 已落地（c6094f2，202 测试绿）；W0 侧实现（registry 双字段+聚合表单一来源+
  sidecar 两相分派+楼级行全链运输+fail-fast 禁静默漏边）已派 codex 执行；实现层安置优化：
  聚合读数由生成侧派生落行（检索零改动）。

## 战役 B 结果（2026-07-08 闭环，提交 aff8676）

诊断（子代理，30 工具调用，对账精确到条）：**第八例"登记了没接线"，三洞同函数**
`evaluate_evidence_requirement`（obligation_deriver.py:913-993）——检索运输链干净（源卡与切片
逐字段一致），坏在派生层消费：
①`artifact_ids` 卡内局部编号（art01/art02）不经本卡 `workflow_operands.artifacts` 注册表解引用，
当键直查 → missing_artifact_mapping 1,217 条 100% 误报（全部可解：549 条解到 17 键映射表且事实在包、
668 条应转正当的 artifact_not_modeled_upstream）；
②读不存在的 `req['slot_ids']`（实名 `slot_ref_ids`）且不走 slot_role_map 解引用——死链，DEBT-041 同族；
③`measure_keys` 通道不存在。
mae 2,229 分账：83% 带可绑引用被忽略（719 条解后事实就在包里本可判值）、17%=381 条源卡三引用全空
（法规卡内容缺口，工单候选）。B/C 类证伪：无别名断链、无漏灌。
另 artifact_not_modeled_upstream 956 条全部命中设计内"未建模 8 键"（A 类正当，非 bug）。

修复：三通道解引用落地（spec §6.3.6 既有引用语义兑现，不引入新判定语义）+5 测试，867 全绿。
尾巴：`record.supervision_checklist` 单键 1,296 条要不要 W0 补建专属槽（DEBT-049 邻接）；
381 条空引用卡清单待列（工单 q7 候选）；多证物"取一"行为合规格性未核。

## 供给侧连环对账（v8 → v9 → 两镣铐，2026-07-08）

**批跑 v8**（seed214 池，决策 D + DEBT-052 生效后）：
- DEBT-052 三预期精确命中：mam 1,217→**0**、mae 2,229→582、未建模转正 +674（预告 668）；
- 决策 D：missing_rule_edge 4,185→2,511；**但 ambiguous_fact_binding 只降到 827**——下钻揭示
  两层残余：①触发器/证据槽绑定路径从不做 §6.4.3 作用域分级（`scoped_facts` 机器**零消费者，
  第九例**），楼级求值把聚合行与 4 条 fragment 行混判歧义（inspection.prescribed.completed ×372
  不降反升）；②文档类槽（report.inspection ×162 等）同构病未在决策 D 射程。

**修复二连**（6c2f060）：①触发器+证据路径接线 `scoped_facts` ②聚合行发射面扩到全聚合表
（12 槽）+ 补两文档槽入表。1023+867 全绿。

**批跑 v9**（seed215 池，健康门三项过：pass 34.5% / 联结零漂移 / 楼级行 50×25 整）：
- blocked 账户接近清空：missing_rule_edge **8,021→192**（战役起点的 2.4%）、歧义 914→**373**、
  blocked 总量 13,358→4,092；
- **N1 仍全 unknown（94 对）**——逐组下钻找最接近闭合组：`repair.general_selection_and_
  classification` 族 26/65 未闭，只剩**两副镣铐各 ×13**：
  - 镣铐A `reporting.artifact.prepared` 缺事实 → **第十例"登记了没接线"**：mapping.slot_targets
    段（该槽由四 sidecar 接口 any_of 供证）整段无消费者。修复=按四接口 artifact 类
    target_slot_ids 并集做双极回退派生（32d04ba）；
  - 镣铐B `kind=method` 义务（action_not_fact_bound）→ 工单 q5 的 7 张卡 method_keys_allowed
    空缺；供给面已验证在位（pull_test ×20 / core_sample ×8 等九类 method 事实）——本地法规卡
    专员按转述者定位执行 q5 中。

## 供给侧连环战 v10 → v13 + 阈值配对突破（2026-07-08）

| 轮 | 变更 | 关键对账 |
|---|---|---|
| v10 | q5 填卡（pull_test×2）+ 镣铐A首版（无键联合派生）| 镣铐A修复姿势错：消费端要"特定证物已备"（带 artifact_key 限定符）→ qualifier_conflict ×1950 |
| v11 | 三案齐落：method 语义（空集=vacuous NA / ["*"]=开放集哨兵）+ 逐键派生 + 量纲五跳运输 | **阈值义务史上首批确定判定**（satisfied 46 / violated 2）；但无限定符消费端撞 12 条逐键行 → 歧义回摆 2,049 |
| v12 | 三层发射（fragment逐键 + 楼级逐键聚合 + 楼级无键联合，后两层 aggregation 标记走 rank3）| missing_rule_edge 2,505→270；歧义仍 2,937——揭示 artifact 绑定路径没接作用域分级 + 证据解引用丢限定符 |
| v13 | 歧义双叉根治（artifact 路径接 scoped_facts + 证据解引用携 qualifiers）| 歧义 →1,110；blocked 总账 13,358(v5)→5,538 |

**spec 钦定的墙（战略转折点）**：最近闭合组（general_selection）残余全是"专业判断类动作"义务
（consider_repair_selection_factor 等），spec §6.3.10.4 明文"均为 open + missing_fact，不得
satisfied"——**全宇宙家族闭合此路不通、且是设计使然**。按 EXP-011 留档的既定路线切换到
**阈值子集对齐口径**（truth 本就按阈值子集打分，同测量基底才是对称比较）。

**第九至十一例"登记了没接线"（本轮机器逐个逼出）**：
- 九：`scoped_facts`（spec §6.4.3 作用域分级机器）建成后零消费者——触发器/证据/artifact
  三条绑定路径全部裸绑，楼级聚合行与部位行混判歧义；
- 十：`mapping.slot_targets` 段（reporting.artifact.prepared 由四 sidecar 接口 any_of 供证）
  整段无消费者——被比较族最大扣押物；
- 十一：**阈值配对键**——`compute_threshold_metrics` 循环只读 `slot_ids`，而阈值义务身份在
  `measure_keys`（同文件注释早已明写）→ threshold_compared_pairs 恒 0。

**阈值配对激活（e01a23c）**：v13 产物离线复算 30 栋——**compared 2,564 对**（永恒 0 → 2,564），
算子一致 91.7% / 阈值值一致 49.9% / pass_bool 一致 46.1%（离线口径方向性数字，正式数字以
批内 eval 报告为准）。§8.4.3 同测量基底区分度指标就位，五道门③的合法开口。

## 战役 D：满血 LLM 档 30 栋首跑（发起于 2026-07-08 深夜）

运行参数（三锚定前置）：本地 Ollama `qwen3.5:latest`｜`EVO_AGENT_LLM_MAX_TOOL_ITERATIONS=64`
（满血"查询次数不设限"高位兜底）｜池 `_exp008_v11_smoke_50x1_seed216/gen_seed_216`｜
库 s25smoke｜代码 e01a23c。批跑驱动已切 `--llm`。

## 结果：首跑完成 + 五道门验收（2026-07-08 深夜，冻结）

**运行**：30/30 栋成功，全程约 93 分钟（本地 qwen3.5，单栋约 0.5-6 分钟）。

**三锚定（门④）**：池 `_exp008_v11_smoke_50x1_seed216/gen_seed_216`（seed216，truth pass 28.5%
健康带内）｜代码 `e01a23c`｜库 `s25smoke`。

**五道门逐项**：

| 门 | 判定 | 证据 |
|---|---|---|
| ① tool_call>0 + 决策链落盘 | ✅（附注）| 29/30 首过（max 37 次/栋）；C-0020 编排瞬态失败（status_trace=failed、无 LLM 轮次），单栋重跑干净（tool_call=12、report_ready）。每栋 llm_tool_log/llm_turns/status_trace 落盘。12 栋触 64 轮上限强制收尾（记 llm_forced_finalize，审计可见）|
| ② blind 审计 | ✅ | 泄漏审计六项 30/30 全过（0 栋 invalid_due_to_answer_leakage）；运行时双闸（ingest guard + source audit）全程在岗 |
| ③ verdict 有区分度 | **🟡 实质达成/字面待裁** | **阈值子集口径（同 truth 测量基底）**：配对 2,564 对、阈值值一致 **1.000**、通过布尔一致 **0.933**、算子一致 0.916——真区分度。家族级 N1 仍 unknown 主导（104 对）：spec §6.3.10.4 钦定专业判断类义务恒 open、家族全宇宙闭合系设计使然（楼级另有 13 例确定 not_applicable）。字面"非全 unknown"按哪一级读，留用户裁 |
| ④ 三锚定可复现 | ✅ | 见上；批跑驱动+聚合脚本在案 |
| ⑤ 数字冻结 | ✅ | 本节即冻结页；跟踪表同步 |

**冻结成绩单（满血 LLM 档，30 栋）**：
- 工具调用：总量跨栋 12-37 次/栋，链路（取事实→取规则→闭包核验→查悬开→钻义务→查卡/条款）；
- 阈值子集：threshold_compared_pairs 2,564 / value_match 1.000 / pass_bool_match 0.933 / operator_match 0.916；
- 家族级（保守头条）：N1 104 对 unknown 主导（unknown->fail 70 / unknown->pass 34），归因账本齐（专业判断类 spec 钦定 + DEBT-049 建模轮未启动项）；
- 供给侧终账：blocked 13,358（v5）→ 5,538（missing_rule_edge 8,021→270、歧义 914→1,110）；
- 判定权/blind：allow_stop 全程由确定性验证器出（两档共守），LLM 只编排+写报告。

**对照系图景就位**：满血 LLM 档（本页）↔ 确定性档（EXP-011 v5 管线地板参照）↔ 未来 evo 探索版
（预算受限+技能优化）——三方对照的两个固定端完成。

## 追加：门③用户裁定（选项3）与可核验子宇宙口径实测（2026-07-08）

用户裁定：门③按**选项3——家族级·可核验子宇宙归约**读（agent 行为/判定权/allow_stop/全宇宙
保守口径全不动，阅卷侧新增口径：归约剔除 spec 钦定无事实通道的 kind=action 义务）。

实现与实测（30 栋重阅卷，确定性档同池同库同 commit——闭包基底与 LLM 首跑同源）：
- 三口径并列落 eval 报告（全宇宙 / verifiable_subuniverse{fragment,building} / 阈值子集）；
- **粗族级：仍 unknown 主导（104 对同全宇宙）**——剔除专业判断类后，压制主力换成
  "可核验但 W0 未建模的事实"（如排水检验 coverage/identify 类），全部归 DEBT-049 建模轮领地。
  口径本身的价值兑现：unknown 从"混着不可答条款的糊账"变成**纯建模缺口清单**；
- **细族级首现确定判定**：样组（排水粗族）8 个细切片 3 个 pass（repair/validate/method，
  satisfied+NA 构成）——机器全链贯通的直接证据；
- 阈值子集口径维持区分度主力（2,564 对 / 值一致 1.000 / pass_bool 0.933）。

**门③终局定位**：选项3口径下粗族级达成待 DEBT-049 建模轮；细族级+阈值子集已具区分度。
跟踪表 QA-Baseline-EXP008-FiveGates 维持 🟡（口径已定、路径已定、数字如实）。

## 插曲登记

重阅卷批首发时 Neo4j 无声宕机（本会话第二次），按既有程序 JAVA_HOME=zulu21 重启，
s25smoke 验活（50 栋完好）后重发，无数据损失。
